# Parity Program — Plan & Review Results (2026-06-24)

Generated by a 40-agent workflow: each of the 20 stages was re-confirmed against current code, then PLANNED (pending) or adversarially REVIEWED (shipped), and each assessment was independently verified by a skeptic agent. This is the authoritative companion to `2026-06-24-parity-program-20-stage.md`.

## Status summary

| Stage | Title | Mode | Verified status | Verify verdict | Effort |
|---|---|---|---|---|---|
| 1 | Voice post-reply safety screening | review | shipped-correct | sound | S |
| 2 | After-hours booking-target inference on voice | review | shipped-correct | sound | S |
| 3 | Voice returning-customer recognition (core; HCP note sub-deferred) | review | shipped-correct | minor-corrections | S |
| 4 | Chat customer-context persistence on linked sessions | review | shipped-correct | sound | S |
| 5 | Reconcile account-data verify gate across voice & chat | plan | real-gap | minor-corrections | M |
| 6 | HCP technician roster sync (mirror FieldPulse technician-sync) | plan | real-gap | sound | M |
| 7 | HCP durable availability sync (persist to technician_availability + cron) | plan | partially-done (gap genuinely OPEN; HCP availability is cache-only, no DB persistence, no cron — and a literal technician_availability mirror is structurally blocked) | minor-corrections | M |
| 8 | HCP address validation on customer sync | plan | real-gap | minor-corrections | M |
| 9 | HCP client rate limiter (mirror FieldPulse rate-limiter) | plan | real-gap | sound | M |
| 10 | HCP bulk operations + admin endpoint | plan | real-gap | sound | M |
| 11 | FieldPulse job line-items on push | plan | real-gap | sound | M |
| 12 | technician_skills table + CRUD UI | plan | real-gap | sound | M |
| 13 | Technician base location + proximity scoring | plan | real-gap | minor-corrections | M |
| 14 | Tunable confidence threshold (org-level) | plan | real-gap | sound | M |
| 15 | Failed-auto-assign reconcile sweep (cron) | plan | real-gap | minor-corrections | M |
| 16 | Real-time availability (PTO / sick / live load) | plan | real-gap | sound | M |
| 17 | Technician push notification (tech_assigned) | plan | real-gap | minor-corrections | M |
| 18 | Customer "your technician is X" messaging | plan | real-gap | sound | M |
| 19 | Membership plans edit flow (detail page + PATCH) | plan | already-closed | sound | S |
| 20 | Tech portal mutations (photos / notes / timeline) | plan | partially-done | minor-corrections | M |

---

## Stage 1 — Voice post-reply safety screening  _(review)_

**Verified status:** shipped-correct  **·  Verify verdict:** sound  **·  Effort:** S

**Evidence (re-confirmed against code):**
- src/lib/ai/voice-turn.ts:961 — single generateText call (the only free-form LLM reply on voice)
- src/lib/ai/voice-turn.ts:971 — `const screened = screenAssistantReply(text);` runs BEFORE TTS+persist (plan cited stale line 943-954; lines 943-959 are now the Stage-3 customerHint block)
- src/lib/ai/voice-turn.ts:978 — `const reply = toSpokenReply(screened.reply, { nearLimit });` uses screened text; toSpokenReply (phone-agent.ts:62-83) only strips markdown/escalation + optionally appends a fixed safe sentence, so it cannot reintroduce a price/booking claim
- src/lib/ai/voice-turn.ts:986 + :1019 — persisted assistant message and returned reply both derive from screened.reply
- src/lib/ai/output-guardrail.ts:107-134 — screenAssistantReply runs all four detectors: PRICE_REGEX, FALSE_BOOKING_REGEX, DANGEROUS_DIY_REGEX, CREDENTIAL_REGEX; dangerous-diy/credentials replaced first (highest risk)
- src/lib/ai/voice-turn.test.ts:763-791 — WS1 test asserts BOTH returned reply and persisted message are scrubbed of price + 'booked' (passes)
- All other 8 voice return points (lines 273/342/660/732/751/878/942) use deterministic templates (VOICE_OFFICE_REPLY/VOICE_SUBMITTED_REPLY/verdict/baseReply) — already safe, correctly bypass screening
- Tests green: output-guardrail.test.ts 60/60, voice-turn 'output guardrail' 1/1
- hvac-knowledge.ts untouched by parity work (last commit 294f441 is unrelated prompt tuning); screening wiring predates Stage 1 review (added in chatbot Stage 10, commit 6926777)

**Review:**

no issues (behavioral). The plan's re-confirmation is accurate: the voice LLM-fallback reply is screened through screenAssistantReply (all four detectors) before TTS and persist, the screened text is what's both spoken and persisted, and the deterministic template paths that bypass screening are genuinely safe. Both channels share the same single-source-of-truth detectors, which the eval gate also imports (run-eval.ts:20, golden-transcripts.ts), so runtime and CI cannot drift. metadata.verify, money-safety, and frozen safety text are not touched by this path.

Two minor non-blocking notes, neither a regression:
1) DOC DRIFT (minor): plan §Stage 1 cites `voice-turn.ts:943-954` for the screen call, but the actual call is line 971; lines 943-959 are now the Stage-3 customerHint block. The prose claim is still correct — only the line cite is stale. Worth fixing the cite to avoid the exact 'stale audit' failure the program warns about.
2) DETECTOR SCOPE (minor, pre-existing, shared with chat — NOT a parity regression): PRICE_REGEX = /\$\s?\d/ (output-guardrail.ts:20) only catches '$'-prefixed numerals. Spoken-form prices the LLM is *more* likely to emit on a voice turn — 'two hundred dollars', '200 bucks', 'around two-fifty' — pass screening unflagged. Because TTS never renders a literal '$', the pricing detector is effectively weaker on the voice modality than its 'post-reply safety screening' name implies. This limitation exists identically on chat, so voice↔chat parity (the stage's actual goal) is preserved; flagging only as a latent safety-coverage gap for a future detector-hardening stage, not as a Stage-1 defect.

**Key risks:**
- Spoken-form/word prices (e.g. 'two hundred dollars', '200 bucks') bypass PRICE_REGEX on voice — latent safety gap shared with chat, not a Stage-1 regression
- Plan cites stale line numbers (943-954 vs actual 971) — risk of future audits mis-locating the screen call

**Independent verifier — sound:**
- (no corrections — independently confirmed)

_Verifier notes:_ Independently verified every cited file:line against the real code; all check out.

CONFIRMED CLAIMS:
- voice-turn.ts:961 single generateText (only free-form LLM reply on voice); :971 screenAssistantReply(text) runs before TTS+persist; :978 toSpokenReply(screened.reply) uses screened text; :986 persist and :1019 return both derive from screened.reply. Verified by reading lines 930-1020.
- toSpokenReply (phone-agent.ts:62-83) only strips TAP_HUMAN_PATTERN/markdown/newlines and optionally appends a fixed safe escalation sentence — cannot reintroduce a price or booking claim. Confirmed.
- output-guardrail.ts:107-134 runs all four detectors (PRICE_REGEX, FALSE_BOOKING_REGEX, DANGEROUS_DIY_REGEX, CREDENTIAL_REGEX); dangerous-diy/credentials replaced first (lines 120-121). Confirmed.
- The 8 deterministic return points (273/342/660/732/751/878/942 + screened 1019) verified. Note: line 342 is a multi-line return with `reply: spokenReply` at :343 — assessment's inclusion of 342 is correct (my first grep missed it because the pattern spans lines).
- Eval gate run-eval.ts:20 imports PRICE_REGEX, FALSE_BOOKING_REGEX, screenAssistantReply from ../output-guardrail — genuine single source of truth shared by runtime and CI; golden-transcripts.ts also routes probes through screenAssistantReply. No drift possible.
- Tests green: output-guardrail.test.ts exactly 60/60; voice-turn.test.ts full suite passes; combined 87/87. WS1 test (voice-turn.test.ts:763-791) asserts both returned reply and persisted message are scrubbed of price + 'booked'.

MINOR NOTES BOTH VALID (non-blocking, correctly classified):
1) Doc drift: plan cites voice-turn.ts:943-954 for the screen call but actual is :971; lines 943-959 are now the Stage-3 customerHint block (loadCustomerContextById/buildCustomerContextHint). Confirmed by reading.
2) PRICE_REGEX = /\$\s?\d/ scope gap: empirically tested — 'two hundred dollars', '200 bucks', 'around two-fifty' all return false; '$200' and '$ 1,200' return true. Spoken-form prices bypass screening on voice. This is shared identically with chat (same module), so voice↔chat parity (the stage goal) is preserved; it is a latent cross-channel safety gap, not a Stage-1 regression. Correctly flagged as future detector-hardening, not a defect here.

No over-rating or under-rating found. No frozen-safety-text, money-safety, metadata.verify, or tenant-scoping invariants touched by this path. Status shipped-correct holds.

---

## Stage 2 — After-hours booking-target inference on voice  _(review)_

**Verified status:** shipped-correct  **·  Verify verdict:** sound  **·  Effort:** S

**Evidence (re-confirmed against code):**
- src/lib/ai/after-hours-chat.ts:144-159 — inferBookingTarget extracted as a shared exported pure helper (asap→now, morning/afternoon/evening→business_hours, then urgency fallback)
- src/lib/ai/after-hours-chat.ts:176-182 — business_hours bookingTarget returns offer_next_day and OVERRIDES the urgent branch (line 187-197), so an after-hours daytime-window request never gets disclose_charge
- src/lib/ai/voice-turn.ts:21 + 792 — voice imports inferBookingTarget and now passes bookingTarget: inferBookingTarget(merged.extras?.preferredWindow, "unknown") into decideAfterHoursDisclosure (the prior gap)
- src/app/api/chat/route.ts:18-20,1364-1373 — chat imports the same shared helper and calls it identically; git show 7d2783b shows the inline chat copy was DELETED (-36 lines), not duplicated
- src/lib/ai/extraction-schema.ts:63-68 — preferredWindow enum is exactly morning/afternoon/evening/asap, matching inferBookingTarget; voice-turn.ts:90 maps preferred_window→preferredWindow so the wiring is live, not inert
- src/lib/ai/voice-turn.ts:795-805 — voice only prepends + latches afterHoursShown in the disclose_charge branch; suppressed (offer_next_day) cases neither speak a charge nor latch
- src/lib/ai/after-hours-chat.test.ts — 19/19 pass incl. inferBookingTarget table (asap→now; morning/afternoon/evening→business_hours; not_urgent→business_hours; urgent→now; unknown→unknown)
- src/lib/ai/voice-turn.test.ts — 27/27 pass; git show 7d2783b confirms hvac-knowledge.ts NOT in the diff (frozen safety text untouched); tsc --noEmit clean for all three files

**Review:**

No issues. The shipped implementation is correct and matches the Stage 2 intent. Verified: (1) inferBookingTarget is a single shared pure helper (after-hours-chat.ts:144), the chat route's inline copy was removed (git show 7d2783b: -36 lines in route.ts, no duplication), and voice now computes+passes bookingTarget (voice-turn.ts:792) — true DRY parity. (2) The decision logic correctly prioritizes the business_hours branch over urgency (after-hours-chat.ts:176 before 187), so a caller phoning at 11pm for a tomorrow-morning slot yields offer_next_day, which voice maps to no charge-disclosure prefix — exactly the bug Stage 2 set out to fix. (3) The preferredWindow enum (extraction-schema.ts:63) matches the helper's matched strings and voice maps preferred_window→preferredWindow (voice-turn.ts:90), so the wiring is genuinely live. (4) Degrade-safety preserved: the ah-config load is wrapped in try/catch (voice-turn.ts:806-811) that skips disclosure on failure; inferBookingTarget takes `unknown` and returns \"unknown\" for unrecognized input, failing safe. (5) Latch only fires in the disclose_charge path (voice-turn.ts:797), so suppressed cases don't consume the once-per-session flag. (6) Frozen safety text untouched, no tenant-scope or money-safety surface touched, no metadata.verify interaction.\n\nOne intentional asymmetry worth noting (NOT a Stage 2 defect): voice hardcodes customerSignal: \"unknown\" (voice-turn.ts:788) whereas chat passes a real urgencySignal (route.ts:1372). This means voice's bookingTarget is driven solely by preferredWindow, never by a spoken \"it can wait\" reply — so voice cannot reach the not_urgent→business_hours no-charge affirmation that chat can. This is a pre-existing voice limitation (voice has never parsed a spoken urgency-answer into a CustomerUrgencySignal; it still feeds merged.urgency via the `urgency` field at line 787) and lies outside Stage 2's preferredWindow-scoped charter. It also fails SAFE — at worst voice asks/discloses rather than wrongly suppressing — so it is not a correctness or trust regression.

**Key risks:**
- Voice hardcodes customerSignal: "unknown" (voice-turn.ts:788), so a caller who SAYS "it can wait until tomorrow" without naming a window gets ask_urgency/disclose rather than the no-charge next-day affirmation chat gives. Pre-existing voice gap (no spoken urgency-signal parsing), fails safe, out of Stage 2 scope — track separately if voice↔chat after-hours parity is to be fully closed.
- Voice after-hours tests mock decideAfterHoursDisclosure (voice-turn.test.ts:86-92) while keeping the real inferBookingTarget, so no voice test directly asserts the bookingTarget arg value reaching the decision; correctness of the computed target rests on after-hours-chat.test.ts. Adequate but a wiring-level assertion (spy on the decision call args from a morning-window after-hours turn) would harden against future regressions.

**Independent verifier — sound:**
- Minor citation imprecision (not a correctness error): the assessment cites 'voice-turn.ts:90 maps preferred_window->preferredWindow so the wiring is live'. Line 90 is actually the VOICE_OPTIONAL_STEP_EXTRA latch map ({preferred_window: 'preferredWindow'}) used to mark a skipped optional step, not the extraction/merge step that populates merged.extras.preferredWindow read at line 792. The substantive claim (the preferredWindow extra is genuinely populated and consumed) still holds via the slot/extras pipeline, but the specific line-90 label is the wrong mechanism.
- Cosmetic: the assessment reports test counts as '19/19' (after-hours-chat) and '27/27' (voice-turn). I confirmed both suites pass and total 46 (2 files, 46 tests). I did not separately re-derive the per-file split, but the combined pass is verified and the git stat (after-hours-chat.test.ts +29, voice-turn.test.ts +12) is consistent.

_Verifier notes:_ Independently verified every load-bearing claim against the real code.

CONFIRMED CORRECT:
- inferBookingTarget is a single shared exported pure helper at after-hours-chat.ts:144-159 (asap->now; morning/afternoon/evening->business_hours; not_urgent->business_hours; urgent->now; else unknown).
- Decision priority is correct: business_hours branch (after-hours-chat.ts:176-182, returns offer_next_day) executes BEFORE the urgent branch (187-197), so an after-hours daytime-window request never reaches disclose_charge. This is exactly the Stage 2 bug fix.
- Voice wiring is live: voice-turn.ts:21 imports inferBookingTarget; line 792 calls inferBookingTarget(merged.extras?.preferredWindow, 'unknown') into decideAfterHoursDisclosure; line 787 feeds merged.urgency; line 788 hardcodes customerSignal 'unknown'.
- DRY parity confirmed via git show 7d2783b: route.ts -36 lines (inline inferBookingTarget DELETED, +1 import), after-hours-chat.ts +29, voice-turn.ts +6. The inline copy was removed, not duplicated.
- Chat route (route.ts:1364-1373) calls the same shared helper with a REAL urgencySignal (readUrgencySignal at line 804), confirming the noted voice<->chat asymmetry (voice hardcodes 'unknown').
- preferredWindow enum (extraction-schema.ts:63-68) is exactly morning/afternoon/evening/asap, matching the helper's matched strings.
- Latch only fires in disclose_charge path (voice-turn.ts:795); suppressed cases neither speak a charge nor latch afterHoursShown.
- Degrade-safety: ah-config load wrapped in try/catch (voice-turn.ts:777-811); catch logs and skips disclosure so intake continues.
- Frozen safety text untouched: hvac-knowledge.ts NOT in 7d2783b diff.
- tsc --noEmit: clean (no errors for after-hours-chat.ts, voice-turn.ts, extraction-schema.ts, chat/route.ts).
- Tests: 46/46 pass across the two suites.

keyRisks are both accurate and correctly scoped: (1) voice's hardcoded customerSignal 'unknown' means a spoken 'it can wait' without a named window can't reach not_urgent->business_hours — a pre-existing voice limitation that fails SAFE (at worst it asks/discloses rather than wrongly suppressing), out of Stage 2's preferredWindow charter. (2) voice tests mock decideAfterHoursDisclosure (voice-turn.test.ts:86-94) while keeping real inferBookingTarget, so no voice test asserts the computed bookingTarget arg reaching the decision; correctness of the target rests on after-hours-chat.test.ts. Both are correctly flagged as hardening opportunities, not defects.

No money-safety, tenant-scope, neon-http-transaction, or metadata.verify surfaces touched. The shipped implementation matches Stage 2 intent.

---

## Stage 3 — Voice returning-customer recognition (core; HCP note sub-deferred)  _(review)_

**Verified status:** shipped-correct  **·  Verify verdict:** minor-corrections  **·  Effort:** S

**Evidence (re-confirmed against code):**
- src/lib/ai/customer-context.ts:113-158 — loadCustomerContextById extracted (single tenant-scoped query via withTenant(customers, organizationId, eq(customers.id, customerId)); returns null on raced delete)
- src/lib/ai/customer-context.ts:103 — lookupCustomerContext now delegates to loadCustomerContextById (chat behavior unchanged)
- src/lib/ai/voice-turn.ts:952-959 — voice loads ctx by session.customerId and builds hint, degrade-safe via .catch(() => null)
- src/lib/ai/voice-turn.ts:963 — customerHint appended to PHONE_SYSTEM_PROMPT + slotContextHint for the LLM turn
- src/lib/ai/voice-turn.ts:971 — reply still routed through screenAssistantReply after hint injection
- src/lib/ai/voice-turn.ts:250-276 — do-not-service gate is upstream and independent of the recognition hint
- src/lib/ai/voice-turn.test.ts:393-423 — asserts [RETURNING_CUSTOMER] reaches call.system for a known caller; loadCustomerContextByIdMock called with ('org-1','cust-1')
- src/lib/ai/voice-turn.test.ts:443-451 — asserts no load + no hint when session has no customerId
- git diff 7d2783b..25e721c — hvac-knowledge.ts NOT in diff (frozen safety text untouched); 5 files, surgical
- vitest: customer-context.test.ts + voice-turn.test.ts = 46 passed; tsc --noEmit clean for both files

**Review:**

No issues. Adversarial review found the shipped implementation correct and within scope.

Checked and cleared:
- TENANT SCOPE: loadCustomerContextById uses withTenant(customers, organizationId, ...) (customer-context.ts:133) — no cross-org leak; voice passes the call's organizationId (voice-turn.ts:955).
- DEGRADE-SAFETY: the load is wrapped in .catch(() => null) (voice-turn.ts:957) and buildCustomerContextHint returns "" for null (customer-context.ts:234), so any failure leaves PHONE_SYSTEM_PROMPT byte-identical. A raced customer delete returns null, not a throw (customer-context.ts:138).
- PLACEMENT/NO-REGRESSION: the hint load sits on the LLM-fallback path AFTER the budget-exhausted early return (voice-turn.ts:908-943), so deterministic 0-token turns and budget-degraded turns never pay for it. lookupCustomerContext was refactored to call the extracted function (customer-context.ts:103) with identical query logic — chat path unchanged (chat/route.ts:680/691/1610/1618).
- SAFETY INVARIANTS: hvac-knowledge.ts is absent from the Stage 3 diff (frozen safety text intact). The hint-influenced LLM reply is still screened by screenAssistantReply (voice-turn.ts:971) for pricing/false-booking before TTS/persist. metadata.verify is untouched by this change (no gather/extraction code modified).
- PII: buildCustomerContextHint surfaces only firstName + counts/enums (customer-context.ts:236-255); fullName/email/phone/hcpCustomerId are explicitly kept out of the prompt per the documented contract (customer-context.ts:32-44).
- DO-NOT-SERVICE: handled by the separate upstream gate (voice-turn.ts:250-276); the warm-greeting hint does not weaken refusal/booking-block behavior.
- SCOPE: the HCP enrichWithServiceHistory note is intentionally sub-deferred (external fetch on the latency-bound spoken turn) and documented inline (voice-turn.ts:948-951) — consistent with the plan; not a gap.

**Key risks:**
- Minor (not a bug): every LLM-fallback voice turn re-queries customer context from the DB (voice-turn.ts:954) — one light query, but it is the plan's acknowledged latency tradeoff; a once-per-call cached hint on the session would remove repeated DB round-trips on the latency-bound spoken turn.
- The HCP prior-service note remains sub-deferred; voice returning-customers get name/count/membership but no 'last service' one-liner that chat gets — intentional, tracked as follow-up.
- eval 30/30 was not run headless (no keys); low risk since hvac-knowledge.ts frozen blocks are untouched and the appended text is the already-vetted chat hint.

**Independent verifier — minor-corrections:**
- Evidence imprecision (not a code bug): voice-turn.test.ts:423 asserts the literal '[RETURNING_CUSTOMER]' (underscore), but that string comes from a MOCK — voice-turn.test.ts:99-101 replaces buildCustomerContextHint with `(ctx) => ctx ? ' [RETURNING_CUSTOMER]' : ''`. The REAL buildCustomerContextHint emits '[RETURNING CUSTOMER]' with a SPACE (customer-context.ts:259). So the voice test proves the hint plumbing reaches call.system, but does NOT validate the real hint text; the real text is covered separately by customer-context.test.ts:246 ('RETURNING CUSTOMER'). The assessment's bullet ('asserts [RETURNING_CUSTOMER] reaches call.system') reads as if the real hint text were verified end-to-end in the voice test, which it is not.
- Minor line-range drift: do-not-service gate cited as voice-turn.ts:250-276; the actual gate block spans 247-278. Substantively correct (upstream, independent of the recognition hint, returns before router/LLM at line 273), just off by ~2 lines.
- Framing nit: 'chat/route.ts:680/691/1610/1618' is cited under 'chat path unchanged', but chat/route.ts is NOT part of the Stage 3 diff (diff = 5 files: plan doc + customer-context.ts/.test + voice-turn.ts/.test). The referenced lines exist and are accurate (680 loadCustomerContextById, 691 lookupCustomerContext, 1610 enrichWithServiceHistory, 1618 buildCustomerContextHint), but they are pre-existing chat code, not changes introduced this stage.

_Verifier notes:_ Independently re-derived: status is correctly shipped-correct. I opened all cited files and confirmed: loadCustomerContextById is tenant-scoped via withTenant(customers, organizationId, eq(customers.id, customerId)) (customer-context.ts:120-134), returns null on missing row (138); voice loads it degrade-safe with .catch(()=>null) and appends customerHint to PHONE_SYSTEM_PROMPT+slotContextHint (voice-turn.ts:953-963), AFTER the budget-exhausted early return (908-943), with screenAssistantReply still applied (971). PII contract holds: hint surfaces only firstName + counts/enums (customer-context.ts:236-263); fullName/email/phone/hcpCustomerId kept out. hvac-knowledge.ts absent from git diff 7d2783b..25e721c (verified). HCP enrichWithServiceHistory note is on the chat path (chat/route.ts:1610) and deliberately NOT on the voice turn (documented voice-turn.ts:948-951) — scope claim accurate. Ran the two suites: 46 passed. tsc --noEmit clean for both files. The keyRisks (per-LLM-turn DB re-query, HCP note deferred, eval not run headless) are all real and honestly flagged. The only defects are evidence-description imprecisions, chiefly that the voice test's '[RETURNING_CUSTOMER]' assertion is against a mock string, not the real '[RETURNING CUSTOMER]' hint — worth correcting so a future reader does not assume the underscore form is the production marker. No functional gap, no invariant violation, no over/under-rating of the actual implementation.

---

## Stage 4 — Chat customer-context persistence on linked sessions  _(review)_

**Verified status:** shipped-correct  **·  Verify verdict:** sound  **·  Effort:** S

**Evidence (re-confirmed against code):**
- src/app/api/chat/route.ts:672-689 — new linked-session branch: `if (session.customerId) { customerContext = await loadCustomerContextById(organizationId, session.customerId).catch(... return null) }` runs every turn, degrade-safe
- src/app/api/chat/route.ts:690-738 — unlinked email/phone branch unchanged: still links session (705-714) and enforces do-not-service mid-turn (717-737)
- src/app/api/chat/route.ts:425-458 — load gate selects only `customers.doNotService` via withTenant and refuses linked do-not-service customers BEFORE the context load, validating the 'already enforced above' claim in the 678-679 comment
- src/lib/ai/customer-context.ts:113-134 — loadCustomerContextById is tenant-scoped: `withTenant(customers, organizationId, eq(customers.id, customerId))`; null on raced delete (138)
- src/app/api/chat/route.ts:744-747 — seededName derived from customerContext.fullName; consumed by deterministic router at lines 1160 & 1260, so the fix repairs BOTH router and LLM paths
- src/lib/ai/customer-context.test.ts:193-227 — loadCustomerContextById unit-tested (maps row w/o contact lookup; null on raced delete); full suite 19/19 green
- tsc --noEmit exits 0; vitest customer-context.test.ts 19 passed

**Review:**

No issues. The shipped fix exactly matches the plan and is correct. Verified the core claims against current code:

(1) Correctness — the linked-session branch (route.ts:673-689) hydrates context by id on every turn via loadCustomerContextById, fixing the turn-1-only vanish. The unlinked path (690-738) is byte-preserved (lookup → link FK → mid-turn do-not-service). if/else makes them mutually exclusive; no double-resolution.

(2) Do-not-service safety — the branch deliberately skips re-checking doNotService, relying on the load gate at 425. I confirmed this is safe: grepped all `session.customerId` writes and `.update(customerSessions)` between lines 425 and 673 — only line 561 fires, and it sets `turnCount` only. `session.customerId` is identical at both gates, so the 425 gate (selecting `customers.doNotService` under withTenant) provably covers the linked branch. The comment at 678-679 is accurate (and the prior stale comment was correctly removed per the diff).

(3) Tenant scope — loadCustomerContextById uses `withTenant(customers, organizationId, eq(customers.id, customerId))` (customer-context.ts:133); no cross-org leak. Raced-delete → null (138), so a stale FK on a resumed session degrades to no-context, not a throw.

(4) Degrade-safety — every new call is `.catch(() => null)`/logged; a CRM blip never blocks the streamed reply. Matches the rest of the route.

(5) Invariants untouched — the diff (commit 225be10) only edits the context if/else and one comment. It does not touch metadata.verify, hvac-knowledge frozen safety text, or any money path. enrichWithServiceHistory (1610) sits only on the LLM-fallback path and short-circuits without an hcpCustomerId, so deterministic replies pay no new HCP round-trip and behavior is identical when HCP is absent.

(6) Verified green — tsc 0; customer-context unit tests 19/19. The route has no unit harness (plan acknowledges this), so route-level verification is via tsc + suite, consistent with the plan's stated verify method.

Minor non-issue (not flagged as a finding): linked turns now issue two reads of the same customers row — the 425 doNotService gate plus the 680 full-context load. They could be merged into one query, but both are indexed single-row reads and degrade-safe; this is a deliberate separation (early refuse before any work vs. later hydrate) and not a defect.

**Key risks:**
- Two indexed reads of the same customers row per linked turn (425 doNotService gate + 680 full context). Negligible cost, degrade-safe, intentional split — could be merged later but not a bug.
- Do-not-service correctness on linked sessions is load-bearing on the invariant that session.customerId is not mutated between line 425 and 673; verified true today, but a future edit inserting a session-link before 673 without re-gating could open a mid-turn window.

**Independent verifier — sound:**
- (no corrections — independently confirmed)

_Verifier notes:_ Independently verified every load-bearing claim against current code; all hold.

VERIFIED:
- Commit 225be10 ("fix(chat): persist returning-customer context on linked sessions (parity Stage 4)") exists; diff touches only route.ts (the context if/else + one comment) plus the plan doc — 37 lines, no invariant violations.
- route.ts:673-689 — new linked branch: `if (session.customerId) { customerContext = await loadCustomerContextById(organizationId, session.customerId).catch(() => { log; return null }) }`. Runs every turn, degrade-safe. Fixes turn-1-only vanish (prior code was `if (!session.customerId && (resolvedEmail || resolvedPhone))`, confirmed via git show).
- route.ts:690-738 — unlinked branch byte-preserved: lookup, FK link at 705-714, mid-turn do-not-service at 717-737.
- route.ts:425-458 — load gate selects only customers.doNotService via withTenant(eq(customers.id, session.customerId)), refuses linked do-not-service before context load. Comment at 678-679 ("already enforced by the load gate above") is accurate.
- customer-context.ts:113-134 — loadCustomerContextById tenant-scoped (withTenant), returns null on raced delete (138). No cross-org leak.
- seededName (744-747) consumed by deterministic router at 1160 (nameThisTurn) and 1260 (mergeSlots name) — fix repairs both router and LLM paths.
- enrichWithServiceHistory at route.ts:1610 short-circuits without hcpCustomerId (customer-context.ts:194), so deterministic/HCP-absent behavior is unchanged.

SAFETY CLAIM RE-DERIVED INDEPENDENTLY: the do-not-service correctness depends on session.customerId being unmutated between line 425 and 673. I grepped all customerSessions writes in that range: only the soft-guardrail update (561-563) fires, sets turnCount only, AND returns early at 570 (never reaches 673). The customerId write at 707 is inside the unlinked else-if and cannot precede the linked branch. Invariant holds.

VERIFICATION COMMANDS: tsc --noEmit exits 0 with zero output; vitest customer-context.test.ts 19/19 passed (tests at 193-227 exist and assert the exact mapped object + null-on-raced-delete).

The two keyRisks are correctly framed as non-defects (negligible double indexed read; the load-bearing 425-673 invariant, which I confirmed). No missed bug, no over-rated non-issue.

---

## Stage 5 — Reconcile account-data verify gate across voice & chat  _(plan)_

**Verified status:** real-gap  **·  Verify verdict:** minor-corrections  **·  Effort:** M

**Evidence (re-confirmed against code):**
- src/app/api/chat/route.ts:836 — `if (verdict.action === "ACCOUNT_LOOKUP" && accountCustomerId) {` then line 837 immediately calls `buildAccountLookupReply(verdict.intentId, ...)` with NO requiresVerify/ZIP check for any intent including account-data-balance / account-data-membership-status
- src/app/api/chat/route.ts grep: zero matches for `account-verify`, `requiresVerify`, `extractZipsFromAddress`, `checkZipMatch`, `VerifyState` — chat does not import the verify module at all
- src/lib/ai/voice-turn.ts:354 `if (!requiresVerify(intentId))` ... :365 else branch runs the full ZIP-verify state machine (lines 366-449): pending/passed/failed VerifyState persisted under metadata.verify, MAX_VERIFY_ATTEMPTS, VOICE_VERIFY_ASK/DEFER — voice DOES gate
- src/lib/ai/account-verify.ts:4-14 INTENT_SENSITIVITY marks account-data-balance + account-data-membership-status as "financial"; requiresVerify() returns true only for those — the shared, channel-agnostic gate predicate already exists
- src/lib/ai/chat-slots.ts:93-108 parseKnownSlots reads only ExtractionResult fields (issueType/address/name/...) — it ignores a top-level `verify` key, so chat metadata can safely carry verify state exactly like voice (session.metadata at route.ts:643,1275)
- Memory [[voice-chatbot-parity]] claim that ZIP-verify shipped on BOTH channels is FALSE; the audit (chat lacks it) is TRUE in current code

**Plan:**

GAP IS OPEN. Chat (src/app/api/chat/route.ts:836-889) serves financial account-data reads (balance, membership-status) to ANY identified session with no verification; voice gates the same intents behind a 2-attempt ZIP check. This is a real security-parity gap. The shared predicate (requiresVerify) and helpers (extractZipsFromAddress, checkZipMatch, VerifyState, MAX_VERIFY_ATTEMPTS) already exist in src/lib/ai/account-verify.ts — voice's logic is the template to mirror.

PLAN (mirror voice-turn.ts:291-451 into the chat ACCOUNT_LOOKUP block, text-channel-adapted):

1. src/app/api/chat/route.ts — modify the block at 836-889:
   - Add imports: `requiresVerify, checkZipMatch, extractZipsFromAddress, MAX_VERIFY_ATTEMPTS, type VerifyState` from "@/lib/ai/account-verify"; `customers, customerLocations` (customers already imported via schema — confirm), `decrypt` from the crypto util voice uses (src/lib/ai/voice-turn.ts imports `decrypt` + `customerLocations`/`customers` + `withTenant`/`eq` — copy that exact import set), and add 2 chat-specific copy constants CHAT_VERIFY_ASK / CHAT_VERIFY_DEFER (text-channel wording, no \"key in\"/DTMF phrasing; e.g. ASK = \"To pull up your account details, what's the 5-digit ZIP code on file?\", DEFER = \"I can't confirm that here right now — our team can follow up, or check your account online. Anything else?\").
   - Read verifyState: `const rawMeta = session.metadata ? JSON.parse(session.metadata) : null; const v = rawMeta?.verify` guarded exactly like voice-turn.ts:296-303.
   - Branch on requiresVerify(verdict.intentId):
     * NOT financial → existing behavior unchanged (call buildAccountLookupReply, persist; do NOT write a verify key — preserve existing).
     * financial → state machine: passed→serve; failed→DEFER (no re-ask); pending→read the user's message as the ZIP answer (text channel: `guardrailResult.sanitized`, NOT dtmf), checkZipMatch against on-file ZIPs; match→passed+serve; mismatch→attempts++ → >=MAX_VERIFY_ATTEMPTS ? failed+DEFER : pending(reasked)+ASK; no state→pending{attempts:0}+ASK.
   - On-file ZIPs: replicate voice-turn.ts:392-421 — select customers.addressEncrypted (withTenant(customers, organizationId, eq(customers.id, accountCustomerId))) + customerLocations.addressEncrypted (withTenant, eq customerId), decrypt each in try/catch, extractZipsFromAddress. Degrade-safe: decrypt/read failure → that source yields no ZIP (never throws to caller).
   - Persistence: when writing a financial-turn reply, merge `{ ...existingMeta, verify: newVerify }` into session.metadata via JSON.stringify (mirror persistAccountTurn at voice-turn.ts:328-341), keeping turnCount/updatedAt. Reuse the existing db.insert(messages)+db.update(customerSessions) pattern already at 852-862; return cannedTextResponse(replyText). NOTE neon-http: these are 2 separate awaits today (no transaction) — keep that, or db.batch([insert, update]) for atomicity (voice does sequential awaits; matching voice is acceptable). NO interactive transaction.
   - Tenant scope: every read/write uses withTenant(table, organizationId, ...) + the existing sessionScope — accountCustomerId is already org-resolved (route.ts:828-829).
   - Degrade-safety: buildAccountLookupReply already .catch→null with handoff fallback (843-848); keep. If ZIP reads throw, treat as empty ZIP list → mismatch path (never auto-pass).

2. Tests (TDD, vitest) — new src/app/api/chat verify suite OR extend an account chat test (template: src/lib/ai/voice-turn.test.ts:1025-1123 + src/lib/ai/bot-edge-cases-account.test.ts which is DB-mocked):
   - unverified chat session + account-data-balance → reply is the ASK challenge, NOT balance data (the stage's named verify gate).
   - correct ZIP on next turn → passed state persisted + balance served.
   - wrong ZIP x2 → failed + DEFER, never serves data; third financial ask still DEFER.
   - non-financial intent (account-data-next-visit) → served immediately, NO verify key fabricated (assert metadata.verify stays absent/unchanged — mirrors voice-turn.test.ts:1025-1072 invariant).
   - tenant: ZIP from another org's customer must not pass (withTenant scoping).

3. Verify gates: `npm run -s tsc`/typecheck, lint (must stay 0), `npm run test:unit` (new suite green), build. NO prompt changes to hvac-knowledge.ts frozen safety text → eval not required. Confirm money-safety untouched (buildAccountLookupReply/balanceReply unchanged — balance is read-only existing-invoice fact). Confirm metadata.verify survives merges (parseKnownSlots ignores it; financial writes round-trip it).

Effort M: ~80-120 LOC in one route block + ~5 tests; all helpers + the voice template already exist, so it is largely a faithful port.

**Key risks:**
- Metadata clobber: chat has MULTIPLE later customerSessions.update paths (route.ts:706,726,1078,1548,1789,1842,1918) that rewrite metadata from parseKnownSlots-derived slots — but the financial-verify block RETURNS early at the equivalent of line 885 before reaching them, so verify state persists only if the block writes it itself and returns. Must ensure the new code returns before any other metadata write, exactly as the current block does.
- Channel wording: voice copy says 'tell me or key in' / sets nextGatherMode:'dtmf_zip' — chat must NOT reuse VOICE_VERIFY_ASK verbatim (no keypad) and must read the next user message (sanitized text) as the ZIP, not dtmfDigits.
- Identity-gate interaction: accountCustomerId can be resolved THIS turn (customerContext?.customerId, route.ts:829). First financial ask issues ASK; the ZIP answer arrives a turn later when session.customerId may now be set — verify the customerId used for ZIP lookup is the same org-scoped id on both turns.
- Do not fabricate 'passed' on the non-financial path (voice invariant at voice-turn.ts:351-353) — a fabricated pass would let a later balance ask skip the check.
- neon-http: insert(messages)+update(customerSessions) cannot be wrapped in db.transaction(); use sequential awaits (as voice does) or db.batch.

**Independent verifier — minor-corrections:**
- INCOMPLETE PLAN STEP (the one real flaw): The plan handles persisting verify state only inside the ACCOUNT_LOOKUP block's own early-return path, but does NOT protect chat's OTHER metadata writes from clobbering the verify key on intervening turns. src/app/api/chat/route.ts:1286 (`metadataStr = JSON.stringify(extraction)`) and :1511 (`metadataStr = JSON.stringify(buildExtraction(...))`) rebuild metadata from a fresh extraction object that drops the top-level `verify` key — the EXACT clobber bug that voice fixes via preserveVerifyKey (called at src/app/api/voice/gather/route.ts:193; helper at src/lib/ai/account-verify.ts:45-70). Scenario: financial ZIP challenge issued (verify:{pending} written) → user sends an intervening intake message that hits the 1286/1511 write path → verify lockout WIPED → unlimited ZIP retries, defeating MAX_VERIFY_ATTEMPTS. The plan must wire preserveVerifyKey(extraction, session.metadata) into those chat writes (and the fresh-metadata re-read path at route.ts:1730-1797) to reach true parity. The plan's keyRisk #1 only covers in-block return ordering and misses this cross-turn write path entirely.
- Minor: keyRisk #1 cites metadata-write paths at route.ts:706,726,1078,1548,1789,1842,1918 — all 7 are real `customerSessions.update` calls (verified), but the ones that actually rebuild metadata and threaten verify-clobber are 1548 (uses metadataStr from 1286/1511) and the 1730-1797 re-read block; lines 706/726/1078 write status/turnCount or escMetadata, not a stripped extraction, so the risk framing slightly mislabels which writes are dangerous.
- Minor doc nit: plan says 'customers already imported via schema — confirm' and 'copy that exact import set' from voice-turn.ts. Confirmed `withTenant` (route.ts:21) and `customers` (route.ts:95) are already imported, and `eq` is in use — but `decrypt` and `customerLocations` are NOT imported in chat/route.ts and must be added (plan does say to add them; just confirming they are genuinely absent today).

_Verifier notes:_ Independently verified against the real code. STATUS is correct: this is a genuine open security-parity gap. Chat's ACCOUNT_LOOKUP block (src/app/api/chat/route.ts:836-889) calls buildAccountLookupReply for ANY identified session with zero verify check, including the financial intents account-data-balance / account-data-membership-status. Voice (src/lib/ai/voice-turn.ts:291-451) gates those same intents behind a 2-attempt ZIP state machine. The shared predicate requiresVerify() and INTENT_SENSITIVITY ("financial" for balance + membership-status) live in src/lib/ai/account-verify.ts:4-14, and helpers extractZipsFromAddress/checkZipMatch/MAX_VERIFY_ATTEMPTS/VerifyState all exist. Chat imports NONE of account-verify (grep: zero matches) — confirmed.

SPECIAL-NOTE contradiction RESOLVED with evidence: the voice-chatbot-parity memory's implication that ZIP-verify shipped on BOTH channels is FALSE; the audit (chat lacks it) is TRUE in current code. The assessment correctly resolved this.

CITED LINE REFS: accurate. route.ts:828-829 (accountCustomerId resolution), :836 (the unguarded ACCOUNT_LOOKUP if), :843-848 (.catch→null handoff), :852-862 (insert messages + update customerSessions), :885 (return cannedTextResponse). chat-slots.ts:93-108 parseKnownSlots reads only ExtractionResult core fields + pickExtras; verify is NOT in EXTRA_SLOT_KEYS or CONTROL_SLOT_KEYS (verified at chat-slots.ts:20-75), so parseKnownSlots genuinely ignores a top-level verify key — that claim holds. voice-turn.ts:354 (!requiresVerify branch), :392-421 (on-file ZIP read from customers + customerLocations with withTenant + decrypt + try/catch degrade), :436 MAX_VERIFY_ATTEMPTS, :447-449 first-ask pending — all accurate.

INVARIANTS: Plan correctly respects neon-http no-transactions (sequential awaits / db.batch, no db.transaction), tenant scoping (withTenant + sessionScope), frozen safety text (no hvac-knowledge.ts prompt change → eval not required), and money-safety (buildAccountLookupReply unchanged). Channel-wording adaptation (no DTMF/keypad copy; read sanitized text not dtmfDigits) is correct and necessary.

Plan is feasible and a faithful port of the voice template. The single substantive gap is the missed preserveVerifyKey wiring into chat's intervening-turn metadata writes — without it the lockout is resettable and full parity is not achieved. Hence minor-corrections, not sound.

---

## Stage 6 — HCP technician roster sync (mirror FieldPulse technician-sync)  _(plan)_

**Verified status:** real-gap  **·  Verify verdict:** sound  **·  Effort:** M

**Evidence (re-confirmed against code):**
- src/lib/integrations/housecall-pro/ (full listing): no technician-sync.ts — only technician-mapping.ts which maps to opaque synthetic ids, NOT a users-table roster upsert
- src/lib/integrations/fieldpulse/technician-sync.ts:51 syncTechniciansFromFieldpulse upserts into users (the pattern to mirror); no HCP analog exists (grep syncTechnicians* in housecall-pro returns nothing)
- src/lib/db/schema.ts:278 fieldpulseUserId column + schema.ts:322 uniqueIndex users_org_fieldpulse_user_id_unique exist; grep for housecallProUserId/hcp_user_id in schema.ts => NONE (no identity column for HCP techs)
- src/lib/integrations/housecall-pro/client.ts:514 listTechnicians() already exists and returns HousecallTechnician[] (the data source is ready)
- src/lib/integrations/housecall-pro/types.ts:197-204 HousecallTechnician = {id; name?; isActive?} — NO email, NO role fields; client.ts:279 toTechnician returns {id,name,isActive} only (does not capture email)
- src/lib/integrations/housecall-pro/scheduling-source.ts:25-31 getActiveTechnicianIds only counts opaque ids for availability — confirms HCP has no true roster, exactly as the plan states
- drizzle/meta/_journal.json last entry 0021_tearful_echo; next migration is 0022 (the 0034/0035 files in git status are an unrelated untracked FP working line, not committed)

**Plan:**

GAP IS OPEN. Plan to mirror FP technician-sync for HCP.\n\nKEY DESIGN DIFFERENCE (the trap): FP keys its upsert on (organizationId, EMAIL) and skips techs without email/name (technician-sync.ts:67-77, onConflictDoUpdate target [users.organizationId, users.email] at :100). HCP's HousecallTechnician (types.ts:197) carries NO email — so a naive copy cannot key on email. HCP /employees DOES return email (HCP employee API has email; toTechnician at client.ts:258 just doesn't capture it). Decision: capture email in the HCP tech type and key the upsert on (organizationId, email) for parity, while tracking identity via a new housecallProUserId column. Skip techs with no email (same as FP).\n\nFILES TO CREATE/MODIFY:\n\n1. src/lib/integrations/housecall-pro/types.ts — add `readonly email?: string;` to HousecallTechnician (line ~204). PII note already present for name; email is also PII (used only server-side for upsert, never crosses public availability surface — keep listTechnicians out of scheduling-source PII path; it already is).\n\n2. src/lib/integrations/housecall-pro/client.ts:258 toTechnician — capture email: `const email = str(obj.email);` and add to return {id,name,isActive,email}. Verify HCP employee payload field name (likely `email`); tolerant of omission (str() returns undefined). Add a client.test.ts case asserting email is parsed and omitted-email yields undefined.\n\n3. src/lib/db/schema.ts — add column at users (after fieldpulseUserId, ~line 278): `housecallProUserId: text(\"housecall_pro_user_id\"),` and a partial per-org unique index mirroring users_org_fieldpulse_user_id_unique (~line 322): `uniqueIndex(\"users_org_hcp_user_id_unique\").on(table.organizationId, table.housecallProUserId).where(sql\\`${table.housecallProUserId} IS NOT NULL\\`)`. HCP ids are opaque strings — per-org unique partial index (NOT global) is correct, same rationale as FP.\n\n4. drizzle/0022_*.sql — HAND-AUTHORED migration (additive): `ALTER TABLE \"users\" ADD COLUMN \"housecall_pro_user_id\" text;` + `CREATE UNIQUE INDEX IF NOT EXISTS \"users_org_hcp_user_id_unique\" ON \"users\" (\"organization_id\",\"housecall_pro_user_id\") WHERE \"housecall_pro_user_id\" IS NOT NULL;`. Append entry to drizzle/meta/_journal.json and copy 0021_snapshot.json -> 0022_snapshot.json with the new column+index added (project uses hand-authored trigger/snapshot pattern per MEMORY hand-authored-trigger-migration). REMINDER per MEMORY migrations-not-run-on-deploy: run `npm run db:migrate` after — Vercel build skips migrations and a missing column 500s .returning() writes.\n\n5. src/lib/integrations/housecall-pro/technician-sync.ts (NEW) — mirror FP exactly: export `syncTechniciansFromHousecall(organizationId, fetchImpl = fetch): Promise<{readonly synced: number}>`. Steps: getHousecallClient (client.ts:582) -> null => {synced:0}; try { const techs = await client.listTechnicians(); for each: normalize email, skip if no email/name (logger.warn); db.insert(users).values({organizationId, email, name, role:'technician', isActive: tech.isActive !== false, passwordHash:null, housecallProUserId: tech.id, createdAt, updatedAt}).onConflictDoUpdate({target:[users.organizationId, users.email], set:{name,isActive,housecallProUserId,updatedAt}, setWhere: eq(users.role,'technician')}).returning({id}); count. Then GUARDED soft-deactivate: only if currentHcpIds.length>0, update users set isActive:false where org + role technician + isNotNull(housecallProUserId) + notInArray(housecallProUserId, currentHcpIds). } catch => logger.warn + {synced:0} (degrade-safe). NO db.transaction (neon-http forbids — loop of awaits is fine, matches FP). NOTE: HCP has no role field, so unlike FP there is no isFieldpulseTechnician filter — every /employees row is a tech candidate; optionally filter on isActive only. Document this divergence in the module header.\n\n6. src/lib/integrations/housecall-pro/technician-sync.test.ts (NEW) — port fieldpulse/technician-sync.test.ts cases: (a) not-connected => {synced:0}, no db calls; (b) empty roster => NO mass-deactivate (set not called); (c) upserts each tech then one guarded deactivation, synced count correct, tech without email skipped; (d) listTechnicians throws => {synced:0} degrade. Mock db.insert/update chain and getHousecallClient identically to the FP test (wireInsert/wireUpdate helpers).\n\n7. (Optional, plan scope says admin-triggered) Wire an admin endpoint mirroring FP. NOTE: the FP sync is itself NOT yet wired to a route (grep syncTechniciansFromFieldpulse in src/app => NONE — only module+test). For strict parity, either skip the route (matching current FP reality) or add /api/admin/integrations/housecall/sync-technicians POST calling it via after() for background. Recommend: defer the route to match FP's current state and keep Stage 6 to the module+test+schema; call this out so reviewers don't flag a missing endpoint as incomplete.\n\nVERIFY GATES: `npm run db:migrate` (apply 0022 to dev DB), `npx tsc --noEmit`, `npm run lint`, `npm run test:unit -- housecall-pro/technician-sync` (and client.test for email parse). No prompt changes => no eval needed. No frozen-safety-text / money-safety / metadata.verify surfaces touched.\n\nDEGRADE-SAFETY: all HCP/network errors swallowed -> {synced:0} (mirrors FP). TENANT SCOPING: every query filtered by organizationId; upsert keyed (org,email); identity column per-org-unique partial index. NEON-HTTP: no interactive transactions; sequential awaits in the loop (same as FP, acceptable).

**Key risks:**
- HousecallTechnician lacks email (types.ts:197) and toTechnician (client.ts:258) doesn't parse it — must extend both, else the FP-style email-keyed upsert is impossible. Confirm HCP /employees actually returns an `email` field before keying on it (HCP employee API does, but verify against client.test fixtures).
- New users.housecall_pro_user_id column needs a hand-authored migration + journal entry + snapshot copy; per MEMORY, Vercel build skips migrations so the column must be applied via npm run db:migrate or .returning() writes 500.
- HCP /employees has no role field (unlike FP), so the isFieldpulseTechnician-style filter has no analog — every employee is treated as a technician; if HCP returns office/admin staff in /employees they'd be mis-upserted as role='technician'. Filter on isActive and document the divergence.
- FP sync itself is not wired to any admin route (grep => none); adding an HCP route would over-deliver vs the FP template — keep parity by deferring the endpoint and noting it, or add both. Avoid scope creep.
- Soft-deactivate guard must replicate FP exactly (only when currentHcpIds.length>0) or a transient empty/failed roster mass-deactivates the org's techs.

**Independent verifier — sound:**
- Minor citation imprecision: the assessment text says 'client.ts:279 toTechnician returns {id,name,isActive}' and 'client.ts:514 listTechnicians()' which are exactly correct, but in one place it writes 'toTechnician at client.ts:258 just doesnt capture it' AND elsewhere references 'toTechnician (client.ts:258)' as the modify point. Line 258 is the function declaration; the actual return to edit is line 279. Both lines are real and the intent is unambiguous, so this is not a substantive error.
- The HousecallTechnician PII comment cited as 'types.ts:197-204' is accurate, but note the comment block actually spans 188-204 (the interface body is 197-204). Trivial.
- Test-port nuance not wrong but worth flagging for the implementer: the FP 'empty roster' test (technician-sync.test.ts:52-68) achieves zero technicians by returning a NON-technician row that the isFieldpulseTechnician filter drops. HCP has no role filter (plan correctly notes every /employees row is a tech candidate), so the HCP case (b) mock MUST return an actually-empty listTechnicians() array, not a non-tech row, or the guarded-deactivate assertion will not be exercised as intended. The plan's prose covers this but a mechanical port could miss it.

_Verifier notes:_ Independently verified every cited reference against the code. Status 'real-gap' is correct: no technician-sync.ts in src/lib/integrations/housecall-pro/ (only technician-mapping.ts, which maps to opaque synthetic ids per its module header, NOT a users-table roster upsert); grep for syncTechniciansFromHousecall returns nothing; no housecallProUserId/hcp_user_id column or index in schema.ts (only fieldpulseUserId at :278 and users_org_fieldpulse_user_id_unique at :322-324). FP technician-sync.ts confirmed as the mirror template: upserts into users keyed on (organizationId, email) with setWhere eq(users.role,'technician') (:98-110), skips techs without email/name (:67-77), guarded soft-deactivate only when currentFpIds.length>0 (:124). HousecallTechnician (types.ts:197-204) is {id; name?; isActive?} with NO email/role — confirmed. toTechnician (client.ts:258-280) returns {id,name,isActive}, does not capture email — confirmed. listTechnicians (client.ts:514) hits /employees — confirmed. The plan's claim that HCP payloads carry email is corroborated: client.ts:158 already parses str(obj.email) for the customer path, so adding it to toTechnician is feasible. getActiveTechnicianIds (scheduling-source.ts:204) uses opaque mapped ids — confirms no true roster. Journal last entry 0021_tearful_echo, latest snapshot 0021, next migration 0022 — confirmed; the 0034/0035 in git status are untracked FP working files, not committed (consistent with the assessment). FP sync is NOT wired to any route (grep syncTechniciansFromFieldpulse in src/app returns nothing) — confirmed, validating the recommendation to defer the HCP route for strict parity. FP test (technician-sync.test.ts) has exactly the wireInsert/wireUpdate helpers and the four cases the plan describes porting. All invariants respected: no transactions (sequential awaits, matching FP and neon-http constraint), every query org-scoped, per-org partial unique index (not global), no frozen-safety/money/metadata.verify surfaces touched, migration flagged as hand-authored + needing npm run db:migrate per MEMORY. Plan is feasible and would compile. Effort 'M' is reasonable. Corrections are all minor/non-blocking.

---

## Stage 7 — HCP durable availability sync (persist to technician_availability + cron)  _(plan)_

**Verified status:** partially-done (gap genuinely OPEN; HCP availability is cache-only, no DB persistence, no cron — and a literal technician_availability mirror is structurally blocked)  **·  Verify verdict:** minor-corrections  **·  Effort:** M

**Evidence (re-confirmed against code):**
- src/lib/integrations/housecall-pro/scheduling-source.ts:49 — DEFAULT_HCP_AVAILABILITY_TTL_MS = 30_000; HCP availability is in-memory cache-only, no DB persistence (confirms the gap)
- src/lib/integrations/housecall-pro/availability-sync.ts — DOES NOT EXIST (ls miss); no HCP availability sync module
- src/app/api/cron — only sync-fieldpulse-availability and sync-housecall-invoices exist; NO sync-housecall-availability cron
- src/lib/integrations/housecall-pro/types.ts:167 — `HousecallAvailabilitySlot { startIso; endIso }` carries NO technician/employee id (account-level windows only)
- src/lib/integrations/housecall-pro/client.ts:479-512 — listAvailability hits GET /company/schedule_availability and returns only {startIso,endIso}; no per-tech attribution
- src/lib/db/schema.ts (technicianAvailability block ~1053) — technicianId uuid NOT NULL .references(()=>users.id); every row REQUIRES a real users.id
- src/lib/db/schema.ts:278 — users has fieldpulseUserId only; NO housecallProUserId column exists
- src/lib/integrations/housecall-pro/technician-sync.ts — DOES NOT EXIST (Stage 6 not shipped); HCP has no real roster, only opaque hcp-tech-<employeeId> ids from mapHcpTechnicians
- src/lib/integrations/housecall-pro/availability-mapping.ts:37,99 — windows map to synthetic `hcp-slot-<n>` tech ids, NOT users.id values
- src/lib/db/schema.ts:1144-1176 — housecallProConnections has NO availabilitySyncStatus / lastAvailabilitySyncAt / lastSyncError columns (those live ONLY on fieldpulseConnections at 1209-1219)
- grep: NONE — no file under housecall-pro/ references technicianAvailability; no HCP availability persistence anywhere

**Plan:**

GAP IS OPEN, but a literal "mirror FP" is STRUCTURALLY BLOCKED and the plan's wording is misleading. Two hard blockers, both evidenced:

1) HCP availability windows have NO technician attribution. `HousecallAvailabilitySlot` is `{startIso,endIso}` (types.ts:167) and the client only extracts start/end (client.ts:497-510). `technician_availability.technicianId` is NOT NULL FK → users.id (schema ~1063). FP works ONLY because FP windows carry `fp_<userId>` resolvable to users.fieldpulseUserId (availability-sync.ts:117-149). HCP has nothing to resolve to.

2) No real HCP roster. Stage 6 (technician-sync.ts) does not exist; there is no `housecallProUserId` column on users (only fieldpulseUserId, schema:278). Even the in-memory roster is opaque `hcp-tech-<employeeId>` ids (technician-mapping.ts), never users.id.

Therefore you CANNOT write per-technician rows into technician_availability for HCP. The honest "cache-vs-DB consistency decision" (which the plan asks for) is: HCP account-level availability is NOT durably persistable in the per-technician shape, so don't force it. Two viable paths:

OPTION A (recommended, small, honest): Document the HCP-only limitation + cheap durability win. (a) Add the three sync-tracking columns to housecallProConnections (additive migration, mirroring fieldpulseConnections:1209-1219) ONLY IF you also build a persistence target — otherwise skip. (b) Better: leave availability cache-only but harden it: write a short ADR/comment in scheduling-source.ts explaining why HCP availability is non-durable (account-level, no tech attribution, FK requires users.id), and OPTIONALLY add a tiny per-org snapshot cache table `hcp_availability_snapshot(org, slots jsonb, fetched_at)` refreshed by a cron so a cold serverless start has a warm fallback instead of hitting HCP. This is the only DB-persistence that the HCP data shape actually supports. (c) No new technician_availability writes.

OPTION B (faithful mirror, LARGE, depends on Stage 6 + an HCP API that doesn't exist here): requires (i) Stage 6 real roster sync adding users.housecallProUserId + per-org unique index (mirror schema:322-324), (ii) an HCP endpoint that returns PER-TECHNICIAN availability (the current /company/schedule_availability does not), (iii) then a syncAvailabilityFromHousecall mirroring availability-sync.ts. (ii) is not satisfiable with the current client/API — so B is not implementable today without new HCP API surface. Do NOT attempt B.

CONCRETE PLAN (Option A, the buildable one):
Files to CREATE:
- drizzle/00XX_hcp_availability_snapshot.sql + snapshot JSON (hand-authored migration; new table hcp_availability_snapshot: id uuid pk, organization_id uuid NOT NULL refs organizations onDelete cascade, slots jsonb NOT NULL, fetched_at timestamptz NOT NULL default now, unique(organization_id)). Update drizzle journal + meta snapshot per the hand-authored-trigger-migration memory pattern.
- src/lib/integrations/housecall-pro/availability-sync.ts: `syncAvailabilityFromHousecall(orgId, fetchImpl=fetch)` — getHousecallProClient → if null return {success:false}. Fetch windows for the 14-day horizon (reuse AVAILABILITY_HORIZON_DAYS), map via mapHcpAvailability, then UPSERT one snapshot row (db.insert ... onConflictDoUpdate on organization_id; single statement, neon-http safe, no transaction needed). Degrade-safe: wrap in try/catch, logger.warn, never throw. withTenant scoping on the read of any per-org status if you add status columns; the snapshot upsert is org-scoped by value.
- src/app/api/cron/sync-housecall-availability/route.ts: mirror sync-fieldpulse-availability/route.ts EXACTLY — verifyCronAuth Bearer gate (fail closed), select connected housecallProConnections, after(() => syncAvailabilityFromHousecall(orgId)) per org (after(), never detached promise), return {initiated}.
Files to MODIFY:
- src/lib/integrations/housecall-pro/scheduling-source.ts: loadMapped() reads snapshot table as a fallback BEFORE hitting HCP only on cold cache (optional), and the cron keeps the snapshot warm. Add a header comment ADR explaining non-durability of per-tech HCP availability.
- vercel.json: add a crons entry `{ "path": "/api/cron/sync-housecall-availability", "schedule": "0 * * * *" }` mirroring line 32.
Tests (TDD, vitest):
- availability-mapping already covered; add availability-sync.test.ts: (1) connected → upsert called with mapped slots; (2) not connected → {success:false}; (3) HCP throw → degrade-safe {success:false}, no throw; (4) re-run converges (upsert, single row per org). Mock client.listAvailability + db.
- cron route test mirroring any existing cron test: unauthorized → 401; authorized → schedules after() per connected org.
Verify gates: npx tsc --noEmit; npm run lint; npm run test:unit (target the new files); npm run build. NO prompt changes → no eval needed. No frozen-safety-text / money-safety / metadata.verify surfaces touched.

DECISION TO SURFACE TO USER: The plan's "mirror FP into technician_availability" is not achievable for HCP (no tech-attributed windows, FK to users.id). Recommend reframing Stage 7 as "durable HCP availability snapshot + cron" (Option A) and noting the technician_availability mirror as HCP-blocked pending a per-tech HCP availability API + Stage 6 roster. If user insists on technician_availability writes, Stage 7 must be marked blocked-on-Stage-6 AND blocked-on-HCP-API.

**Key risks:**
- FALSE-PARITY TRAP: blindly mirroring FP would write synthetic hcp-slot-<n> ids into technician_availability.technicianId — a NOT NULL FK to users.id — which will FK-violate at runtime (or, worse, require fabricating user rows). Do not do this.
- Hard dependency: any per-technician HCP availability requires Stage 6 (real roster + users.housecallProUserId) AND an HCP API that returns per-tech windows; the current /company/schedule_availability returns account-level {start,end} only — neither exists today.
- Migration must be hand-authored (journal + meta snapshot copy) per repo convention; drizzle-kit auto-gen + an un-run migration on Vercel deploy causes schema-drift 500s (see migrations-not-run-on-deploy memory) — run npm run db:migrate after merge.
- Degrade-safety + neon-http: cron must use after() not a detached promise (Vercel freeze); snapshot write must be a single upsert (no db.transaction()).
- Scope creep risk: do NOT add availabilitySyncStatus columns to housecallProConnections unless a real persistence target is built — copying FP's three columns without a writer is dead schema.

**Independent verifier — minor-corrections:**
- PLAN NAMING ERROR: the assessment's concrete plan repeatedly references `getHousecallProClient`, but the actual org-scoped HCP client factory is `getHousecallClient` (src/lib/integrations/housecall-pro/client.ts:582). The FP analog it mirrors is `getFieldpulseClient` (used at fieldpulse/availability-sync.ts:178). Pseudocode-level nit, not a feasibility blocker.
- PLAN REUSE NIT: the plan says 'reuse AVAILABILITY_HORIZON_DAYS' but that constant is module-private in scheduling-source.ts:44 (not exported). A new availability-sync.ts cannot import it as-is; it must be exported or redefined.
- EVIDENCE LINE-NUMBER NUANCE (assessment is still CORRECT): the assessment cites housecallProConnections at 'schema.ts:1144-1176' as lacking availabilitySyncStatus/lastAvailabilitySyncAt/lastSyncError and says those live ONLY on fieldpulseConnections at 1209-1219. I confirmed this is accurate: the housecallProConnections block (1144-1176) ends at line 1170 with only basic columns; the sync-tracking columns at 1209-1219 are inside the fieldpulseConnections block which starts at 1188. A naive grep for those column names returns 1209-1219 which could mislead a reader into thinking they're on the HCP table, but the assessment got the attribution right.

_Verifier notes:_ Independently confirmed all load-bearing claims against the code. Status (partially-done / gap open) is correct, not mis-rated. Both structural blockers hold: (1) HousecallAvailabilitySlot is {startIso,endIso} only (types.ts:167-169) and client.listAvailability returns only start/end from GET /company/schedule_availability (client.ts:479-516) — no tech attribution; technician_availability.technicianId is uuid NOT NULL .references(users.id) (schema:1060-1062), so synthetic hcp-slot-<n> ids (availability-mapping.ts:37,99) would FK-violate. (2) users has only fieldpulseUserId (schema:278), no housecallProUserId; no technician-sync.ts exists; HCP ids are opaque hcp-tech-<employeeId> (technician-mapping.ts:28). FP works because fp_<userId> resolves to users.fieldpulseUserId→users.id, skipping unresolvable (availability-sync.ts:117-149,218-220) — HCP has no such anchor. Confirmed no HCP availability-sync module, no sync-housecall-availability cron (only sync-fieldpulse-availability + sync-housecall-invoices). FP cron route (sync-fieldpulse-availability/route.ts) confirms the exact mirror pattern the plan prescribes: verifyCronAuth Bearer fail-closed, after() not detached promise, {initiated} response. vercel.json line 32 is indeed the FP availability cron. Option A (per-org jsonb snapshot table + cron, single upsert, no transaction) is feasible and respects neon-http/after()/tenant-scoping invariants; Option B (faithful per-tech mirror) is correctly flagged as blocked on Stage 6 roster + a non-existent per-tech HCP availability API. The keyRisks (false-parity FK violation, hand-authored migration per repo convention, after()/no-transaction, no dead sync columns without a writer) are all accurate and grounded. The 'reframe Stage 7 as durable snapshot + cron, mark technician_availability mirror HCP-blocked' recommendation is the honest call. Effort 'M' is reasonable for Option A. Verdict is minor-corrections solely due to the getHousecallProClient/getHousecallClient naming slip and the private-constant reuse nit in the plan pseudocode — neither changes the architecture or feasibility.

---

## Stage 8 — HCP address validation on customer sync  _(plan)_

**Verified status:** real-gap  **·  Verify verdict:** minor-corrections  **·  Effort:** M

**Evidence (re-confirmed against code):**
- src/lib/integrations/housecall-pro/customer-sync.ts:83-95 toCreateInput carries free-text into `{ street: contact.address }` with NO validation/normalization (sync) — the gap
- src/lib/integrations/fieldpulse/customer-sync.ts:24-26 imports validateAddressForSync + hasMinimumAddressQuality; :90-107 applies them in toCreateInput before sync — the FP reference behavior
- src/lib/integrations/fieldpulse/address-validation.ts:122-194,224-278 the validation path (Photon-first, FP-geocode fallback, degrade to original) — provider-coupled at :91 getFieldpulseClient + :97 client.geocodeAddress
- grep over src/lib/integrations/housecall-pro/ for validateAddressForSync|hasMinimumAddressQuality|address-validation returned exit 1 (no matches) — nothing implemented
- src/lib/integrations/housecall-pro/client.ts:53-581 client interface has NO geocodeAddress method (FP has it at client.ts:139); HCP cannot mirror the FP-geocode fallback leg
- src/lib/integrations/housecall-pro/types.ts:19-24 HousecallAddress is structured (street/city/state/zip) — richer than the current single-street write at customer-sync.ts:85

**Plan:**

GAP IS OPEN. HCP customer-sync writes raw free-text into HousecallAddress.street with zero validation; FP validates first. Implement to match FP parity.

KEY DESIGN DECISION (state explicitly, do not silently pick): FP's validateAddressForSync has TWO legs — (1) provider-agnostic Photon autocomplete + quality scoring + normalize, and (2) a FP-SPECIFIC geocode fallback (address-validation.ts:91,97). The HCP client has NO geocodeAddress (client.ts has none; FP has it at client.ts:139). So full byte-for-byte parity is impossible. Two viable options:
  - Option A (recommended, lowest risk, matches plan 'apply the shared path'): Extract the provider-agnostic core (Photon + scoreAddressQuality + filterByQuality + hasMinimumAddressQuality + normalize) into a shared module, then HCP uses Photon-only validation (no geocode fallback). HCP gains street/city/state/zip normalization from Photon, degrading to original on any miss. This is parity on the part that matters (low-quality address -> flagged/normalized) without inventing an HCP geocode API that doesn't exist.
  - Option B: also add geocodeAddress to the HCP client if HCP's API supports it. Heavier; likely no HCP geocode endpoint exists. Defer unless confirmed.
Plan below assumes Option A.

FILES TO CREATE:
1. src/lib/address/validation-core.ts (NEW, shared, provider-agnostic) — move/extract from fieldpulse/address-validation.ts: scoreAddressQuality, filterByQuality, hasMinimumAddressQuality, normalizeAddress (rename normalizeAddressForFieldpulse -> normalizeAddressLine), and the Photon-only fetchValidatedAddressSuggestions WITHOUT the FP geocode branch. Export a generic ValidatedAddress type { street, city, state, zip }. Keep all functions pure + degrade-safe (empty array / original on error), identical thresholds (MIN_ZIP_LENGTH=5, MIN_STREET_LENGTH=3, DEFAULT_QUALITY_THRESHOLD=0.6) so HCP flags low-quality addresses IDENTICALLY to FP (the verify requirement).
   - To avoid churn/regressions in FP: keep fieldpulse/address-validation.ts as-is but have it re-export/delegate the shared pure helpers, retaining its FP-geocode fallback leg. Do NOT rewrite the FP path — surgical.
2. src/lib/integrations/housecall-pro/address-validation.ts (NEW) — thin HCP wrapper: validateHcpAddress(address) parses the free-text street via Photon (fetchValidatedAddressSuggestions from validation-core), returns a normalized HousecallAddress {street,city,state,zip} or the original on miss. Degrade-safe (try/catch -> original). Tenant-agnostic (Photon is global; no org client needed in Option A).

FILES TO MODIFY:
3. src/lib/integrations/housecall-pro/customer-sync.ts — make toCreateInput async (mirror FP customer-sync.ts:84-116): if contact.address && hasMinimumAddressQuality({street}) -> await validateHcpAddress; map result to HousecallAddress (now populates city/state/zip, not just street); else fall back to { street: contact.address }; on throw -> { street: contact.address }. Update the single call site (line 211 `toCreateInput(contact)`) to `await toCreateInput(contact)`. Keep it inside the existing try/catch so failure stays a WARN-and-swallow (lines 231-237). Background-task safety unchanged (caller uses after()).

TESTS (TDD, vitest):
4. src/lib/address/validation-core.test.ts — port the existing fieldpulse address-validation unit tests for the pure helpers (scoreAddressQuality boundaries, hasMinimumAddressQuality true/false, normalize). 
5. src/lib/integrations/housecall-pro/address-validation.test.ts — mock fetchAddressSuggestions: (a) high-quality Photon hit -> normalized street/city/state/zip; (b) low-quality/no Photon hit -> returns original street unchanged (flagged, not enriched) — assert IDENTICAL flag/normalize outcome to the FP test for the same input (the plan's explicit verify: 'low-quality address -> flagged/normalized identically to FP'); (c) Photon throws -> degrades to original, never throws.
6. Extend src/lib/integrations/housecall-pro/customer-sync.test.ts — add a case: address present + Photon enriches -> createCustomer called with full HousecallAddress {street,city,state,zip}; and Photon-miss -> createCustomer called with { street } only. Reuse the existing fakeClient()/encrypt() harness (test already encrypts addressEncrypted at lines 174, 142).

DEGRADE-SAFETY: every new path returns the original address (never null-out a real street, never throw) so a Photon outage can't break HCP sync — matches FP address-validation.ts:251-258,269-277 and the customer-sync golden rule (file header lines 5-9).
TENANT SCOPING: Option A needs no org-scoped client; if Option B chosen, getHousecallClient(organizationId) must be threaded (already passed into syncCustomerToHcp). DB read already tenant-scoped via withTenant at customer-sync.ts:184. No new DB writes -> neon-http transaction concern N/A.

VERIFY GATES (run all): `npx tsc --noEmit`; `npm run lint`; `npm run test:unit` (the 3 new/extended suites green); `npm run build`. No prompts touched -> no eval needed. Note: per MEMORY 'vitest-env known failures', ignore the ~7 pre-existing DB/server import-time failures (baseline, not regressions) — confirm the new suites specifically pass.

**Key risks:**
- FP geocode fallback is not portable to HCP (no HCP geocodeAddress in client.ts) — 'identical to FP' can only mean identical on the Photon/quality-scoring leg, not the geocode leg. Must scope verify to the shared pure path or the test will be unsatisfiable.
- Refactoring fieldpulse/address-validation.ts to share helpers risks regressing Stage-equivalent FP behavior — keep FP file delegating, do not rewrite its geocode leg; its existing tests must stay green.
- toCreateInput becomes async — must await at the call site (customer-sync.ts:211) or createCustomer receives a Promise and silently mis-syncs; easy to miss, covered by the new test asserting createCustomer args.
- Populating city/state/zip from Photon changes the HCP payload shape vs today's street-only — verify HCP createCustomer accepts the fuller address (types.ts:19-24 supports it) and that a wrong Photon parse doesn't overwrite a correct street; degrade-to-original guards this.
- Photon is an external network call now on the sync path — it runs in after() background (not response path) so latency is acceptable, but must stay degrade-safe on timeout.

**Independent verifier — minor-corrections:**
- The assessment's verify requirement ('low-quality address -> flagged/normalized IDENTICALLY to FP') is built on a partly-dead FP path. FP customer-sync.ts:92 calls hasMinimumAddressQuality({ street: contact.address }) with ONLY a street and no city/state/zip. hasMinimumAddressQuality (address-validation.ts:286-299) requires hasStreet && (hasCity || hasState || hasZip); with city/state/zip undefined it returns FALSE. So for a single free-text address (which is exactly what the customer-sync path passes, since our customers.addressEncrypted is one free-text field), FP ALWAYS takes the else-if branch at lines 104-106 and writes { street } as-is. validateAddressForSync / Photon / geocode are NEVER reached from the FP customer-sync path for free-text addresses. The assessment treats FP as actively validating; in practice FP's validation is dead code on this path. 'Parity with FP' for free-text input therefore means write { street } unchanged — the planned Photon enrichment would actually exceed FP behavior, not merely match it. The KEY DESIGN DECISION / geocode-portability analysis is technically accurate but somewhat moot for this call path.
- Minor wording: assessment evidence says FP customer-sync.ts:90-107 'applies them in toCreateInput before sync' — accurate as code structure, but as noted the validation leg is unreachable for the single-street input the path supplies. The import (24-26) and structure exist; the runtime effect for free-text is no-op.

_Verifier notes:_ Independently confirmed all six evidence citations are real and accurate: HCP customer-sync.ts:83-95 toCreateInput writes { street: contact.address } with no validation (verified lines 83-95); FP customer-sync.ts:24-26 imports validateAddressForSync+hasMinimumAddressQuality and :90-107 wires them (verified); FP address-validation.ts has the two-leg path with getFieldpulseClient at :91 and client.geocodeAddress at :97 (verified); grep over housecall-pro/ for validateAddressForSync|hasMinimumAddressQuality|address-validation returns exit 1 / no matches, no address-validation.ts in the HCP dir (verified by ls); HCP client.ts has no geocodeAddress (grep confirms only FP client.ts:139 declares + :735 implements it); HousecallAddress in types.ts:19-26 is structured street/street_line_2/city/state/zip/country (verified, richer than the single-street write). Status real-gap is CORRECT — HCP truly has zero address-validation scaffolding.

Plan feasibility: Option A is sound and compiles against the real code. Dependencies all exist: src/lib/address/photon.ts exports fetchAddressSuggestions (:63); HCP customer-sync.test.ts has the fakeClient()/encrypt() harness with addressEncrypted cited (verified lines 66-82, 139-234); call site to make async is the inline (await client.createCustomer(toCreateInput(contact))).id at line 211 (verified). No frozen-safety-text, no money-safety, no neon-http transaction, no tenant-scoping violations — DB read already withTenant'd (:184), no new DB writes. The async-call-site risk and degrade-safety risks they list are real and correct.

Net: the gap is genuinely open and the plan is implementable. The one substantive caveat is that 'identical to FP' is a weaker/odd target than stated because FP's own validation never fires on the free-text customer-sync path; implementing real Photon enrichment for HCP would diverge from (improve on) actual FP runtime behavior. Worth surfacing to whoever defines the parity bar, but does not block the plan.

---

## Stage 9 — HCP client rate limiter (mirror FieldPulse rate-limiter)  _(plan)_

**Verified status:** real-gap  **·  Verify verdict:** sound  **·  Effort:** M

**Evidence (re-confirmed against code):**
- src/lib/integrations/housecall-pro/client.ts:33-44 - HCP has only per-request retry: MAX_ATTEMPTS=3, BACKOFF_BASE_MS, isRetryableStatus(429/5xx)
- src/lib/integrations/housecall-pro/client.ts:299-332 - request() loops attempts with sleep(BACKOFF_BASE_MS*2**attempt); no token bucket, no adaptive throttle, no cross-call rate tracking
- grep 'rate-limiter|RateLimiter|waitForRateLimit|reportThrottle|checkLimit' over src/lib/integrations/housecall-pro/ returns ZERO matches - HCP has no limiter at all
- src/lib/integrations/fieldpulse/rate-limiter.ts:57-220 - the FP token-bucket RateLimiter + fieldpulseRateLimiter singleton + waitForRateLimit() to mirror
- src/lib/integrations/fieldpulse/bulk-operations.ts:26,59,83,114,269 - the ONLY consumer of the FP limiter: waitForRateLimit(clientId) before each item, reportSuccess on ok, reportThrottle on 429; clientId = orgId (line 262 doc)
- src/lib/integrations/fieldpulse/client.ts:36-49 - FP client itself is NOT wrapped by the limiter (same per-request retry as HCP); 'wrap the client' is looser than the actual FP precedent
- ls src/lib/integrations/housecall-pro | grep bulk -> NO HCP bulk-operations (Stage 10 not yet done), so there is no natural batch consumer to wrap today
- src/lib/integrations/fieldpulse/bulk-types.ts:121-132 - RateLimitInfo shape (allowed/state/remaining/resetMs/suggestedDelayMs) the limiter returns
- src/lib/integrations/ has only fieldpulse/ google-calendar/ housecall-pro/ - no shared/common dir exists yet for an extracted limiter

**Plan:**

GAP IS OPEN. Recommended approach: extract the limiter to a shared module and add an HCP-bound instance + tests. Faithful to the FP precedent (limiter is standalone, consumed by bulk-ops keyed on orgId), NOT a wrap of the per-call client.request() (which already retries 429/5xx and would conflict with bucket logic).\n\nFILES:\n1. CREATE src/lib/integrations/shared/rate-limiter.ts - MOVE the class verbatim from fieldpulse/rate-limiter.ts (RateLimiter, RateLimiterOptions, waitForRateLimit, chunk, sleep). It is already integration-agnostic. Define a local RateLimitInfo interface here (copy from bulk-types.ts:121-132) to avoid a shared->fieldpulse import cycle. Export both fieldpulseRateLimiter and a new housecallRateLimiter = new RateLimiter() singleton (separate buckets per API).\n2. MODIFY src/lib/integrations/fieldpulse/rate-limiter.ts - re-export from shared (export { RateLimiter, waitForRateLimit, chunk, fieldpulseRateLimiter } from '../shared/rate-limiter') so existing FP imports (bulk-operations.ts:26, bulk-operations.test.ts, rate-limiter.test.ts) keep working with zero churn. Keep RateLimitInfo in bulk-types.ts as the canonical FP type; shared type is structurally identical.\n3. CREATE src/lib/integrations/housecall-pro/rate-limiter.ts - thin: export { housecallRateLimiter, waitForRateLimit } from shared, OR a tiny helper withHcpRateLimit(orgId, fn) that: waitForRateLimit(orgId, housecallRateLimiter); try { const r = await fn(); housecallRateLimiter.reportSuccess(orgId); return r } catch(e) { if String(e).includes('429') housecallRateLimiter.reportThrottle(orgId); throw }. This is the consumer-side glue mirroring bulk-operations.ts:59-114. No call site wiring needed in this stage if Stage 10 (HCP bulk-ops) is the intended consumer - but to satisfy 'wrap HCP client calls' minimally, the helper is the deliverable and Stage 10 calls it. If a consumer is required NOW, wrap the per-item loop inside customer-history.ts/invoice-sync.ts batch reads with withHcpRateLimit.\n\nTESTS (TDD, vitest):\n- CREATE src/lib/integrations/shared/rate-limiter.test.ts - move + de-duplicate the existing fieldpulse/rate-limiter.test.ts cases (burst up to capacity allowed, blocked past capacity, refill over time with vi.useFakeTimers, independent clients, reportThrottle reduces modifier+zeros tokens, reportSuccess recovers). Add the Stage-9 verify cases explicitly: (a) BURST: N>burstCapacity checkLimit calls -> last returns state 'blocked'; (b) 429 BACKOFF: reportThrottle -> getAdaptiveRate halved, next waitForRateLimit returns a positive delay (assert via fake timers, not real sleep).\n- CREATE src/lib/integrations/housecall-pro/rate-limiter.test.ts - withHcpRateLimit: success path calls reportSuccess; a thrown '...429...' calls reportThrottle and rethrows; clientId isolation between two orgIds.\n- Keep fieldpulse/rate-limiter.test.ts importing from the FP re-export so the FP suite still passes unchanged (proves the extract is behavior-preserving).\n\nVERIFY GATES: npx tsc --noEmit; npm run lint (target 0 per memory); npm run test:unit (new shared+hcp suites green, FP suite still green); npm run build. No prompt changes -> NO eval needed. No DB/migration -> neon-http/db.batch/withTenant N/A (limiter is in-memory per warm instance, keyed by orgId string = the tenant scope; matches FP). Degrade-safety: limiter is pure in-memory, cannot throw on the happy path (constructor guards sustainedRps>0/burstCapacity>0 already at rate-limiter.ts:67-72); a cold lambda just starts with full buckets, same as FP. Memory: waitForRateLimit calls cleanup() each hot-path call (rate-limiter.ts:233) so per-org buckets evict - inherited free by the extract.\n\nSCOPE NOTE / push-back: the plan's verb 'wrap the HCP client calls' overstates the FP precedent - FP never wrapped its client, only bulk-ops. Wrapping client.request() directly would double-handle 429 against the existing retry loop (client.ts:325-328). Recommend the limiter+helper deliverable and let Stage 10 (HCP bulk-ops) be the real consumer; flag if reviewer wants a forced call-site wire now.

**Key risks:**
- RateLimitInfo currently lives in fieldpulse/bulk-types.ts (bulk-types.ts:121); moving the limiter to shared/ must not create a shared->fieldpulse import edge - duplicate the small interface in shared/ to keep the dependency arrow one-directional.
- No HCP consumer exists yet (no HCP bulk-operations - that's Stage 10). Stage 9 in isolation ships the limiter+helper but may have zero live call sites, making 'wrap the HCP client calls' only half-satisfiable until Stage 10. Confirm whether reviewer accepts limiter-ready-for-Stage-10 vs. demands a forced wire into an existing batch read (customer-history/invoice-sync).
- Mirroring too literally (wrapping client.request) would conflict with the existing per-request 429/5xx retry loop (client.ts:325-328), causing double backoff. Limiter must sit at the batch/consumer layer like FP, not inside request().
- FP test suite (fieldpulse/rate-limiter.test.ts) and bulk-operations.ts import from ./rate-limiter; the extract must leave a re-export shim or these break - this is the main regression surface.

**Independent verifier — sound:**
- (no corrections — independently confirmed)

_Verifier notes:_ Independently verified every cited file:line; all accurate and the status (real-gap, GAP OPEN) is correct.

CONFIRMED CLAIMS:
- HCP client (client.ts:33-44) has ONLY per-request retry: MAX_ATTEMPTS=3, BACKOFF_BASE_MS=100, isRetryableStatus(429/5xx). request() loop with sleep(BACKOFF_BASE_MS*2**attempt) at lines 299-332. Verified.
- grep for rate-limiter/RateLimiter/waitForRateLimit/reportThrottle/checkLimit/reportSuccess over housecall-pro/ returns ZERO matches. Verified — HCP has no limiter.
- FP rate-limiter.ts:57-220 has the RateLimiter class + fieldpulseRateLimiter singleton (line 220) + waitForRateLimit (line 226). Constructor guards sustainedRps>0/burstCapacity>0 at lines 67-72. cleanup() called on hot path at line 233. All verified.
- bulk-operations.ts is the ONLY FP limiter consumer: import at line 26, waitForRateLimit at line 59, reportSuccess at 83, reportThrottle at 114. clientId=orgId doc at line 262 ("e.g., organization ID"). Verified.
- FP client.ts:36-49 is NOT wrapped by the limiter — same per-request retry as HCP, no limiter import (grep confirmed NO LIMITER). The "wrap the client" pushback is correct: wrapping client.request() would double-handle 429 against the existing retry loop (client.ts:325-328). Verified.
- No HCP bulk-operations file (Stage 10 not done) — confirmed via ls (no bulk file). RateLimitInfo shape at bulk-types.ts:121-132 (allowed/state/remaining/resetMs/suggestedDelayMs). Verified.
- No shared/ dir exists (only fieldpulse/ google-calendar/ housecall-pro/). Verified.

PLAN FEASIBILITY: All steps feasible. The import-cycle risk (RateLimitInfo in bulk-types.ts; duplicate small interface in shared/ to keep arrow one-directional) is real and the mitigation is correct. The re-export shim concern is real: rate-limiter.test.ts and bulk-operations.test.ts both import from "./rate-limiter" (lines 11/14), so the extract MUST leave a re-export to avoid breaking the FP suite. The "if a consumer is required NOW" fallback is grounded — customer-history.ts has a per-item loop (line 33) and invoice-sync.ts has getInvoice/listJobInvoices reads (lines 228/250-251) that could be wrapped.

No corrections. Effort M is reasonable (mechanical extract + shim + 2 new test files). The degrade-safety/neon-http/tenant-scoping notes are all accurate (limiter is pure in-memory, keyed by orgId string, no DB).

---

## Stage 10 — HCP bulk operations + admin endpoint  _(plan)_

**Verified status:** real-gap  **·  Verify verdict:** sound  **·  Effort:** M

**Evidence (re-confirmed against code):**
- src/lib/integrations/housecall-pro/ dir listing: no bulk-operations.ts, no bulk-types.ts (only client/customer-sync/job-sync/invoice-sync/webhook-* etc.)
- src/app/api/admin/integrations/housecall/ has only connect/, disconnect/, status/ routes — no bulk-update/
- grep -rln bulk over housecall-pro/ and admin/integrations/housecall/ returns ZERO matches
- FP reference exists: src/lib/integrations/fieldpulse/bulk-operations.ts:265 bulkUpdateJobStatus, fieldpulse/bulk-types.ts:14 BulkJobStatusUpdate, app/api/admin/integrations/fieldpulse/bulk-update/route.ts:29 POST
- CONSTRAINT: FP fieldpulse/types.ts:88 UpdateJobInput has workStatus; FP client.ts:545 sends work_status. HCP types.ts:148-160 UpdateJobInput has NO workStatus field — only description/scheduleStart/scheduleEnd/lineItems
- HCP client.ts:399-413 updateJob body = {description, schedule, line_items} — no work_status. The ONLY HCP status mutation is cancelJob (client.ts:416-423, PUT /jobs/{id}/cancel)
- HCP has addJobNote (client.ts:425-435, POST /jobs/{id}/notes) — note path IS portable
- No HCP rate limiter: grep rate-limiter over housecall-pro/ returns nothing; Stage 9 (HCP limiter) is NOT done. FP limiter fieldpulse/rate-limiter.ts:16 imports RateLimitInfo from ./bulk-types (FP-coupled)

**Plan:**

Gap is real and fully open. But a naive port is BLOCKED by the HCP API surface: FP's bulkUpdateJobStatus works because FP updateJob accepts work_status; HCP updateJob has no work_status setter (types.ts:148, client.ts:399). HCP only exposes cancelJob for status transitions. Two honest options — recommend (B):\n\nOPTION A (full status bulk): requires first adding work_status to HCP client.updateJob + UpdateJobInput, which is an unverified HCP API assumption (the existing code deliberately omits it; webhook-events.ts:108 shows HCP communicates status via events, not a documented PUT field). Risk: shipping a bulk endpoint that 400s/no-ops against real HCP. Do NOT do this without API confirmation.\n\nOPTION B (RECOMMENDED — bulk note + bulk cancel, the operations HCP demonstrably supports, plus an explicit documented limitation for arbitrary status). This satisfies the plan's escape hatch: 'if the HCP API supports it; else document HCP-only limitation explicitly.'\n\nCONCRETE PLAN (Option B):\n\n1. CREATE src/lib/integrations/housecall-pro/bulk-types.ts — port fieldpulse/bulk-types.ts, renaming fieldpulseJobId->hcpJobId, FieldpulseJob->HousecallJob. Define BulkJobOperation = {hcpJobId, serviceRequestId, action: 'note'|'cancel', note?}. Drop the workStatus field (unsupported). Keep BulkJobUpdateResult/BulkOperationSummary/BulkOperationError/BulkOperationOptions/BulkJobStatusUpdateRequest/Response shapes for response parity.\n\n2. CREATE src/lib/integrations/housecall-pro/bulk-operations.ts — port the bounded-worker-pool from fieldpulse/bulk-operations.ts:145-207 verbatim (it is generic; preserves order, no orphaned promises, continueOnError abort). Replace processSingleUpdate body: for action==='cancel' call client.cancelJob(id); for action==='note' call client.addJobNote(id, note). Keep timeout via Promise.race (client.ts:399 path), retry classification (429/timeout/5xx/ECONN*), aggregateErrors, validateBulkUpdates (validate action enum + note required when action==='note', cap 1000). Export bulkJobOperations(client, ops, options, clientId).\n   - RATE LIMITER: since Stage 9 (HCP limiter) isn't built, either (a) extract a shared generic limiter to src/lib/integrations/shared/rate-limiter.ts and have BOTH FP and HCP use it (cleaner, but touches FP — note FP limiter currently imports RateLimitInfo from ./bulk-types, so move that type too), or (b) the simplest degrade-safe path: rely ONLY on options.batchDelayMs + maxConcurrency pacing (no token bucket) and note it. RECOMMEND (b) for Stage 10 to stay surgical; leave token-bucket to Stage 9. Document the choice in the file header.\n\n3. CREATE src/app/api/admin/integrations/housecall/bulk-update/route.ts — port fieldpulse/bulk-update/route.ts:29-153. getAdminSession() gate (return 401 if null) — TENANT SCOPE via session.organizationId, clientId=`org:${session.organizationId}`. slidingWindow(`admin:housecall-bulk:${session.userId}`, RATE_LIMITS.adminMutation...). Body shape-guard (typeof object, non-null, Array.isArray(ops)) -> 400 before destructure (matches route.ts:48-58). getHousecallClient(session.organizationId) -> 400 NOT_CONFIGURED if null (client.ts:582). successResponse({summary, aggregatedErrors, completeSuccess}). SyntaxError->400. GET returns a static {supportsStatusBulk:false} note + (if limiter omitted) drop the rate-limit GET or return batchDelay defaults. NOTE: this route has NO dynamic params, so the Next 16 Promise-params caveat doesn't apply here; use after() only if any background fan-out is added (none needed — bulk runs inline within the request).\n\n4. TESTS (TDD, vitest, mirror fieldpulse/bulk-operations.test.ts):\n   - bulk-operations.test.ts: partial failure reported per-item (PLAN VERIFY GATE); order preserved; continueOnError=false aborts remaining; retry on transient then success; note-failure non-fatal vs cancel-failure fatal; validateBulkUpdates rejects empty/>1000/bad-action/note-missing-when-action-note.\n   - bulk-types.test.ts: type-shape smoke if FP has one (it does: bulk-types.test.ts exists for FP).\n   - tests/api/housecall-bulk-update.test.ts: 401 no session; 400 bad body; 400 NOT_CONFIGURED; happy path summary; tenant scope (clientId derives from session.organizationId, not body).\n\nDEGRADE-SAFETY: continueOnError default true (partial success); note failures non-fatal; no detached promises (inline await within request). NEON-HTTP: this stage touches NO DB (pure HCP API calls + session read) — no db.batch/transaction concern. FROZEN safety text / money-safety / metadata.verify: N/A — no chat prompts, no money writes, no chat metadata touched.\n\nVERIFY GATES: npx tsc --noEmit; npm run lint; npm run test:unit (new suites green); npm run build. No eval (no prompt changes).

**Key risks:**
- HCP updateJob has NO work_status field (types.ts:148, client.ts:399) — a literal FP port that calls updateJob({workStatus}) will not change status on HCP. Must scope bulk to cancel+note (Option B) or first prove HCP PUT /jobs accepts work_status before Option A.
- addJobNote endpoint shape is an ASSUMPTION (client.ts:426 comment 'ASSUMED HCP SHAPE') — bulk-note may fail at runtime against real HCP if the path/body is wrong; the per-item error aggregation will surface it, but plan should flag it as unverified.
- Stage 9 (HCP rate limiter) is not implemented; reusing FP's limiter pulls in FP-coupled types (rate-limiter.ts:16 imports from ./bulk-types). Either extract a shared limiter (scope creep into FP) or ship Stage 10 with only batchDelay/concurrency pacing and defer token-bucket to Stage 9.
- Plan's own escape hatch ('else document HCP-only limitation explicitly') means a defensible deliverable is bulk note/cancel + a documented no-arbitrary-status note — do not over-build an unverified status path to chase false parity.

**Independent verifier — sound:**
- Minor (non-substantive): the plan cites the FP bounded-worker-pool as 'fieldpulse/bulk-operations.ts:145-207'. The pool/comment block actually begins at line 151 (worker-pool comment) and the loop logic spans ~151-208. Off by ~6 lines; does not affect feasibility.
- Minor: plan says 'port the bounded-worker-pool verbatim (it is generic)'. It is NOT fully generic as-written — FP's processSingleUpdate (bulk-operations.ts:59-115) contains FP-rate-limiter calls (waitForRateLimit, fieldpulseRateLimiter.reportSuccess/reportThrottle). The plan's Option-B(b) 'no token bucket' path requires STRIPPING these calls when porting, not just replacing the updateJob line. The plan implies this ('Replace processSingleUpdate body') but does not call out the limiter-call removal explicitly; an implementer following 'port verbatim' literally would pull in undefined symbols. Worth flagging so the port compiles.

_Verifier notes:_ Independently verified against the actual code. Every load-bearing claim holds:

STATUS = real-gap (CONFIRMED). ls of src/lib/integrations/housecall-pro/ shows no bulk-operations.ts / bulk-types.ts; src/app/api/admin/integrations/housecall/ has only connect/disconnect/status (no bulk-update/); grep 'bulk' and 'rate-limiter' over HCP return zero. Gap is fully open.

CORE CONSTRAINT (the assessment's thesis) = CORRECT and well-grounded:
- HCP UpdateJobInput (types.ts:148-160) has only description/scheduleStart/scheduleEnd/lineItems — NO workStatus. Confirmed.
- HCP updateJob (client.ts:392-413) PUT body = {description, schedule, line_items} — no work_status. Confirmed.
- cancelJob = PUT /jobs/{id}/cancel (client.ts:416-423). addJobNote = POST /jobs/{id}/notes with body {content} and an 'ASSUMED HCP SHAPE' comment at client.ts:426. Confirmed (risk #2 accurate).
- The kill-shot: FP processSingleUpdate (bulk-operations.ts:75-77) literally calls client.updateJob(jobId, {workStatus}). A verbatim port to HCP would silently no-op on status. The assessment correctly identifies this and recommends Option B (bulk cancel+note), matching the plan's documented escape hatch.

FP REFERENCE = ACCURATE: bulkUpdateJobStatus (bulk-operations.ts:265), validateBulkUpdates (344), Promise.race timeout (75), continueOnError abort (186). bulk-types.ts has all cited shapes incl. RateLimitInfo (121). FP rate-limiter.ts:16 imports RateLimitInfo from ./bulk-types — FP-coupling claim CONFIRMED (risk #3 valid). FP bulk-update/route.ts uses getAdminSession, slidingWindow(`admin:fieldpulse-bulk:${session.userId}`, RATE_LIMITS.adminMutation...), getFieldpulseClient, NOT_CONFIGURED, Array.isArray body guard, clientId=`org:${organizationId}`, SyntaxError->400. All confirmed.

PLAN FEASIBILITY = SOUND: getHousecallClient(organizationId) -> Promise<HousecallProClient|null> at client.ts:582 (matches NOT_CONFIGURED branch). AdminSessionPayload (auth/types.ts:10-11) has userId + organizationId (plan's session.* refs valid). RATE_LIMITS.adminMutation exists (rate-limit.ts:86). Rename targets fieldpulseJobId->hcpJobId, FieldpulseJob->HousecallJob, drop workStatus all map to real FP field names. No DB writes in this stage (pure HCP API + session read) so neon-http no-transaction concern is correctly N/A. Tenant scoping via session.organizationId is correct. No frozen-safety-text / money / chat-metadata surfaces touched.

Stage 9 (HCP rate limiter) not-done claim = CONFIRMED (no rate/limit file in HCP dir).

Only nits are two cosmetic line-range/wording issues that do not change the verdict or feasibility.

---

## Stage 11 — FieldPulse job line-items on push  _(plan)_

**Verified status:** real-gap  **·  Verify verdict:** sound  **·  Effort:** M

**Evidence (re-confirmed against code):**
- src/lib/integrations/fieldpulse/job-sync.ts:195-201 createJob is called with only {customerId, description, scheduleStart, scheduleEnd, requestId} — no line items
- src/lib/integrations/fieldpulse/job-sync.ts:155-159 the update path sends only {description, scheduleStart, scheduleEnd} — no line items
- src/lib/integrations/fieldpulse/types.ts:73-89 CreateJobInput/UpdateJobInput have no lineItems field (only customerId/description/schedule/assignedUserId/requestId/workStatus)
- src/lib/integrations/fieldpulse/client.ts:520-528 createJob body has no line_items key; updateJob:540-546 likewise
- src/lib/integrations/fieldpulse/job-mapping.ts:44-50 MappedJobFields exposes only {description, scheduleStart?, scheduleEnd?} — no lineItems, no import of any line-items builder
- ls src/lib/integrations/fieldpulse/ shows NO line-items.ts (HCP has src/lib/integrations/housecall-pro/line-items.ts)
- HCP reference impl: housecall-pro/job-mapping.ts:22 imports buildLineItemsFromRequest, :62 MappedJobFields.lineItems, :99-114 derives+embeds; job-sync.ts:151,194 passes fields.lineItems on update+create; client.ts:179-194 toLineItemsPayload serializes name/kind/quantity/description and OMITS unit_price (no pricing)

**Plan:**

Gap is OPEN. FP job push carries no structured line items; HCP does. Mirror the HCP three-file pattern, with one blocking caveat to resolve first.

CAVEAT (decide before coding): FP's only line-item knowledge in the codebase is the INVOICE read path — client.ts:372/383 flattens line_items[].line_components[] off the /invoices resource. There is NO evidence FP's POST /jobs accepts line items; in FieldPulse, line items live on invoices/estimates, not jobs. Verify via vendor docs/Context7 (per AGENTS.md research-first rule). Two outcomes:
  (A) FP /jobs supports a line_items array → implement the full mirror below.
  (B) It does NOT → the honest parity move is to keep parity at the DESCRIPTION level (already done via job-mapping.ts:63-67 Work Type/System lines) and document the FP-only limitation in job-sync.ts, matching the Stage-10 plan's 'else document the limitation explicitly' convention. Do NOT invent a fake /jobs/{id}/line_items endpoint. Note: even the HCP impl is admittedly 'ASSUMED HCP SHAPE' (client.ts:169) — so option (A) at the same confidence level is defensible if the type/serializer stays additive and degrade-safe.

CONCRETE PLAN (option A — the mirror):
1. CREATE src/lib/integrations/fieldpulse/line-items.ts — copy housecall-pro/line-items.ts verbatim in logic (PURE, no DB/network), export buildLineItemsFromRequest + LineItemSource. Emit FieldpulseJobLineItem[] (service/labor rows, quantity 1, NO price). Keep the NO-PRICING invariant (line-items.ts:10-14): never set a unit price; descriptive only.
2. EDIT types.ts — add `export interface FieldpulseJobLineItem { name: string; kind?: 'service'|'material'|'labor'; quantity?: number; description?: string }` (mirror HousecallLineItem:103-117 but DROP unitPriceCents entirely, since FP job line items are unproven — keeps the type honest). Add `readonly lineItems?: readonly FieldpulseJobLineItem[]` to BOTH CreateJobInput (types.ts:73) and UpdateJobInput (types.ts:83). Name it FieldpulseJobLineItem to avoid colliding with existing FieldpulseInvoiceLineItem (types.ts:114).
3. EDIT job-mapping.ts — import buildLineItemsFromRequest; add `readonly lineItems?: readonly FieldpulseJobLineItem[]` to MappedJobFields (mapping.ts:44); in serviceRequestToJobFields (mapping.ts:86) derive `const li = buildLineItemsFromRequest({issueType, jobType, systemType, accessNotes}); const lineItems = li.length>0 ? li : undefined;` and include in both return branches (:91,:97). RequestJobInput already carries jobType/systemType/accessNotes (mapping.ts:31-36) — no new fields needed.
4. EDIT client.ts — add a toJobLineItemsPayload() mirroring toLineItemsPayload (client.ts:179-194) but with NO unit_price key at all; add `line_items: toJobLineItemsPayload(input.lineItems)` to createJob body (:520) and updateJob body (:540). Confirm the actual FP key name in docs (`line_items` is the read-side key per client.ts:372, reuse it).
5. EDIT job-sync.ts — no signature change needed; serviceRequestToJobFields(row.jobFields) already returns the new fields. Pass `lineItems: fields.lineItems` into client.createJob (job-sync.ts:195) and client.updateJob (job-sync.ts:155). jobFields already loads jobType/systemType/accessNotes (job-sync.ts:79-84,110-115) — no query change.

TESTS (TDD, vitest):
- CREATE src/lib/integrations/fieldpulse/line-items.test.ts — clone housecall-pro/line-items.test.ts: assert service line 'No Cool — No cooling', system line 'Central Ac service', access labor line carries accessNotes, empty source → []. CRITICAL added assertion: no row ever has a price field (guards the NO-PRICING invariant).
- ADD job-mapping test (no FP job-mapping test exists today) asserting serviceRequestToJobFields embeds lineItems and OMITS them when source is bare.
- ADD job-sync test with injected fetchImpl (job-sync.ts:139 fetchImpl param) asserting the POST /jobs body includes line_items with the derived rows and never includes a price/unit_price; assert update path forwards lineItems too.

DEGRADE-SAFETY: all additive + optional; an undefined lineItems omits the key (toJobLineItemsPayload returns undefined for empty), so existing pushes are byte-unchanged. job-sync error path (job-sync.ts:221-227) still swallows FP errors. If FP rejects an unknown line_items key it 4xx's → caught + WARN-logged → request flow unaffected (good), but this would SILENTLY drop the feature — hence the doc-the-caveat fallback if docs can't confirm support.
TENANT SCOPING: untouched — loadJobSyncRow already uses withTenant (job-sync.ts:88-94); no new queries.
NEON-HTTP: no new writes/transactions introduced; the single guarded UPDATE (job-sync.ts:205-215) is unchanged. No db.batch needed.
NO MONEY-SAFETY RISK: line items are price-free; they do not touch the native invoices table or the fieldpulse_invoice_id read-only guards.

VERIFY GATES: tsc --noEmit; lint (→0 per program convention); test:unit (new line-items + job-mapping + job-sync suites green); build. No prompt changes → no eval needed. No frozen-safety-text (hvac-knowledge.ts) or metadata.verify surfaces touched.

**Key risks:**
- UNCONFIRMED FP API: no evidence POST /jobs accepts line items (codebase only reads them off /invoices, client.ts:372). If unsupported, FP silently 4xx-drops them inside the swallowed catch (job-sync.ts:221) — feature appears done but no-ops. Resolve via vendor docs FIRST; else ship the documented-limitation fallback.
- Naming collision: FieldpulseInvoiceLineItem already exists (types.ts:114). Use a distinct FieldpulseJobLineItem and do not reuse the invoice type (it carries cents/amounts — would violate NO-PRICING).
- NO-PRICING invariant: FP job line items must carry no unit price (drop unit_price key entirely). HCP carries an unset optional unitPriceCents (types.ts:114); for FP, omit the field from the type so a price can never leak.
- Parity is to an ADMITTEDLY-ASSUMED HCP shape (client.ts:169 'ASSUMED HCP SHAPE') — this is guess-mirroring guesswork; keep everything additive/degrade-safe so a wrong shape can't break the booking flow.
- FP job-sync/job-mapping currently have ZERO unit tests (no job*.test.ts in fieldpulse/) — must add them, not just the line-items test, to actually satisfy the Verify gate 'pushed FP job carries the expected line items'.

**Independent verifier — sound:**
- Minor (not an error): assessment says HCP client.ts:179-194 toLineItemsPayload 'OMITS unit_price'. The source actually always includes a unit_price key set to item.unitPriceCents (which is undefined because the mapping never sets a price); JSON.stringify then drops the undefined value, so nothing reaches the wire. Net effect matches the claim; the plan's own step-4 ('no unit_price key at all') is in fact cleaner than HCP.
- Trivial line-offset drift in two citations: FP client.ts createJob is at 519-534 (cited ~520-528) and updateJob at 536-552 (cited ~540-546); the read-side invoice key is at client.ts:367/372/383 (cited ~372). All within tolerance and the substance (no line_items in job bodies; line_items only read off /invoices) is correct.

_Verifier notes:_ Independently confirmed every cited file:line. Gap is OPEN: FP job push carries description + schedule only (job-sync.ts:155-159 update, 195-201 create); types.ts CreateJobInput/UpdateJobInput (73-89) and client.ts createJob/updateJob bodies have no line_items; no line-items.ts exists in fieldpulse/ while HCP has the full three-file pattern (line-items.ts, job-mapping.ts:22/62/99-114, job-sync.ts:151/194, client.ts:179-194). Plan is feasible end-to-end: loadJobSyncRow already SELECTs jobType (job-sync.ts:79) and systemType (:80) and maps accessNotes (:84/115), so steps 3 and 5 need no query change as claimed; RequestJobInput already carries jobType/systemType/accessNotes (job-mapping.ts:33-36). All five keyRisks are grounded in real code, including the critical one: FP /jobs line-item support is unproven (codebase only reads line_items off /invoices) and a rejected unknown key would be silently swallowed by the catch at job-sync.ts:221-227 — the assessment correctly gates on vendor-doc confirmation and offers a documented-limitation fallback (option B). Naming collision with FieldpulseInvoiceLineItem (types.ts:114, carries cents) is real. FP job-sync/job-mapping have zero unit tests today (confirmed). Invariants honored: additive/optional + degrade-safe, no neon-http transaction, tenant scoping untouched, price-free so no money-safety surface touched. No frozen-safety-text or metadata.verify surfaces involved. Verdict: sound.

---

## Stage 12 — technician_skills table + CRUD UI  _(plan)_

**Verified status:** real-gap  **·  Verify verdict:** sound  **·  Effort:** M

**Evidence (re-confirmed against code):**
- grep for technician_skill|technicianSkill|technician-skill across *.ts/*.tsx/*.sql returns zero matches (no table, no model, no UI, no API)
- src/lib/db/schema.ts:998 — only mention of 'skill' is a comment; no technician_skills pgTable exists
- src/lib/ai/dispatch/signals.ts:66-98 — skill signal is derived purely from completed serviceRequests matching jobType/systemType (skillPredicate over serviceRequests.status='completed'); no explicit-skill source
- src/lib/ai/dispatch/score.ts:45 — `const skillMatched = tech.skillJobsCompleted > 0;` hard gate; a new tech with certs but 0 completed jobs is dropped by rankTechnicians (score.ts:72 `.filter((r) => r.skillMatched)`)
- src/lib/admin/scheduling-queries.ts:784-812 — rankedTechnicianOrder feeds skillJobsCompleted straight from loadDispatchSignals; this is the single integration point for an explicit-skill override
- drizzle/meta/_journal.json — last migration idx 21 tag 0021_tearful_echo; next is 0022
- src/app/admin/(dashboard)/technicians and src/app/api/admin/technicians both exist — natural home for the skills CRUD sub-surface

**Plan:**

GAP IS OPEN. Nothing named technician_skills exists anywhere. Skill is 100% completion-history-derived (signals.ts:66-98 → score.ts:45 hard gate), so the exact failure in the plan (new certified tech, 0 completed jobs → never auto-assigned, dropped at score.ts:72) is real.

PLAN (file-level):

1. Migration (hand-authored + snapshot — drizzle-kit can't be trusted on neon for this repo per memory):
   - Add `drizzle/0022_technician_skills.sql`:
     CREATE TABLE technician_skills (
       id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
       organization_id uuid NOT NULL REFERENCES organizations(id) ON DELETE CASCADE,
       user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,
       job_type job_type,          -- nullable
       system_type system_type,    -- nullable
       proficiency integer NOT NULL DEFAULT 1,  -- optional depth signal (1..5)
       created_at timestamptz NOT NULL DEFAULT now(),
       updated_at timestamptz NOT NULL DEFAULT now()
     );
     CHECK at least one of job_type/system_type NOT NULL.
     UNIQUE(organization_id, user_id, job_type, system_type) — note NULLs: use a partial/expression unique or COALESCE-based unique index since SQL treats NULLs as distinct. Recommend two partial unique indexes (one WHERE system_type IS NULL keyed on job_type, one WHERE job_type IS NULL keyed on system_type) OR a generated normalized key column. Keep it additive.
     index on (organization_id, user_id).
   - Append entry idx 22 tag 0022_technician_skills to drizzle/meta/_journal.json and copy 0021_snapshot.json → 0022_snapshot.json with the new table added (the hand-authored-trigger-migration memory pattern). Reuse existing job_type/system_type pgEnums (schema.ts:77,117) — DO NOT create new enums.
   - REMEMBER per memory 'migrations-not-run-on-deploy': run `npm run db:migrate` after adding, or .returning() writes 500.

2. Schema model — add `technicianSkills` pgTable to src/lib/db/schema.ts mirroring users-table tenant conventions (organizationId FK cascade, index org_id, withTenant-compatible). Reference jobTypeEnum/systemTypeEnum.

3. Query layer — new src/lib/admin/technician-skills-queries.ts:
   - listTechnicianSkills(orgId, userId?) — withTenant-scoped.
   - createTechnicianSkill(orgId, {userId, jobType?, systemType?, proficiency}) — validate userId belongs to org (select users withTenant) before insert; catch 23505 → friendly duplicate error.
   - deleteTechnicianSkill(orgId, id) — withTenant guard in WHERE.
   - (Stage 19-style PATCH optional; CRUD plan says create + delete is enough for parity; add update only if proficiency editing wanted.)
   - loadExplicitSkills(orgId, technicianIds, job) → Map<techId, boolean> matching job.jobType OR job.systemType. neon-http: plain selects, no transaction; if a batch insert is ever needed use db.batch (no interactive tx).

4. Wire precedence into dispatch (the load-bearing change):
   - Option A (minimal, preferred): in scheduling-queries.ts rankedTechnicianOrder (lines 794-812), after loadDispatchSignals, fetch loadExplicitSkills(orgId, technicianIds, job); for any tech with an explicit matching skill, ensure skillMatched passes even at 0 completed jobs. Cleanest: extend TechSignalRow/DispatchSignals.tech with `hasExplicitSkill: boolean` and change score.ts:45 to `const skillMatched = tech.skillJobsCompleted > 0 || tech.hasExplicitSkill;`. Keep skillDepth from completion history (explicit skill grants the gate, not artificial depth) so ranking still favors proven techs — explicit skill only RESCUES a 0-history certified tech from the drop filter (score.ts:72). Default hasExplicitSkill=false everywhere (signals.ts:58 defaulted map) → zero behavior change for orgs with no skills rows (degrade-safe).
   - Do the explicit-skill lookup inside loadDispatchSignals (add 4th parallel query in the Promise.all at signals.ts:81) so it's one round-trip and tenant-scoped consistently; default 0/false for missing techs as the map already does.

5. API routes (mirror membership-plans/route.ts template exactly — getAdminSession gate, slidingWindow RATE_LIMITS.adminRead/adminMutation, successResponse/errorResponse, logAudit best-effort .catch):
   - src/app/api/admin/technicians/skills/route.ts → GET (list, optional ?userId=), POST (create, zod schema: userId uuid, jobType enum optional, systemType enum optional, refine at-least-one, proficiency int 1-5 default 1).
   - src/app/api/admin/technicians/skills/[id]/route.ts → DELETE (params is a Promise — `const { id } = await params` per Next.js 16 fork). Audit action create_technician_skill / delete_technician_skill; details = field names/enums only (no PII per audit-details memory).

6. CRUD UI — add a Skills section to the existing technicians admin surface (src/app/admin/(dashboard)/technicians): per-tech skill chips with add (jobType/systemType select + proficiency) and remove. Match the existing admin component/Card styling; client component calls the new endpoints. No new nav entry needed (lives under technicians).

7. TESTS (TDD, vitest):
   - score.test.ts: add case — skillJobsCompleted=0 + hasExplicitSkill=true → skillMatched true, NOT dropped by rankTechnicians; skillJobsCompleted=0 + hasExplicitSkill=false → still dropped (preserves existing score.test.ts:74-87 expectations). Confirm weight sum unchanged (skillDepth still completion-based).
   - signals.test.ts: extend the mocked Promise.all to a 4th (explicit-skill) result; assert hasExplicitSkill maps correctly and defaults false for unknown techs (DB-mocked, runs headless — unlike route tests).
   - technician-skills-queries: unit test create dedupe (23505 path) + tenant scoping (withTenant in WHERE) + cross-org userId rejected. Note per vitest-env memory: DB-touching suites that import the real client fail at import headless — mock db like signals.test.ts does.

VERIFY GATES: `npm run tsc` / typecheck, `npm run lint` (must stay 0 per improvement-program memory), `npm run test:unit` (score/signals/queries green), `npm run build`. No prompt edits → eval NOT required (hvac-knowledge.ts frozen safety untouched). No money-safety / metadata.verify surfaces touched.

DEGRADE-SAFETY: default hasExplicitSkill=false → orgs without skills rows behave identically (no regression); explicit skill only ADDS candidates, never removes a previously-matched tech. TENANT: every query withTenant(table, orgId); userId validated in-org before insert. NEON-HTTP: only selects/single inserts; no db.transaction (use db.batch if a multi-write ever needed).

**Key risks:**
- NULL-in-unique-constraint: SQL treats NULL as distinct, so UNIQUE(org,user,job_type,system_type) won't dedupe rows where one of job_type/system_type is NULL — must use partial unique indexes or a normalized generated key, else duplicate skills slip in
- Precedence semantics: granting full skillDepth to an explicit-skill tech with 0 history would let an unproven certified tech outrank a 9-job veteran — plan deliberately grants only the gate (skillMatched), not depth; reviewer should confirm this is the intended product behavior
- Migration-not-run-on-deploy (memory): forgetting `npm run db:migrate` makes any .returning() insert 500 in prod; and the journal+snapshot hand-edit must be exact or drizzle-kit drifts
- Reusing job_type/system_type enums vs free-text: a free-text cert (e.g. 'EPA 608') can't be matched against the jobType/systemType-keyed dispatch signal — scope is enum-aligned skills only; non-enum certs are out of scope for auto-assign matching

**Independent verifier — sound:**
- Minor line-number imprecision (substance correct): assessment cites scheduling-queries.ts:784-812 for 'rankedTechnicianOrder feeds skillJobsCompleted'; the function starts at 784 but the actual loadDispatchSignals call + candidate mapping that feeds skillJobsCompleted is lines 794-812 (the plan body itself correctly says 794-812). Confirmed at score.ts:45 (skillMatched gate) and score.ts:72 (.filter) which match exactly.
- The plan says to add hasExplicitSkill to 'TechSignalRow/DispatchSignals.tech' as if one edit — these are TWO distinct interfaces in TWO files (TechSignalRow at signals.ts:23, DispatchSignals.tech at score.ts:13), plus the candidate-construction mapping at scheduling-queries.ts:800-811 must also be extended. Three touch-points, not implied two. Not a defect, just under-counted in the prose.
- score.test.ts existing tests (cited :74-87, confirmed accurate) use a local signals(...) helper that builds DispatchSignals; adding hasExplicitSkill means that helper must default the field for existing cases to keep them green. The plan's claim 'preserves existing score.test.ts:74-87 expectations' holds only if the helper defaults hasExplicitSkill=false — feasible but unstated.
- API route DELETE template uses `const { id } = await context.params` (params nested under context arg), not the plan's shorthand `const { id } = await params`. Cosmetic; the 'params is a Promise / Next.js 16 fork' point is correct.

_Verifier notes:_ Independently re-derived the status: GAP IS OPEN, confirmed. grep for technician_skill|technicianSkill|technician-skill across *.ts/*.tsx/*.sql returns zero matches. schema.ts:998 is the sole 'skill' hit (a comment) — verified. Skill is 100% completion-history-derived: signals.ts:66-98 (skillPredicate over serviceRequests.status='completed') → score.ts:45 hard gate `tech.skillJobsCompleted > 0` → score.ts:72 `.filter((r) => r.skillMatched)` drops 0-history techs. The plan's failure scenario (new certified tech, 0 completed jobs, dropped) is real and accurately traced.

Plan feasibility checked against real code — all load-bearing facts hold:
- Journal last idx = 21 (0021_tearful_echo); next is 0022 — CONFIRMED. 0021_snapshot.json exists, so the journal+snapshot copy pattern (hand-authored-trigger-migration memory) is the established approach.
- jobTypeEnum at schema.ts:77, systemTypeEnum at schema.ts:117 — CONFIRMED exact lines. Reusing them as columns is valid (already used at schema.ts:490/492). 'DO NOT create new enums' is sound.
- withTenant signature (tenant.ts) is (table, orgId, ...conditions: SQL[]) — matches every query-layer call the plan proposes.
- membership-plans/route.ts + [id]/route.ts template matches the plan's prescription exactly: getAdminSession gate, slidingWindow + RATE_LIMITS.adminRead/adminMutation, successResponse/errorResponse, logAudit best-effort .catch, await params. A faithful copy target.
- membership-queries.ts confirms the query-layer conventions (withTenant in every WHERE, .returning() insert, db.batch for multi-write — corroborating the neon-http no-transaction and migrations-not-run-on-deploy risks the plan flags).
- Both technicians dirs exist: src/app/api/admin/technicians/ (route.ts + [id]/) and src/app/admin/(dashboard)/technicians/page.tsx — natural CRUD home, as claimed.
- users table has organizationId + users_org_id_idx (schema.ts:262,296) — the tenant/index pattern the new table mirrors.

Key risks in the assessment are legitimate and well-prioritized — the NULL-in-unique-constraint risk is genuinely the trickiest part (SQL treats NULLs as distinct; partial unique indexes or a normalized key are the right fix). The precedence-semantics call (grant the gate, not depth) is a sound, conservative product choice that keeps the existing skillDepth weighting unchanged (degrade-safe). No invariant violations: no frozen-safety/prompt edits, no money-safety/metadata.verify surfaces, neon-http transactions avoided, tenant scoping preserved. Effort 'M' is reasonable (migration + schema + query layer + dispatch wiring + 2 routes + UI + 3 test files). Verdict: sound, with only cosmetic/under-counting corrections noted.

---

## Stage 13 — Technician base location + proximity scoring  _(plan)_

**Verified status:** real-gap  **·  Verify verdict:** minor-corrections  **·  Effort:** M

**Evidence (re-confirmed against code):**
- src/lib/db/schema.ts:258-329 users table has email/name/role/laborRateCents etc. but NO baseLat/baseLng/latitude/longitude columns
- src/lib/ai/dispatch/score.ts:30-32 only W_SKILL=0.5, W_QUALITY=0.3, W_LOAD=0.2 (sum 1.0); no proximity weight
- src/lib/ai/dispatch/score.ts:51 score = skillDepth*W_SKILL + quality*W_QUALITY + load*W_LOAD — no distance term
- src/lib/ai/dispatch/score.ts:13-19 DispatchSignals.tech has skillJobsCompleted/avgRating/sameDayJobCount only — no distanceKm
- src/lib/ai/dispatch/signals.ts:22-27 TechSignalRow has no distance; loadDispatchSignals never reads coordinates
- src/lib/admin/scheduling-queries.ts:735-752 loadJobClassification selects jobType/systemType/urgency only — never resolves locationId or site coords
- src/lib/db/schema.ts:478 serviceRequests.locationId (nullable) → customer_locations; src/lib/db/schema.ts:1845-1846 customer_locations.latitude/longitude exist (job-site side already present)
- src/lib/address/photon.ts:211-227 pure haversineKm(aLat,aLon,bLat,bLon) already exists and is reusable
- drizzle/meta/_journal.json last idx 21 (0021_tearful_echo.sql) — next migration is 0022; hand-authored + snapshot convention
- src/app/api/admin/staff/[id]/route.ts:17-36 updateStaffSchema has name/role/isActive/laborRateCents — no baseLat/baseLng; src/lib/admin/staff-queries.ts:245 updateStaff is the save seam

**Plan:**

GAP IS OPEN. No tech coordinates exist; scorer has no proximity term. Concrete plan:

(1) SCHEMA + MIGRATION (additive). Add to users (schema.ts:258): `baseLat: doublePrecision('base_lat')`, `baseLng: doublePrecision('base_lng')` (both nullable — most techs won't have coords day 1). Hand-author drizzle/0022_*.sql: `ALTER TABLE users ADD COLUMN base_lat double precision; ADD COLUMN base_lng double precision;` Update drizzle/meta/_journal.json (append idx 22) + copy/patch the snapshot under drizzle/meta (the hand-authored-migration convention in MEMORY). Run `npm run db:migrate` after (Vercel skips migrations — see migrations-not-run-on-deploy memory).

(2) SCORE.TS (pure, the core). Extend DispatchSignals.tech with `readonly distanceKm: number | null` (score.ts:13-19) and RankedTech unchanged. Re-balance weights so they still sum to 1.0 AND the existing weight-sum test passes: set W_SKILL=0.45, W_QUALITY=0.25, W_LOAD=0.15, W_PROX=0.15 (sum 1.0). Compute `proximity = distanceKm == null ? NEUTRAL : 1 - Math.min(distanceKm, DIST_CAP)/DIST_CAP` with DIST_CAP=80 (km, ~50mi service radius per business-location memory). DEGRADE-SAFETY: when distanceKm is null (tech or site has no coords) use NEUTRAL=0.5 so a coordless tech is neither rewarded nor hard-excluded — proximity must NEVER gate (skillMatched stays the only filter in rankTechnicians, score.ts:69-77). Add a reason string only when distance is known: `${Math.round(distanceKm)}km away`. CRITICAL: the existing 'max signals → 1.0' test (score.test.ts:35-44) must be updated — max now needs distanceKm=0 to reach 1.0; assert new weight sum.

(3) SIGNALS.TS. Add `distanceKm` to TechSignalRow (signals.ts:22-27). loadDispatchSignals needs the tech baseLat/baseLng (one `select` from users by ids, withTenant) AND the job-site lat/lng. Pass site coords IN as a param (don't re-query) — compute distance in JS via haversineKm imported from photon.ts (reuse, do not reimplement). When either side is null → distanceKm null.

(4) SCHEDULING-QUERIES.TS wiring. loadJobClassification (735-752) must also resolve the job site coords: LEFT JOIN customer_locations ON serviceRequests.locationId, select latitude/longitude (both org-scoped via withTenant). Thread siteLat/siteLng through rankedTechnicianOrder (784) into loadDispatchSignals and into the candidate map (800-811). Null site coords → all distances null → proximity neutral → ZERO behavior change vs today (degrade-safe; preserves the 'opt-in never strands a job' invariant at 850-860).

(5) GEOCODE-ON-SAVE. Extend updateStaffSchema (staff/[id]/route.ts:17-36) with optional baseLat/baseLng (z.number().min(-90).max(90) / min(-180).max(180), nullable) OR a baseAddress string that gets geocoded. Simplest + lowest-risk: accept explicit baseLat/baseLng from an admin address-autocomplete field (reuse src/components/chat/address-autocomplete.tsx which already returns lat/lon via Photon) — avoids a server-side geocode call entirely. If geocoding server-side is desired, call fetchAddressSuggestions in after() (NEVER a detached promise — Vercel freeze, per serverless-background-work memory) and patch the row; but explicit-coords-from-autocomplete is preferred (no new failure mode). Update updateStaff (staff-queries.ts:245) to persist the columns. Audit-log field names only (no PII) per the existing pattern at route.ts:131-137.

TESTS (TDD, vitest): score.test.ts — (a) weight sum still 1.0; (b) closer tech outranks farther equal-skill tech; (c) distanceKm=null → neutral 0.5 proximity, never excluded; (d) DIST_CAP clamps; (e) max signals incl distance=0 → 1.0. signals.test.ts — distance computed from tech+site coords; null when either missing; tenant-scoped users query. Optional integration: rankedTechnicianOrder prefers nearer tech.

VERIFY GATES: `npx tsc --noEmit`, lint, `npm run test:unit` (score/signals suites), `npm run build`. No prompt changes → no eval needed. NEON-HTTP: no transactions used here (pure reads + one additive migration); if any multi-write is added use db.batch (no interactive tx). TENANT: every new query withTenant(table, orgId, ...). Note vitest-env baseline: ~7 DB-dependent suites fail at import without .env.local — score.test/signals.test are pure and unaffected.

**Key risks:**
- Weight re-balance silently breaks the existing 'max signals → 1.0' assertion (score.test.ts:35-44) unless the test is updated to set distanceKm=0; this is the most likely regression
- Proximity must be a soft signal (neutral on null), NOT a filter — if it ever gates, a coordless tech (the common case today) gets excluded and an opted-in org regresses to assigning fewer jobs than opted-out, violating the documented invariant at scheduling-queries.ts:783-793,850-860
- Job-site coords are only present when serviceRequests.locationId is set AND customer_locations.lat/lng populated (both lazy/nullable, schema.ts:478,1845-1846) — in practice often null, so proximity is frequently neutral; the feature only bites once locations are geocoded
- Tenant scope: the new users coord query and the customer_locations join must both be withTenant — a missing org filter on the join leaks cross-org site coordinates
- Server-side geocode-on-save, if chosen, must use after() not a detached promise (Vercel freeze) and degrade to no-coords on Photon failure

**Independent verifier — minor-corrections:**
- Incomplete test-impact analysis (the one substantive flaw). The plan flags only the 'max signals -> 1.0' test (actually score.test.ts:34-45) as needing update for the weight rebalance. But the proposed weights (W_QUALITY 0.3->0.25, W_LOAD 0.2->0.15) ALSO break two other existing tests that hard-code the old constants: 'defaults a missing rating to 3.5/5' (score.test.ts:47-53, asserts r.score === (3.5/5)*0.3 + 0.2) and the arithmetic baseline of 'penalizes same-day load' (score.test.ts:55-61). Any W_QUALITY/W_LOAD change breaks line 52 exactly. These were not listed in the TESTS section and must also be updated.
- Imprecise test reference: the assessment repeatedly refers to an 'existing weight-sum test' / 'assert new weight sum' (step 2 and keyRisks #1). There is NO standalone weight-sum test in score.test.ts (verified via grep). The only assertion pinning the weights is the max-signals test at lines 34-45. The risk is real but mislabeled.
- Minor line-range drift: 'score.test.ts:35-44' for the max-signals test — the it() block spans 34-45 (comment at 35, assertions through 44). 'score.ts:13-19' for DispatchSignals.tech and '30-32' for weights are exact. customer_locations.latitude/longitude cited at 1845-1846 — exact. users table cited at 258-329 — table spans 258-326. All trivially close, none misleading.

_Verifier notes:_ Independently re-derived the status as real-gap: users table (schema.ts:258-326) has no base_lat/base_lng; scoreTechnician (score.ts:43-62) has no distance term and DispatchSignals.tech (13-19) carries no distanceKm. Confirmed.

Every cited file:line was opened and verified accurate: score.ts weights/signals, signals.ts TechSignalRow + loadDispatchSignals, scheduling-queries.ts loadJobClassification(735-752)/rankedTechnicianOrder(784-813), photon.ts haversineKm(211-227, pure + exported), serviceRequests.locationId(478), customer_locations lat/lng(1845-1846), staff/[id]/route.ts updateStaffSchema(17-36)+audit pattern(131-137), staff-queries.ts updateStaff(245), journal last idx 21 (0021_tearful_echo, next is 0022).

Plan is feasible: doublePrecision is already imported and in use; haversineKm is already reused by address-autocomplete.tsx (8,33) which returns lat/lon, validating the explicit-coords-from-autocomplete approach in step 5; the degrade-safe neutral-on-null design genuinely preserves the documented 'opt-in never strands a job' invariant (scheduling-queries.ts:778-793,849-861) since skillMatched stays the only filter in rankTechnicians(69-77); additive migration + withTenant scoping + no-transaction constraints all respected.

The single substantive correction is the incomplete test-update list: rebalancing W_QUALITY/W_LOAD breaks the rating-default and load-penalty tests, not just the max-signals one. keyRisks correctly identifies the most-likely-regression in spirit but understates its breadth and mislabels it as a 'weight-sum test' that does not exist. These are plan refinements; the gap call, architecture, weights-sum-to-1.0 target, DIST_CAP/NEUTRAL semantics, and verify gates are all sound. Effort 'M' is reasonable.

---

## Stage 14 — Tunable confidence threshold (org-level)  _(plan)_

**Verified status:** real-gap  **·  Verify verdict:** sound  **·  Effort:** M

**Evidence (re-confirmed against code):**
- src/lib/ai/dispatch/score.ts:45 — `const skillMatched = tech.skillJobsCompleted > 0;` (the hard boolean gate)
- src/lib/ai/dispatch/score.ts:72 — `.filter((r) => r.skillMatched)` in rankTechnicians; nothing reads a score threshold
- src/lib/ai/dispatch/score.ts:69 — `rankTechnicians(candidates: readonly DispatchSignals[])` takes no threshold/options arg
- src/lib/db/schema.ts:999 — only `autoDispatchEnabled` boolean on organization_settings; no min-score/threshold column
- src/lib/admin/scheduling-queries.ts:812 — `return rankTechnicians(candidates);` (caller passes no threshold)
- src/lib/admin/org-config-types.ts:125,148,164 — OrgConfigUpdate/OrgConfig/DEFAULT_ORG_CONFIG expose only autoDispatchEnabled, no threshold
- src/components/admin/settings/dispatch-panel.tsx:60-66 — UI is a single on/off Switch; no threshold control
- grep for minScore|dispatch_threshold|confidenceThreshold across src/ returns only unrelated FieldPulse address-quality hits — no dispatch threshold exists
- drizzle/0021_tearful_echo.sql:1 — last/highest migration on disk adds auto_dispatch_enabled; next hand-authored migration is 0022 (journal idx 21 is latest)

**Plan:**

GAP IS OPEN. Auto-dispatch gates purely on the boolean `skillMatched = skillJobsCompleted > 0` (score.ts:45) and drops non-matches (score.ts:72); there is no org-tunable minimum score. Concrete plan:

(1) MIGRATION — hand-authored `drizzle/0022_dispatch_min_score.sql`: `ALTER TABLE \"organization_settings\" ADD COLUMN \"dispatch_min_score\" real;` (nullable; NULL = current behavior). Mirror the 0021 hand-authored pattern: add the column to the latest snapshot (copy drizzle/meta/0021_snapshot.json -> 0022_snapshot.json with the new column under organization_settings.columns, bump id/prevId) and append a journal entry (idx 22) per the hand-authored-trigger-migration memory. Use `real` to match score range 0..1 (floating). Remember to run `npm run db:migrate` after (migrations-not-run-on-deploy memory).

(2) SCHEMA — src/lib/db/schema.ts: add `dispatchMinScore: real(\"dispatch_min_score\")` to organizationSettings (import `real` from drizzle-orm/pg-core if not already imported). Place near autoDispatchEnabled:999.

(3) SCORING — src/lib/ai/dispatch/score.ts: change `rankTechnicians(candidates, minScore?: number)`. Keep `skillMatched` as the hard skill gate (a tech with zero matching jobs must still be dropped — that is the explainability/skill invariant, not the tunable). Add a SECOND filter: `.filter((r) => r.score >= (minScore ?? 0))`. Default `minScore = 0` (or undefined) so existing behavior is byte-identical (degrade-safe). Do NOT remove the skillMatched filter — threshold is additive, not a replacement, since a non-skill-matched tech can still score >0 via quality/load (quality 3.5/5*0.3 + load 0.2 = 0.41 with zero skill), so dropping the skill gate would REGRESS by auto-assigning unqualified techs. This is the key correctness trap.

(4) CALLER — src/lib/admin/scheduling-queries.ts: `isAutoDispatchEnabled` (725) currently selects only `enabled`; replace with a single read returning `{ enabled, minScore }` (or add a sibling select) so we read both columns in one query (org_settings is PK-keyed by orgId — eq is the tenant scope, already correct at :729). Thread `minScore` through `rankedTechnicianOrder` (784) into `rankTechnicians` at :812. Pass via after()-driven path unchanged.

(5) CONFIG TYPES — src/lib/admin/org-config-types.ts: add `dispatchMinScore: z.number().min(0).max(1).nullable().optional()` to orgConfigUpdateSchema (:125 area, keep .strict()); add `readonly dispatchMinScore: number | null` to OrgConfig (:148) and `dispatchMinScore: null` to DEFAULT_ORG_CONFIG (:164).

(6) CONFIG QUERIES — src/lib/admin/org-config-queries.ts: map `dispatchMinScore: row.dispatchMinScore ?? null` (:52 area); add patch + insert wiring mirroring autoDispatchEnabled (:90-91, :112): `if (update.dispatchMinScore !== undefined) patch.dispatchMinScore = update.dispatchMinScore;` and insert default `dispatchMinScore: update.dispatchMinScore ?? null`. neon-http upsert already uses onConflictDoUpdate (no transaction needed — respects neon-http no-transactions rule).

(7) UI — src/components/admin/settings/dispatch-panel.tsx: add a slider/number input (0.0–1.0, step 0.05) shown only when autoDispatchEnabled, calling onSave({ dispatchMinScore }); disable/grey when toggle off. Keep copy explaining higher = stricter (more jobs left for a dispatcher).

(8) TESTS (TDD, vitest) — score.test.ts: (a) threshold filters as configured (tech with score below threshold dropped); (b) default/undefined minScore preserves current behavior (existing assertions unchanged); (c) CRITICAL: a non-skill-matched tech is still dropped even when its quality/load score exceeds a low threshold (guards the trap in step 3). org-config-queries / org-config-types: round-trip dispatchMinScore through update/read; partial update doesn't clobber. scheduling-queries.test.ts: ranked order respects org minScore.

VERIFY GATES: `npm run typecheck` (tsc), `npm run lint`, `npm run test:unit`, `npm run build`. No prompt changes -> no eval needed. No frozen-safety-text / money-safety / metadata.verify surfaces touched.

DEGRADE-SAFETY: NULL/undefined threshold == today's behavior exactly; an out-of-range value is rejected at the API zod boundary; a mis-stored value falls back to 0. TENANT SCOPE: all reads/writes go through organizationSettings keyed by orgId (PK) or withTenant — preserved. NEON-HTTP: single upsert, no interactive transaction.

Effort M: ~7 files + 1 hand-authored migration + tests; mechanical once the additive-not-replacement skill-gate decision is respected.

**Key risks:**
- Correctness trap: replacing the skillMatched filter with a score threshold would auto-assign UNQUALIFIED techs (a zero-skill tech scores ~0.41 from quality+load), regressing safety. Threshold must be ADDITIVE to the skill gate, never a replacement.
- Degrade-safety: must default to NULL/0 so opted-in orgs and existing tests see byte-identical behavior; non-default could strand jobs that first-fit would have placed.
- Migration must be hand-authored (drizzle-kit snapshot/journal copy per 0021 pattern) and `npm run db:migrate` run post-merge — Vercel build skips migrations, and a .returning()/insert against a missing column 500s.
- Score units: weights sum to 1.0 so score ∈ [0,1]; UI/schema must constrain threshold to 0–1, not an arbitrary integer, or the slider produces a gate that drops everyone.

**Independent verifier — sound:**
- (no corrections — independently confirmed)

_Verifier notes:_ Independently verified against the real code; the assessment holds up.

STATUS CONFIRMED real-gap. Auto-dispatch gates purely on the boolean skillMatched (score.ts:45) and drops non-matches (score.ts:72); rankTechnicians takes no threshold (score.ts:69). No org-tunable min-score column exists. Re-derived the status myself — not an over- or under-call.

EVERY cited file:line is accurate:
- score.ts:45 `const skillMatched = tech.skillJobsCompleted > 0;` — exact.
- score.ts:72 `.filter((r) => r.skillMatched)` — exact.
- score.ts:69 `rankTechnicians(candidates: readonly DispatchSignals[])` — no threshold/options arg — exact.
- schema.ts:999 `autoDispatchEnabled: boolean("auto_dispatch_enabled").notNull().default(false)` — only auto-dispatch flag, no threshold column — exact.
- scheduling-queries.ts:812 `return rankTechnicians(candidates);` — caller passes no threshold — exact.
- org-config-types.ts:125 (schema autoDispatchEnabled), :148 (OrgConfig), :164 (DEFAULT_ORG_CONFIG) — confirmed, no dispatchMinScore anywhere.
- dispatch-panel.tsx:60-66 — single on/off Switch, no threshold control — confirmed.
- grep for dispatch threshold — confirmed none exist.
- 0021_tearful_echo.sql is the latest migration (journal idx 21); it is a hand-authored ALTER TABLE ADD COLUMN auto_dispatch_enabled, and 0021_snapshot.json exists. The "mirror the 0021 hand-authored pattern" claim is grounded in reality.

PLAN FEASIBILITY — every step compiles against the actual code:
- `real` is importable from drizzle-orm/pg-core (schema.ts line 14 import block). Schema add is valid.
- Step 4 caller threading: isAutoDispatchEnabled (725) currently selects only `enabled` via eq(organizationSettings.organizationId, ...) — PK-keyed tenant scope confirmed correct. rankedTechnicianOrder (784) → rankTechnicians (812) is the right path; both need the param threaded, correctly identified.
- Step 6 org-config-queries: row.autoDispatchEnabled map (52), patch wiring (90-91), insert default (112), onConflictDoUpdate upsert (114) all exist exactly as described — neon-http no-transaction rule respected (single upsert, no db.transaction()).
- Step 1 migration: hand-authored + snapshot/journal copy + run db:migrate matches the repo's established pattern and the migrations-not-run-on-deploy memory.

CORRECTNESS TRAP IS MATHEMATICALLY VALID: a zero-skill tech scores skillDepth*0.5 (0) + quality*0.3 (3.5/5*0.3 = 0.21) + load*0.2 (1*0.2 = 0.2) = 0.41 (assessment said ~0.41, exact). So replacing the skillMatched filter with a score threshold WOULD auto-assign unqualified techs. The "additive, not replacement" decision is correct and load-bearing.

DEGRADE-SAFETY claim valid: NULL/undefined minScore defaulting to 0 preserves byte-identical behavior; existing score.test.ts and signals.test.ts present, so TDD step is grounded.

No invariant violations (no frozen-safety-text, money-safety, or metadata.verify surfaces touched; tenant scoping via organizationId PK / withTenant preserved). Effort M is reasonable: ~7 files + 1 hand-authored migration + tests, mechanical.

No corrections to make — the assessment is sound as written.

---

## Stage 15 — Failed-auto-assign reconcile sweep (cron)  _(plan)_

**Verified status:** real-gap  **·  Verify verdict:** minor-corrections  **·  Effort:** M

**Evidence (re-confirmed against code):**
- src/app/api/cron/ — directory listing shows NO reconcile/auto-assign sweep (only cleanup, webhook-cleanup, process-communications, booking-recovery, reconcile-payments, dunning, generate-membership-visits, sync-fieldpulse-availability, sync-fieldpulse-invoices, sync-housecall-invoices)
- vercel.json:2-43 — crons array contains no auto-assign reconcile path
- src/lib/requests/submit-session-request.ts:313-345 — autoAssignBookedRequest is invoked exactly once, inside after(), best-effort: on !result.assigned it only logs 'left soft-held for dispatcher' (line 332-336) and never schedules a retry
- src/lib/admin/scheduling-queries.ts:822-825 — docstring confirms intent: 'Best-effort: returns {assigned:false} when nobody fits, leaving the soft-held window for a dispatcher.' No sweep re-runs it.
- grep for reconcile|sweep|reattempt across src/app/api/cron and src/lib/ai/dispatch returns only payment/booking-recovery/invoice crons — none re-run dispatch
- src/lib/admin/scheduling-queries.test.ts:735-823 — only existing autoAssignBookedRequest tests are the inline scored-vs-first-fit cases; no sweep test exists

**Plan:**

GAP IS OPEN. No reconcile sweep exists. Concrete plan:

DATA MODEL (no migration needed). Reconcile target = a request that was soft-held but never assigned: status IN ACTIVE_BOOKING_STATUSES (scheduling-queries.ts:52-58), arrivalWindowStart IS NOT NULL, arrivalWindowEnd IS NOT NULL, assignedTo IS NULL, autoAssigned=false, and the window is still in the future (gt(arrivalWindowEnd, now)). Bound recency with createdAt within a lookback (e.g. last 48h) so the sweep is a finite re-attempt window, not an unbounded backfill. The (window, isoDay) args autoAssignBookedRequest needs are RECONSTRUCTED from arrivalWindowStart, not stored: isoDay = arrivalWindowStart.toISOString().slice(0,10); window = match arrivalWindowStart.getUTCHours()/getUTCEndHours against WINDOW_HOURS in arrival-window.ts:20-25 (morning[8,12]/afternoon[12,16]/evening[16,20]/anytime[8,20]). Add a pure helper windowFromUtcBounds(start,end): ArrivalWindow|null to arrival-window.ts (single source of truth, unit-testable) rather than re-deriving inline.

FILES TO CREATE:
1) src/lib/admin/reconcile-auto-assign.ts — exports reconcileUnassignedRequests(organizationId): Promise<{considered:number; assigned:number}>. Query the soft-held-unassigned rows tenant-scoped via withTenant(serviceRequests, organizationId, ...) (mirror listUnscheduledRequests at scheduling-queries.ts:310-338 but ADD isNull(assignedTo) AND isNotNull(arrivalWindowStart) AND a future-window bound). For each row: derive isoDay+window; if window===null, skip+log (don't guess); call existing autoAssignBookedRequest(orgId, id, {start,end,isoDay,window}). It is already idempotent — re-running on an already-assigned row is prevented by the assignedTo IS NULL filter, and placeAndAssignRequest re-checks conflicts. Tally considered/assigned.
2) src/app/api/cron/reconcile-auto-assign/route.ts — copy booking-recovery/route.ts verbatim structure (cron route.ts:7-59): export const dynamic='force-dynamic'; runtime='nodejs'; GET gated by verifyCronAuth(request.headers.get('authorization')) → 401 (cron-auth.ts:29 fails closed); loop db.select({id}).from(organizations); per-org try/catch logger.error failure-isolation; return successResponse({orgs,considered,assigned}). Use dynamic import('@/lib/admin/scheduling-queries') chain just like submit-session-request.ts:319 to keep the enum/status chain off module-load.
3) src/lib/admin/reconcile-auto-assign.test.ts — reuse the hoisted db-mock harness from scheduling-queries.test.ts:1-50 (selectQueue/updateQueue chain proxy). Required test from the plan: a previously-unassignable row that now has a free qualified tech gets placed (queue the soft-held row, then queueSuccessfulPlacement-style queue). Plus: window-derivation maps 12:00Z-16:00Z→afternoon; null window → skipped not crashed; already-assigned rows excluded by filter (assert WHERE includes isNull(assignedTo)); tenant scope present.

OPT-IN DECISION (call out): autoAssignBookedRequest already runs first-fit even when auto_dispatch_enabled=false (scheduling-queries.ts:854-860), so the original at-booking attempt is NOT gated on the flag. The plan text says 'respects the opt-in flag.' To stay surgical and consistent with the at-booking behavior, the sweep should NOT add a NEW flag gate that the live path lacks (that would diverge). RECOMMEND: gate the whole sweep on isAutoDispatchEnabled(orgId) ONLY if the user wants reconcile to be opt-in (a stronger, retried action than a one-shot); otherwise mirror the live path (no flag). Surface this as the one design choice needing confirmation. Default in plan: gate on isAutoDispatchEnabled to honor the plan's explicit 'respects the opt-in flag' wording — re-attempting placement is a more aggressive behavior than the one-shot at-booking attempt, so opt-in is the safer default.

DEGRADE-SAFETY: per-org try/catch (one bad org never aborts the sweep), reconcile fn itself wraps each row so one bad row doesn't abort the org. neon-http: NO transactions used — autoAssignBookedRequest already issues sequential single-statement writes (no db.transaction); the sweep adds only selects + reuses that path, so db.batch is not required. TENANT SCOPING: every query uses withTenant(serviceRequests, organizationId, ...); the org loop bounds each call to one org. No frozen-safety-text / money-safety / metadata.verify surfaces are touched (dispatch-only).

VERIFY GATES: npm run tsc; npm run lint; npm run test:unit (new reconcile test + existing scheduling-queries suite green); npm run build. No prompt edits → no eval needed. Add the cron to vercel.json crons (e.g. schedule '*/30 * * * *' or hourly) — a low-frequency sweep, since at-booking after() handles the common case.

CONFIRM-FIRST OPEN QUESTIONS: (1) opt-in gating (above); (2) lookback window length + future-only bound (proposed 48h created, future arrivalWindowEnd) — confirm so the sweep can't reprocess long-stale rows; (3) cron cadence in vercel.json.

**Key risks:**
- Window reconstruction is lossy: WINDOW_HOURS has overlapping bounds — anytime[8,20] shares its start (8) with morning and its end (20) with evening, but is the only 12-hour span, so match on BOTH start and end hours; a stored window whose hours match none (custom dispatcher-set window) must be skipped (window=null), not coerced — otherwise placeAndAssignRequest gets a wrong window.
- Opt-in semantics mismatch: the live at-booking path is NOT gated on auto_dispatch_enabled (runs first-fit regardless, scheduling-queries.ts:854-860); the plan says the sweep should 'respect the opt-in flag.' Picking the wrong default either silently diverges from the live path or makes the sweep a no-op for every org that never set the flag. Needs explicit confirmation.
- Idempotency/race: a dispatcher may manually assign a row between the sweep's SELECT and autoAssignBookedRequest's placeAndAssignRequest. The existing guarded UPDATE in placeAndAssignRequest + markAutoAssigned's assignedTo guard (scheduling-queries.ts:766-774) mitigate, but the sweep must not stamp autoAssigned over a human assignment — relying on the assignedTo IS NULL re-read inside placeAndAssignRequest, not the stale sweep snapshot.
- Unbounded scope without a recency/future bound would have the sweep re-attempt every historical unassigned soft-held row every run (wasteful + could place a tech on a past window). Must bound on future arrivalWindowEnd and a createdAt lookback.

**Independent verifier — minor-corrections:**
- WINDOW RECONSTRUCTION IS WRONG (central plan flaw). The plan derives the arrival window from arrivalWindowStart.getUTCHours() matched against WINDOW_HOURS in arrival-window.ts (the UTC-anchored table: morning[8,12]/afternoon[12,16]...). But the live booking path does NOT store UTC-anchored windows. submit-session-request.ts:92 builds heldSlot via arrivalWindowForSlot (capacity-hold.ts:111-120), which uses businessWallClockToUtc (Eastern wall-clock, DST-aware) -- the same convention as calendar-time.ts:250 arrivalWindowUtcForBusinessDate, NOT arrival-window.ts arrivalWindowForDate. Proof: the existing test scheduling-queries.test.ts:736-743 stores a 'morning' window as start=2026-07-01T12:00:00Z, end=16:00:00Z. Under the plan's getUTCHours() match (12->afternoon), a stored morning window would be mislabeled 'afternoon', and DST varies the offset (4h summer/5h winter) so there is NO fixed UTC-hour->window mapping. The reconstruction must read the hour in the BUSINESS timezone (invert businessWallClockToUtc), not getUTCHours(). The plan names the wrong helper as single source of truth.
- isAutoDispatchEnabled IS NOT EXPORTED. scheduling-queries.ts:725 declares 'async function isAutoDispatchEnabled' (module-private, no export). The plan proposes the new src/lib/admin/reconcile-auto-assign.ts gate the sweep on isAutoDispatchEnabled(orgId); that file cannot import an unexported function. Plan must add an export (or place the gate inside scheduling-queries). Minor but would not compile as written.
- Minor line-ref drift: the docstring the plan cites at scheduling-queries.ts:822-825 ('Best-effort: returns {assigned:false}...') is actually lines 815-826 (the JSDoc above autoAssignBookedRequest); the quoted sentence spans ~823-824. Substance correct, line range slightly off. Similarly the ACTIVE_BOOKING_STATUSES citation '52-58' is the const array body (52-58 OK).
- keyRisk #1 (overlapping window bounds) misdiagnoses the real reconstruction hazard. The genuine problem is timezone anchoring (Eastern wall-clock storage vs UTC-table match) + DST offset variance, not just the anytime[8,20] overlap. The overlap concern is secondary to the wrong-helper bug.

_Verifier notes:_ Status is correctly real-gap: independently confirmed no reconcile/auto-assign cron in src/app/api/cron/ (listing), no entry in vercel.json:2-43, and the at-booking attempt (submit-session-request.ts:313-345) is a one-shot after() with only a log on failure -- no retry. The gap is genuinely open. Most cited refs are accurate and the opt-in analysis (live at-booking path is NOT gated on auto_dispatch_enabled; scheduling-queries.ts:854 only gates SCORING, first-fit runs regardless) is correct and a good catch. The cron skeleton, verifyCronAuth (cron-auth.ts:28, fails closed), booking-recovery template, autoAssigned column (schema.ts:484), markAutoAssigned assignedTo guard (766-775), and tenant-scoping approach all check out and are feasible. Verdict is minor-corrections (not flawed) because the gap finding and overall architecture are sound, but the plan's core data-derivation step (window reconstruction via getUTCHours) would silently mis-place jobs due to the Eastern/DST storage convention, and one referenced gate function is not exported. Both must be fixed before implementation; neither invalidates the plan's structure. No frozen-safety-text / money-safety / neon-http-transaction / tenant-scoping invariant is violated by the plan as written (dispatch-only, selects + reuse of existing sequential-write path).

---

## Stage 16 — Real-time availability (PTO / sick / live load)  _(plan)_

**Verified status:** real-gap  **·  Verify verdict:** sound  **·  Effort:** M

**Evidence (re-confirmed against code):**
- src/lib/db/schema.ts:1043 — technician_availability is ONLY "recurring weekly working hours per technician"; no PTO/sick/date-specific column or table exists
- Global grep for pto|sick|time.?off|vacation|on.?leave across src/lib + src/app (non-test) returns ZERO real hits — every match is a node:crypto import; no time-off concept exists anywhere in the codebase
- src/lib/admin/scheduling-queries.ts:618-642 — the conflict+availability gate in placeAndAssignRequest checks only checkScheduleConflict (overlapping jobs) and isWindowWithinAvailability (weekly hours via getTechnicianAvailability); a tech on PTO with no overlapping job and matching weekly hours STILL passes the gate
- src/lib/admin/scheduling-queries.ts:837-845 — autoAssignBookedRequest pulls candidates by (role=technician, isActive=true) only; nothing excludes a tech who is off that day
- src/lib/ai/dispatch/signals.ts:120-137 — the sameDayJobCount load query counts assigned jobs in [dayStart,dayEnd) but never reads availability; score.ts:49 turns it into the load penalty with no off-day exclusion
- src/lib/ai/dispatch/score.ts:35,49 — LOAD_CAP=6 same-day-jobs is the entire "live load" signal; there is no PTO/sick input to the score
- src/lib/admin/queries.ts:807-835 — getTechnicians (board source) returns all role=technician rows ordered by name; no time-off filter/flag, so a tech on PTO still renders on the board as bookable
- src/lib/admin/scheduling-queries.test.ts + dispatch/*.test.ts — grep for pto|sick|time.?off|unavailable returns nothing; the Stage-16 verify test ("a tech on PTO is excluded from auto-assign and the board") does not exist
- drizzle/meta/_journal.json — last entry idx 21 (0021_tearful_echo); next hand-authored migration is 0022 (NOTE: the session-start git-status snapshot showing 0034/0035 + many ?? files is STALE; actual tree max migration is 0021 and the FieldPulse availability-sync files are already git-tracked at HEAD)
- src/lib/integrations/fieldpulse/availability-sync.ts:1-17 + availability-mapping.ts — the recent FieldPulse availability-sync only mirrors working HOURS into the same technician_availability weekly table (grep for pto|sick|time.?off in mapping = zero); it does NOT introduce PTO/sick and does not close this gap

**Plan:**

GAP IS OPEN. No time-off model exists; PTO/sick is not folded into the conflict gate, auto-assign candidate set, load signal, or the board. The FieldPulse availability-sync work (weekly hours mirror) is adjacent but orthogonal.

CONCRETE PLAN (file-level):

1) Schema + migration (hand-authored, neon-http convention):
- src/lib/db/schema.ts: add `technicianTimeOff` pgTable "technician_time_off": id uuid PK; organizationId uuid notNull FK organizations onDelete cascade; technicianId uuid notNull FK users; startsOn date notNull, endsOn date notNull (inclusive, business-TZ calendar dates — model whole-day PTO/sick, not partial hours, matching weekly-hours granularity); reason enum (pto|sick|other) notNull default 'pto'; note text nullable; createdAt/updatedAt timestamptz defaultNow notNull. Indexes: (organizationId), (organizationId, technicianId), and (organizationId, technicianId, startsOn, endsOn) for the range probe. Mirror the existing block-comment style at schema.ts:1043.
- drizzle/0022_technician_time_off.sql: hand-author CREATE TABLE + indexes (the journal/snapshot dance per memory \"hand-authored-trigger-migration\" — append entry idx 22 to drizzle/meta/_journal.json and copy 0021_snapshot.json → 0022_snapshot.json with the new table added). Do NOT rely on drizzle-kit generate (Next.js 16 fork + hand-authored convention). Remember Vercel build skips migrations — note `npm run db:migrate` must run post-merge (memory \"migrations-not-run-on-deploy\").

2) Time-off query helper — new file src/lib/admin/time-off-queries.ts:
- `isTechnicianOffOn(orgId, technicianId, isoDay): Promise<boolean>` — withTenant(technicianTimeOff, orgId, eq(technicianId), lte(startsOn, isoDay), gte(endsOn, isoDay)); LIMIT 1, return rows.length>0. isoDay is the business-TZ calendar date already used throughout scheduling (heldSlot.isoDay / options.isoDay), so compare date-to-date — no UTC offset math needed.
- `techniciansOffOn(orgId, isoDay, candidateIds): Promise<Set<string>>` — single batched IN-query (inArray(technicianId, candidateIds)) returning the off set, for the auto-assign loop and signals (avoid N+1).
- CRUD: `listTimeOff(orgId, technicianId?)`, `createTimeOff`, `deleteTimeOff` for the admin surface (validate startsOn<=endsOn; reject cross-tenant technicianId via the same role+isActive+org guard used in placeAndAssignRequest:597-613).

3) Fold into the gate — src/lib/admin/scheduling-queries.ts:
- In placeAndAssignRequest's gate (line 618 block, inside `if (!options.override && targetTech)`): add `const off = await isTechnicianOffOn(organizationId, targetTech, options.isoDay);` and include in the reject: `if (conflicts.length > 0 || !within || off)` → add `outsideAvailability` sibling `onTimeOff: off` to the returned `detail`. Override path (line ~688) recomputes for audit symmetrically — add the same off recompute so the override audit records it. Keep reason \"conflict\" so autoAssignBookedRequest's existing try-next-tech loop (line 891: reason===\"conflict\" continues) naturally skips an off tech.
- In autoAssignBookedRequest (line 837): after fetching active techs, pre-filter with `techniciansOffOn(orgId, heldSlot.isoDay, firstFit)` and drop the off set from `firstFit` BEFORE ranking — cheaper than relying on the per-tech gate reject, and keeps the ranked order clean. (Belt-and-suspenders: the gate still rejects, so a race where PTO is added between filter and place is still safe.)

4) Load signal — src/lib/ai/dispatch/signals.ts / score.ts:
- Stage-16 says fold into the load signal. Minimal, correct approach: since off techs are already excluded from the candidate set in step 3, the score never sees them — that satisfies \"excluded from auto-assign\". Do NOT hack sameDayJobCount. If a softer \"reduce live capacity\" signal is desired it's out of scope; the verify gate only requires exclusion.

5) Board — src/lib/admin/queries.ts:807 getTechnicians:
- Add an optional `isoDay?: string` param; when present, batch-probe time-off and add `offToday: boolean` to TechnicianRecord (update types in src/lib/admin/types.ts). The board route passes the viewed day so an off tech renders with an \"off\" badge / is non-droppable. Keep the no-arg signature working (offToday defaults false) so existing callers (queries.ts:1121,1211) are untouched — surgical.

6) Admin surface (minimal): a time-off CRUD route under src/app/api/admin/technicians/[id]/time-off/route.ts (params is a Promise per Next.js 16 fork: `const { id } = await params`). getAdminSession gate (admin+). Reuse the API response envelope.

7) Tests (TDD, vitest):
- src/lib/admin/time-off-queries.test.ts: isTechnicianOffOn true on boundary dates (startsOn and endsOn inclusive), false just outside; tenant isolation (off row in org B never matches org A).
- Extend src/lib/admin/scheduling-queries.test.ts: REQUIRED verify test — a tech on PTO for heldSlot.isoDay is dropped from autoAssignBookedRequest candidates and never assigned (the Stage-16 acceptance test). Plus: gate rejects a manual place onto an off tech with detail.onTimeOff true; override bypasses it.
- Note vitest baseline: DB-dependent suites fail at import without .env.local (memory \"vitest-env-known-failures\") — keep the time-off logic unit-testable with mocked db like the existing dispatch tests.

DEGRADE-SAFETY: the off-probe is a plain read; wrap the auto-assign pre-filter so a probe error logs and falls back to NOT-filtering (never strand a job the old path would have placed) — but the gate reject still protects the manual path. TENANT SCOPE: every query via withTenant(technicianTimeOff, orgId, ...). NEON-HTTP: all single statements / batched IN-queries; no db.transaction — the schema.ts:1043 setTechnicianAvailability uses db.batch for its replace, follow that pattern if any multi-write CRUD is added. No prompt/safety-text changes → no eval gate needed. VERIFY GATES: tsc, lint, test:unit (new + existing scheduling tests green), build.

**Key risks:**
- isoDay semantics: time-off dates and the scheduling layer's isoDay are both business-TZ calendar dates — comparing them as date strings is correct, but a careless UTC conversion would misfire across the DST boundary; reuse businessWeekday/businessWallClockToUtc helpers (availability-coverage.ts:31) and store endsOn INCLUSIVE to avoid an off-by-one on the last PTO day
- Override path must recompute time-off for audit symmetry (scheduling-queries.ts:~688) or an overridden assignment onto an off tech is silently un-audited
- getTechnicians is consumed by multiple overview/board callers (queries.ts:1121,1211) — adding a required param would break them; the isoDay param must be optional with offToday defaulting false to stay surgical
- Stage-16 says fold into the load signal too; the plan satisfies exclusion via candidate pre-filter rather than mutating sameDayJobCount — if a reviewer reads the spec literally as "reduce capacity in the score", scope creep risk; recommend confirming exclusion-only is acceptable
- Vercel skips migrations on deploy (memory) — 0022 must be applied via npm run db:migrate post-merge or the new reads 500 on schema drift

**Independent verifier — sound:**
- Minor scope-tension (not an error, already flagged in keyRisks): Stage-16 title literally says 'live load' and 'fold into the load signal', but the plan deliberately satisfies the gate via candidate pre-filter rather than mutating sameDayJobCount. The assessment is internally honest about this and recommends confirming exclusion-only is acceptable, so it is not a defect — just the one place a literal reading of the spec could call the plan incomplete.
- Minor precision: plan step 3 says add onTimeOff as a 'sibling' to detail, but detail is typed by the exported ScheduleConflictDetail interface (scheduling-queries.ts:485-487), so it requires editing that interface (and ideally making onTimeOff optional or defaulted) — additive and safe (the sole consumer, src/app/api/admin/requests/[id]/reschedule/route.ts:134-141, only reads outsideAvailability/conflicts), but the plan should explicitly note the interface edit.

_Verifier notes:_ Independently verified every cited file:line against HEAD.

STATUS CONFIRMED real-gap (open):
- schema.ts:1043 — technicianAvailability is recurring weekly hours only (dayOfWeek/startMinute/endMinute); no date-specific/PTO/sick column or table. Confirmed verbatim incl. the block comment they cite.
- Global grep pto|sick|time.?off|vacation|on.?leave across src/lib+src/app (non-test) returns ZERO real hits — every match is a node:crypto import. Confirmed.
- scheduling-queries.ts:618-640 gate checks only checkScheduleConflict + isWindowWithinAvailability; an off tech with no overlap + matching weekly hours passes. Confirmed (lines 619-641).
- autoAssignBookedRequest (837+) pulls candidates by role=technician+isActive only; no off-day exclusion. Confirmed (lines 840-846).
- signals.ts sameDayJobCount load query (cited 120-137; actual block 124-137, dayStart/dayEnd at 63-64) counts assigned jobs, never reads availability. score.ts:35,49 LOAD_CAP=6 is the only load signal; no PTO input. Confirmed.
- queries.ts:807-833 getTechnicians returns all role=technician ordered by name, no time-off flag; consumed at 1121,1211. Confirmed.
- Test files: no pto|sick|time.?off|unavailable|onTimeOff hits; the Stage-16 verify test does not exist. Confirmed.
- Journal: last entry idx 21 (0021_tearful_echo); migration files top out at 0021. The assessment's NOTE that the session-start git-status snapshot (0034/0035 + many ?? FieldPulse files) is STALE is CORRECT — git ls-files shows availability-sync.ts IS already tracked at HEAD, and availability-mapping.ts has zero pto/sick hits (orthogonal weekly-hours mirror). This is a notable catch: a less careful reviewer would have trusted the stale snapshot and mis-scoped the migration number.

PLAN FEASIBILITY CONFIRMED:
- ScheduleConflictDetail interface exists (485-487) → onTimeOff is a one-field additive extension; sole route consumer reads only outsideAvailability/conflicts so won't break.
- options.isoDay exists (558) and is business-TZ calendar date → date-to-date compare is valid.
- db.batch neon-http pattern confirmed (setTechnicianAvailability line 203) for any multi-write CRUD; matches memory neon-http-no-transactions.
- withTenant is the tenant primitive (used throughout) → tenant scoping plan is correct.
- autoAssign loop continues on reason==='conflict' (and technician_not_found), breaks otherwise (lines 893-895) → keeping reason:'conflict' for off techs makes the try-next-tech skip work as the plan claims.
- TechnicianRecord at types.ts:79 → adding optional offToday is surgical; keeping getTechnicians no-arg signature preserves callers at 1121,1211.
- Memory cross-refs (hand-authored migration journal/snapshot dance, Vercel-skips-migrations, vitest .env.local baseline) are all accurate.

No invariant violations: no transactions used, tenant-scoped, no frozen-safety-text/prompt changes, no money-path touch. Effort M is reasonable. Verdict: sound.

---

## Stage 17 — Technician push notification (tech_assigned)  _(plan)_

**Verified status:** real-gap  **·  Verify verdict:** minor-corrections  **·  Effort:** M

**Evidence (re-confirmed against code):**
- src/lib/db/schema.ts:1547-1568 — communicationTriggerTypeEnum has NO 'tech_assigned' member (customer-facing triggers only: technician_enroute/arrived are about-the-customer messages, not staff-facing)
- src/lib/db/schema.ts:258-326 — users table has email/name/passwordHash/googleId/fieldpulseUserId/role/laborRateCents but NO phone column
- src/lib/db/schema.ts:1643-1712 — communication_jobs has no idempotency/dedupe column; indexes are org/status-scheduled/service_request/customer/external_id only
- src/lib/db/schema.ts:1766-1786 — outbound_message_ledger.customerId is .notNull() FK to customers, so it cannot dedupe a tech (non-customer) notification
- src/lib/admin/scheduling-queries.ts:870-886 — successful auto-assign path: placeAndAssignRequest.ok -> markAutoAssigned -> return; no notification enqueue anywhere
- src/lib/admin/scheduling-queries.ts:761-772 — markAutoAssigned does an unconditional UPDATE (no guard), so a re-run can re-trigger the assign path -> dup-send risk -> idempotency key needed
- src/lib/requests/submit-session-request.ts:313-344 — autoAssignBookedRequest is invoked inside after() (the only prod caller); confirms background-task entry point
- src/lib/communication/consent.ts:130-132 — checkSendAllowed returns {allowed:true} early when customerId is null: a tech job queued with customerId=null already bypasses customer consent at the send chokepoint
- src/lib/communication/consent.ts:46,161 — TRIGGER_RULES is Record<CommTrigger,TriggerRule>; adding tech_assigned to the enum is a compile error unless a rule entry is added
- src/lib/communication/job-queue.ts:26-59 — queueCommunicationJob encrypts recipientPhone at rest and accepts triggerType as string; reusable for the tech send path
- grep: no 'tech_assigned'/'techAssigned'/'notifyTech' anywhere in src/ — feature does not exist
- drizzle/meta/_journal.json — last entry idx 21 (0021_tearful_echo); next hand-authored migration is 0022 (the 0034/0035 in git status are uncommitted artifacts from another branch, absent from this checkout)

**Plan:**

GAP IS OPEN. Concrete file-level plan (degrade-safe, tenant-scoped, neon-http: no interactive txns — use db.batch where atomicity matters; background work already runs in after()).

1) MIGRATION (hand-authored 0022 + snapshot). drizzle/0022_tech_assigned_notification.sql:
   - ALTER TABLE users ADD COLUMN phone text; (nullable — most techs may have none; send path must no-op when null)
   - ALTER TYPE communication_trigger_type ADD VALUE 'tech_assigned'; (Postgres enum add-value; NOTE: ADD VALUE cannot run in a txn block — fine on neon-http single statements, but keep it as its own statement)
   - ALTER TABLE communication_jobs ADD COLUMN idempotency_key text;
   - CREATE UNIQUE INDEX communication_jobs_org_idempotency_unique ON communication_jobs (organization_id, idempotency_key) WHERE idempotency_key IS NOT NULL;
   Copy drizzle/meta/0021_snapshot.json -> 0022_snapshot.json with the three changes applied; append entry idx 22 to _journal.json (mirror the hand-authored-trigger-migration memory note). Run npm run db:migrate after (migrations are NOT run on Vercel deploy — per memory).

2) schema.ts: add phone: text('phone') to users (after laborRateCents, ~line 287); add 'tech_assigned' to communicationTriggerTypeEnum (end of array ~1567); add idempotencyKey: text('idempotency_key') to communicationJobs + the partial uniqueIndex in the table-config array (~1699-1711).

3) job-queue.ts queueCommunicationJob: add optional idempotencyKey?: string to the param type + .values(); on the unique-constraint race wrap the insert and treat 23505 as 'already enqueued' (return existing/no-op) so a double auto-assign sends once. Keep customerId optional/undefined for tech jobs.

4) consent.ts TRIGGER_RULES: add tech_assigned: { toggle: null, quietHours: false } (mirrors 'escalation' line 57 — a staff operational alert; the toggle:null + null-customerId early-return at line 130 both keep it un-suppressed). This also satisfies the Record<CommTrigger> exhaustiveness check so tsc passes.

5) seeds.ts: add a default SMS template { key:'tech_assigned_sms', triggerType:'tech_assigned', templateType:'sms', bodyTemplate: '{{companyName}}: New job assigned — {{serviceType}} on {{date appointmentDate \"short\"}} {{appointmentTime}} at {{appointmentAddress}}. Ref {{referenceNumber}}.' } so a seeded org has a body.

6) NEW src/lib/communication/triggers.ts: export triggerTechAssigned({organizationId, technicianId, technicianName, serviceRequestId, ...vars}). Look up the active tech_assigned SMS template (eq org+trigger+sms+isActive); load the tech's users.phone (tenant-scoped withTenant(users, orgId, eq(users.id, technicianId))); if no template OR no phone -> return (degrade-safe, no throw). Else queueCommunicationJob({ ..., recipientPhone: tech.phone, customerId: undefined, triggerType:'tech_assigned', idempotencyKey: `tech_assigned:${serviceRequestId}:${technicianId}` }).

7) scheduling-queries.ts:871 — right after markAutoAssigned (inside the result.ok block, before/after the decision log), enqueue: do NOT add network/throw to the hot loop — wrap in try/catch and log-only on failure (the surrounding caller already runs in after()). Pass referenceNumber + window vars (fetch from the request row already in scope or a cheap select). The idempotency key makes a re-assign of the same (request,tech) a single notification.

TESTS (vitest, TDD):
   - consent.test.ts: tech_assigned with null customerId -> allowed:true (bypass).
   - job-queue: second insert with same idempotencyKey -> single row / 23505 swallowed.
   - scheduling-queries.test.ts (the existing autoAssignBookedRequest suite ~735): on successful scored + first-fit assign, exactly ONE tech_assigned job enqueued; a tech with null phone -> zero jobs (degrade-safe); running autoAssign twice for the same request -> still one job (idempotent). Mock the triggers module.
   - triggers test: missing template -> no enqueue, no throw.

VERIFY GATES: npx tsc --noEmit; npm run lint; npm run test:unit (consent/job-queue/scheduling suites); npm run build. No prompt changes -> no eval needed. No frozen-safety-text (hvac-knowledge.ts), money-safety, or metadata.verify surfaces are touched. NOTE the vitest-env baseline: scheduling-queries.test.ts may be in the DB-dependent set that fails at import without .env.local — confirm it currently runs; if DB-bound, assert via the trigger-module mock rather than a live insert.

**Key risks:**
- Postgres ALTER TYPE ... ADD VALUE 'tech_assigned' cannot run inside a transaction block and the new value isn't usable in the same txn it's added — keep it a standalone migration statement; since migrations don't auto-run on Vercel deploy (per memory), the enum value must exist in prod before any code references it or inserts 500 on the enum cast.
- Idempotency key scope: keying on (serviceRequestId, technicianId) means a legitimate REASSIGN back to the same tech later would be silently suppressed — acceptable for auto-assign-once, but if a future manual-reassign should re-notify, the key needs a nonce/timestamp component.
- users.phone is plaintext PII unlike every other contact field in this schema (customer phone is encrypted+blind-indexed). Decide: store encrypted (matches at-rest convention, needs decrypt at send) vs plaintext (simpler, staff-internal). Plaintext is a minor consistency break worth a deliberate call.
- Enqueue must stay strictly degrade-safe and non-blocking inside autoAssignBookedRequest's success path — a comms failure must never roll back or block the assignment (wrap in try/catch, log-only); the caller's after() already isolates it from the request turn.
- neon-http has no interactive transactions — if you want the assign+enqueue atomic, use db.batch; but log-only degrade-safety is preferable here, so atomicity is not actually required.
- checkSendAllowed bypass relies on customerId=null; if a future caller passes a customerId for a tech job it would route through customer consent — document that tech jobs MUST omit customerId.

**Independent verifier — minor-corrections:**
- Evidence item 'scheduling-queries.ts:761-772 — markAutoAssigned does an unconditional UPDATE (no guard)' is inaccurate. The UPDATE at lines 763-775 DOES carry a guard: it is tenant-scoped via withTenant and conditioned on and(eq(serviceRequests.id, requestId), eq(serviceRequests.assignedTo, technicianId)). It is not an idempotency guard against re-notification, so the downstream conclusion (idempotency key needed for the notification send) still holds — but the literal 'no guard' wording is wrong.
- Minor wording: the assessment's evidence repeatedly says the send path uses 'recipientPhone' while the schema column is 'recipientPhoneEncrypted'. This is NOT an error in the plan — queueCommunicationJob (job-queue.ts:31) correctly accepts a plaintext recipientPhone param and encrypts to recipientPhoneEncrypted at line 49. Flagging only because the two names appear side-by-side; the plan's reuse of recipientPhone is correct.
- Plan step 5 references 'seeds.ts' without a path; the actual file is src/lib/communication/seeds.ts. It exists and matches the described shape (objects with key/triggerType/templateType:'sms'/bodyTemplate), so the step is feasible — just specify the full path.

_Verifier notes:_ Independently verified every cited line. Status 'real-gap' is CORRECT and well-supported:

- schema.ts:1547-1568 — communicationTriggerTypeEnum confirmed; NO tech_assigned member (last is warranty_expiring). CORRECT.
- schema.ts:258-326 — users table confirmed: id/orgId/email/name/passwordHash/googleId/fieldpulseUserId/role/isActive/laborRateCents/timestamps. NO phone column. CORRECT.
- schema.ts:1643-1712 — communication_jobs confirmed: no idempotency/dedupe column; indexes are exactly org_id / status_scheduled (partial) / service_request / customer / external_id. CORRECT.
- schema.ts:1766-1786 — outbound_message_ledger.customerId is .notNull() FK to customers. CORRECT (cannot dedupe a non-customer tech send).
- scheduling-queries.ts:870-886 — success path: result.ok -> markAutoAssigned -> optional decision log -> return; NO notification enqueue. CORRECT.
- submit-session-request.ts:313-344 — autoAssignBookedRequest invoked inside after() (dynamic import). CORRECT, confirms background entry point.
- consent.ts:130-132 — checkSendAllowed returns {allowed:true} when customerId is null. CORRECT (tech jobs with customerId=undefined bypass customer consent at both enqueue and the send-time gate in job-queue.ts:120-125).
- consent.ts:46,57,161 — TRIGGER_RULES is Record<CommTrigger,TriggerRule>; escalation is {toggle:null,quietHours:false}; rule lookup at 161 — so adding tech_assigned to the enum WITHOUT a TRIGGER_RULES entry is a tsc exhaustiveness error. CORRECT, and the proposed escalation-mirroring rule is sound.
- job-queue.ts:26-59 — queueCommunicationJob encrypts recipientPhone at rest, accepts triggerType as string, customerId optional. CORRECT and reusable as planned.
- grep — no tech_assigned/techAssigned/notifyTech anywhere in src/. CONFIRMED, feature absent. No existing staff-notify-on-assign in the communication layer.
- _journal.json — last idx is 21 (0021_tearful_echo); next is 0022. CORRECT (the 0034/0035 in git status are from another branch's drizzle/ naming and not in meta).

Plan feasibility CONFIRMED:
- ALTER TYPE ... ADD VALUE is a precedented pattern in this exact enum (drizzle/0001 added estimate_sent/payment_receipt/invoice_overdue; drizzle/0008 added warranty_expiring). The 'ADD VALUE cannot run in a txn block' caveat and the 'enum value must exist in prod before code references it (migrations not run on Vercel deploy)' risk are both accurate and important.
- Driver is drizzle-orm/neon-http (db/index.ts:2) — the 'no interactive transactions, use db.batch' note is accurate; db.batch is already used across the codebase.
- The 23505 unique-constraint dedupe approach, the degrade-safe try/catch enqueue in after(), and the tenant-scoped users.phone lookup are all feasible against the real code.

The three key risks I'd weight highest: (1) enum value must be migrated to prod before deploy or inserts 500 on the cast — real, since Vercel skips migrations; (2) plaintext users.phone breaks the encrypt+blind-index convention used for customer.phone — a genuine consistency decision; (3) idempotency key (serviceRequestId, technicianId) silently suppresses a legitimate manual reassign-back-to-same-tech — acceptable for auto-assign-once but worth the documented caveat. All three are already called out.

Net: the gap is genuinely open, the plan is feasible and respects the codebase invariants (tenant scoping, neon-http no-txn, no frozen-safety/money surfaces touched). Downgraded from 'sound' only for the inaccurate 'no guard' description of markAutoAssigned and two cosmetic path/naming nits.

---

## Stage 18 — Customer "your technician is X" messaging  _(plan)_

**Verified status:** real-gap  **·  Verify verdict:** sound  **·  Effort:** M

**Evidence (re-confirmed against code):**
- src/lib/db/schema.ts:1547-1568 — communication_trigger_type enum has NO tech-assigned/your-technician value (technician_enroute, technician_arrived, job_completed exist; nothing for the assignment moment)
- grep for tech_assigned/technician_assigned/'your technician' across src returns only knowledge-base-coverage.test.ts:1 — no template, trigger, or enum anywhere in production code
- src/lib/communication/consent.ts:46-68 — TRIGGER_RULES Record<CommTrigger,...> has no assignment entry; consent.ts:161-162 reads TRIGGER_RULES[triggerType] then rule.toggle — a missing entry is a runtime crash, so any new enum value MUST be added here
- src/lib/communication/triggers.ts:351-393 (triggerTechnicianEnroute) — the template the trigger to mirror; no triggerTechnicianAssigned exists
- src/lib/admin/queries.ts:378-476 assignTechnician (manual, flips status pending→assigned, returns tech.name) and reassignTechnician:497 — the CONFIRMED-assignment path the customer message must hook; route at src/app/api/admin/requests/[id]/assign/route.ts:62 (POST) and :145 (PATCH)
- src/lib/admin/scheduling-queries.ts:827 autoAssignBookedRequest runs in after() as PROVISIONAL auto-assign — plan explicitly says do NOT message the customer here (avoid naming a tech before the team confirms)
- src/lib/communication/seeds.ts:264-291 seedCommunicationTemplates is per-key idempotent (skips existing key, inserts missing) — adding a default template DOES backfill via this fn; BUT seeds.ts:312 seedAllOrganizationTemplates skips any org that already has ANY template, so it will NOT backfill the new key
- drizzle/0001_money_loop_triggers.sql:1-3 + drizzle/0008_equipment_warranty.sql:1 — established hand-authored `ALTER TYPE ... ADD VALUE` pattern; latest committed migration is drizzle/0021_tearful_echo (journal idx 21), so next is 0022 (note: 0034/0035 in the prompt's git-status snapshot are stale/another branch — not on disk)
- drizzle/meta/0021_snapshot.json:8765-8783 — enum members enumerated in snapshot; a new value requires editing this snapshot + journal by hand (drizzle-kit can't generate ADD VALUE reliably)

**Plan:**

GAP IS OPEN. No enum value, template, trigger fn, or enqueue call for a customer "your technician is X" message exists. Concrete file-level plan:

1. MIGRATION (hand-authored) — drizzle/0022_tech_assigned_customer_msg.sql: `ALTER TYPE \"public\".\"communication_trigger_type\" ADD VALUE 'technician_assigned';`. Add the journal entry (idx 22, tag 0022_...) and edit drizzle/meta/0022_snapshot.json (copy 0021's, append 'technician_assigned' to the communication_trigger_type values array). Standalone ADD-VALUE migration — no table DDL mixed in (neon-http auto-commits each statement; ADD VALUE cannot run inside a tx, which is fine here since neon-http has no interactive transactions anyway). Run `npm run db:migrate` after (per MEMORY: Vercel build skips migrations).

2. SCHEMA — src/lib/db/schema.ts:1567: add `\"technician_assigned\",` to communicationTriggerTypeEnum (after warranty_expiring or grouped with technician_* values). This propagates to CommTrigger type.

3. CONSENT RULE (mandatory, else runtime crash at consent.ts:161) — src/lib/communication/consent.ts:46-68: add `technician_assigned: { toggle: \"automatedConfirmations\", quietHours: false },`. Transactional confirmation (customer is waiting to know who's coming), exempt from quiet hours, gated by automatedConfirmations — matching technician_enroute. tsc enforces this (Record<CommTrigger,...> exhaustive).

4. TEMPLATE SEED — src/lib/communication/seeds.ts: add an entry to defaultTemplates: key `technician_assigned_sms`, triggerType `technician_assigned`, templateType `sms`, bodyTemplate e.g. `{{companyName}}: {{technicianName}} has been assigned to your {{serviceType}} on {{date appointmentDate \"short\"}}{{#if appointmentTime}} at {{appointmentTime}}{{/if}}. We'll text when they're on the way. Questions? Call {{phoneNumber}}.`, variables map, priority ~50. Backfill existing orgs by calling seedCommunicationTemplates(orgId) per org (NOT seedAllOrganizationTemplates — it skips orgs that already have templates, seeds.ts:312). Add a one-off backfill script or fold into the existing provisioning re-seed path (provisioning.ts:290 already calls seedCommunicationTemplates on new orgs).

5. TRIGGER FN — src/lib/communication/triggers.ts: add `triggerTechnicianAssigned(params: { organizationId, serviceRequestId, customerId, customerName, customerPhone?, technicianName, serviceType, appointmentDate, appointmentTime?, companyName, phoneNumber })` mirroring triggerTechnicianEnroute:351-393 — findFirst active sms template for trigger 'technician_assigned', guard on template+customerPhone, queueCommunicationJob(triggerType:'technician_assigned', channel:'sms', priority ~60, customerId, serviceRequestId). Consent/quiet-hours is enforced automatically at send time via TRIGGER_RULES (job-queue.ts:119-122 / consent.ts:161-168) — do NOT re-implement consent here.

6. WIRE the CONFIRMED path only — in the assign route POST handler (src/app/api/admin/requests/[id]/assign/route.ts) after a successful assignTechnician (line 62-97), enqueue the trigger inside Next's after() (NOT a detached promise — Vercel freeze; per MEMORY serverless-background-work). Load the customer's decrypted phone + appointment fields (assignTechnician already returns assignedToName + customerName; you'll need a small extra read for phone/serviceType/scheduledDate — add to queries.ts return or a follow-up select). Decision needed: wire on POST only (initial assignment) — reassignTechnician/PATCH messaging is a judgment call; default to POST-only to match 'confirmed assignment' and avoid double-notifying on every reassign. Do NOT wire into autoAssignBookedRequest (scheduling-queries.ts:827) — that's provisional.

7. TESTS (vitest, TDD) — new src/lib/communication/triggers.test.ts case (or extend existing): confirmed assignment with an active template + consenting customer → exactly ONE pending technician_assigned sms job; assert customerId/serviceRequestId/triggerType. Add consent.test.ts case: technician_assigned with automatedConfirmations=false → not allowed; with do_not_contact → not allowed; quietHours ignored (exempt). Degrade-safety test: no template present → zero jobs, no throw.

VERIFY GATES: npm run lint, tsc (--noEmit), npm run test:unit (the consent+triggers suites; note MEMORY 'vitest-env-known-failures' — DB-dependent suites fail at import as baseline, isolate your unit assertions from a live DB or mock db), npm run build. No prompt/hvac-knowledge.ts changes → no eval needed; frozen-safety-text invariant untouched. Tenant scoping: every read/write uses withTenant or org-scoped eq (assignTechnician already does). neon-http: single inserts only, no db.transaction; if batching seed+enqueue use db.batch.

**Key risks:**
- Forgetting the TRIGGER_RULES entry (consent.ts:46) crashes at send time on TRIGGER_RULES[triggerType].toggle — but tsc's exhaustive Record<CommTrigger> catches a missing entry at build, so the gate protects you IF you run tsc
- Backfill blind spot: seedAllOrganizationTemplates (seeds.ts:312) skips orgs that already have any template, so the new SMS template won't reach existing tenants through it; must call seedCommunicationTemplates(orgId) per org (it IS per-key idempotent at seeds.ts:274)
- Double-notification risk if the trigger is also wired into reassignTechnician/PATCH or into the provisional autoAssignBookedRequest (scheduling-queries.ts:827) — plan says CONFIRMED assignment only; wire POST/assignTechnician path exclusively
- Background send must use after() not a detached promise (Vercel freeze, per MEMORY serverless-background-work); customer phone is encrypted at rest so the enqueue path must pass the decrypted phone (queueCommunicationJob re-encrypts at job-queue.ts:49)
- Migration won't auto-run on Vercel deploy (MEMORY: migrations-not-run-on-deploy) — must run npm run db:migrate manually or the new enum value is absent and inserts referencing 'technician_assigned' 500
- Stale migration numbering in the prompt's git-status (0034/0035) — they are not on disk; real next number is 0022 off main@0021

**Independent verifier — sound:**
- Path imprecision (cosmetic, not wrong line): assessment step 4/risk cites 'provisioning.ts:290' for the per-org re-seed call; the actual file is src/lib/admin/provisioning.ts:290 (import at :12). Line number and seedCommunicationTemplates(organizationId) call are correct.
- Migration note worth tightening: the established multi-ADD-VALUE pattern (drizzle/0001_money_loop_triggers.sql) puts each ALTER TYPE ... ADD VALUE on its own statement separated by '--> statement-breakpoint'. That breakpoint separation is what makes it safe under neon-http (each statement auto-commits; ADD VALUE can't run in a tx). The plan's 'standalone ADD-VALUE migration, no table DDL mixed in' is correct, but if a future variant needs the value usable in the SAME migration, Postgres forbids using a freshly-added enum value in the same transaction — not an issue here since it's standalone.

_Verifier notes:_ Independently verified every cited file:line. Status real-gap is CORRECT: grep across src/ for technician_assigned/tech_assigned/'your technician' returns only test files (knowledge-base-coverage.test.ts:55 'background checked', and the prompt-cited test) — no enum value, template, trigger fn, consent rule, or enqueue in production code.

Confirmed citations:
- schema.ts:1547-1568 communicationTriggerTypeEnum has no assignment value (technician_enroute/arrived/job_completed/warranty_expiring present) — EXACT.
- consent.ts:46-68 TRIGGER_RULES Record<CommTrigger,...>; consent.ts:161-162 reads TRIGGER_RULES[triggerType].toggle — a missing entry IS a runtime crash, and tsc's exhaustive Record<CommTrigger> catches a missing key at build. Both claims hold.
- triggers.ts:351-393 triggerTechnicianEnroute is the correct mirror; uses recipientPhone: params.customerPhone, queues with priority 70, customerId+serviceRequestId — matches plan step 5.
- queries.ts:378-476 assignTechnician (flips pending/scheduled→assigned via guarded UPDATE, returns assignedToName=tech.name + customerName; does NOT return phone/serviceType/scheduledDate, so plan's 'small extra read for phone/serviceType' is accurate). reassignTechnician at :497.
- assign/route.ts: POST calls assignTechnician at :62, PATCH calls reassignTechnician at :145 — EXACT. POST-only wiring recommendation is sound.
- scheduling-queries.ts:827 autoAssignBookedRequest is the provisional after()-run auto-assign — correctly flagged do-not-message.
- seeds.ts:261-292 seedCommunicationTemplates is per-key idempotent (skips existing key per org); seeds.ts:297-322 seedAllOrganizationTemplates skips any org with ANY template (existingCount.length>0) — backfill blind-spot claim is CORRECT and important.
- Migrations: latest on disk is drizzle/0021_tearful_echo (journal idx 21); next is 0022. 0034/0035 in git-status are NOT on disk (confirmed ls). ADD VALUE pattern in 0001 and 0008 confirmed.
- 0021_snapshot.json:8765-8783 enumerates the 15 enum values ending warranty_expiring — EXACT; a new value needs hand-editing snapshot+journal.
- job-queue.ts:49 encrypts recipientPhone; consent enforced at SEND time (checkSendAllowed) at job-queue.ts ~120 — plan's 'consent enforced automatically, don't re-implement' is correct.
- CommTrigger = typeof communicationTriggerTypeEnum.enumValues[number] (consent.ts:23) — adding the enum value propagates the type, forcing the TRIGGER_RULES entry. Plan's compile-time safety claim holds.

All 7 plan steps are feasible against the real code. No invariant violations (no frozen-safety-text or hvac-knowledge.ts touched; tenant scoping via withTenant already present in assignTechnician; neon-http no-tx respected by standalone ADD VALUE). keyRisks are all real and well-targeted. Effort M is reasonable. Only nits are the abbreviated provisioning.ts path and a clarifying note on enum-value same-tx usage — neither changes the plan.

---

## Stage 19 — Membership plans edit flow (detail page + PATCH)  _(plan)_

**Verified status:** already-closed  **·  Verify verdict:** sound  **·  Effort:** S

**Evidence (re-confirmed against code):**
- src/app/api/admin/membership-plans/[id]/route.ts:22-78 — PATCH handler: session gate, adminMutation rate-limit, awaits `context.params` (Next 16 Promise), org-scoped existence check via getMembershipPlanById, zod updateSchema (all fields optional), empty-body 400 guard, calls updateMembershipPlan, audit-logged
- src/lib/admin/membership-queries.ts:104-127 — updateMembershipPlan(): builds partial setFields, no-ops on empty, `db.update(membershipPlans).set({...,updatedAt}).where(withTenant(membershipPlans, organizationId, eq(id)))` — tenant-scoped, single statement (no transaction needed)
- src/lib/admin/membership-queries.ts:86-98 — getMembershipPlanById(): withTenant org+id scoped read used as the PATCH existence/404 guard
- src/app/admin/(dashboard)/membership-plans/page.tsx:21-24,57-66 — handleEditClick sets `editing` and opens MembershipPlanFormDialog with the plan; edit UI is wired
- src/components/admin/memberships/membership-plan-form-dialog.tsx:69,103-112 — isEditMode branch sends method PATCH to `/api/admin/membership-plans/${editing.id}`; form pre-populated via createFormState(editing)
- git log 85c9b6a -- src/app/api/admin/membership-plans/[id]/route.ts — PATCH + update query landed in 'feat(memberships): plans + enrollment v1', i.e. BEFORE the parity program; plan's premise ('route.ts has POST only') was stale
- src/lib/db/schema.ts:2443-2462 — customerMemberships has NO price_cents column; plan price is never snapshotted at enrollment

**Plan:**

GAP IS CLOSED. The plan's stated gap ("plans can be created/deleted but not edited — no PATCH; route.ts has POST only") is factually wrong against current code. PATCH exists at src/app/api/admin/membership-plans/[id]/route.ts:22 alongside DELETE, backed by updateMembershipPlan() in membership-queries.ts:104. The edit UI is fully wired: list page -> handleEditClick -> MembershipPlanFormDialog in edit mode -> PATCH fetch. All of this was committed in 85c9b6a, predating the parity program — an earlier stale audit.

Conventions are respected: Next 16 Promise params (`await context.params`), withTenant org-scoping on both the existence read and the update write, single-statement update (no neon-http transaction needed), audit logging with .catch degrade-safety, adminMutation rate-limit.

Two deviations from the plan's literal wording, neither a real gap:
1. No dedicated `/admin/membership-plans/[id]` page — editing is an in-list modal dialog (membership-plan-form-dialog.tsx, edit mode). This is functionally equivalent CRUD completeness; the dedicated detail-page URL was a 'nicety' framing (cf. the program's own NOT-in-scope note about request detail URL pages being a 'UX nicety'). No reason to add a separate route page.
2. 'Guard active-subscription side effects' — PATCH does not block price edits on plans with active enrollments. This is SAFE in v1: customerMemberships has no price_cents snapshot column (schema.ts:2443-2462), price is read live only via getActiveMembership's join, and v1 has no auto-renew/charge cron (queries.ts:8-11 header). So a price edit produces zero retroactive money effect. A guard becomes relevant only when recurring billing ships.

Minor follow-up (NOT a parity blocker, do not re-open the stage): no unit/integration test directly exercises updateMembershipPlan or the PATCH route. membership-queries.test.ts covers enroll/cancel/getActive only (lines 76,158,175); getMembershipPlanById appears solely as a mock-seq stub (test lines 81,103). The plan's verify criterion ('unit/integration test for update') is the one unmet item. If desired, add: (a) a query unit test asserting updateMembershipPlan builds the correct withTenant-scoped set on partial input and no-ops on empty; (b) a route integration test (mirror tests/api/admin/technicians.test.ts PATCH pattern at line 163) for 401, 404-on-cross-org, 400-on-empty-body, and 200-happy-path. Effort S, additive-only, no production code change.

**Key risks:**
- If recurring auto-renew billing is added later, PATCH price edits will start affecting active members at next renewal — at that point add an active-subscription guard or price-snapshot-at-enrollment. Track with the Stripe-gating work, not this stage.
- Verify gate gap: no test covers the update path; the stage's own 'unit/integration test for update' criterion is unmet, though the production code is correct and typechecks clean (tsc shows no membership errors).

**Independent verifier — sound:**
- Stage-number / source-doc mismatch (not a code error, but worth flagging): the membership work lives at SERVICETITAN-PLAN.md Stage 10 ('Memberships, Reporting/Scorecards & Job Costing'), and PRODUCT-PLAN.md Stage 19 is 'Performance & scale'. The 'Stage 19 — Membership plans edit flow' framing and the verbatim gap wording ('route.ts has POST only', 'Guard active-subscription side effects', 'detail page + PATCH') do not appear in any .md in the repo. The assessment's premise-is-stale conclusion is still correct against code, but I could not locate the source parity document to confirm the exact original gap text it claims to refute.
- Minor evidence-line drift: the assessment cites form-dialog 'createFormState(editing)' and 'isEditMode branch PATCH' at lines 69,103-112; actual locations are line 70 (isEditMode), 77 (setForm(createFormState(editing))), 110-114 (url/method PATCH selection). Substance is correct, line numbers slightly off.

_Verifier notes:_ Independently confirmed all load-bearing code claims:

PATCH EXISTS: src/app/api/admin/membership-plans/[id]/route.ts:22 — session gate (27-30), adminMutation rate-limit (32-39), `await context.params` Next-16 Promise (41), org-scoped 404 via getMembershipPlanById (42-45), zod updateSchema all-optional (14-20), empty-body 400 guard (51-53), calls updateMembershipPlan (55), audit-logged with .catch degrade (57-69). DELETE coexists at line 78. Accurate.

QUERY: membership-queries.ts:102-124 updateMembershipPlan builds partial setFields, no-ops empty (119), single db.update().set({...,updatedAt}).where(withTenant(org, eq(id))) — tenant-scoped, single statement, no transaction. getMembershipPlanById:88-98 withTenant org+id read. Accurate.

UI WIRED: page.tsx:21-24 handleEditClick sets editing+opens dialog; passes editing to MembershipPlanFormDialog (63-68). form-dialog isEditMode branch (70) sends PATCH to /api/admin/membership-plans/${editing.id} (110-114), pre-populated via createFormState(editing) (77). Accurate.

SCHEMA: customerMemberships (schema.ts:2443+) has NO price_cents column — confirmed. Plan price read live via getActiveMembership join only (queries.ts:171-175). No auto-renew/charge cron (header comment 8-11; currentPeriodEnd comment 2460-2462). So PATCH price edits have zero retroactive money effect in v1 → the missing active-subscription guard is genuinely safe, not a money-safety bug. Assessment correct.

GIT: PATCH landed in 85c9b6a 'feat(memberships): plans + enrollment + member-pricing (Stage 10/11 v1)' — `git show 85c9b6a:.../route.ts` shows PATCH at line 21 in that commit. Predates any later parity audit. Accurate.

TEST GAP: membership-queries.test.ts imports only enrollCustomer, cancelMembership, getActiveMembership (lines 3-5); getMembershipPlanById appears only as mock-seq stub. No test imports or exercises updateMembershipPlan, and there is no PATCH route integration test. The assessment's 'one unmet verify criterion = update-path test' is correct; technicians.test.ts:163 PATCH pattern is a valid template.

Conclusion: gap is genuinely closed in production code; all conventions (Next-16 params, withTenant on read+write, single-statement update respecting neon-http no-transactions, audit logging, rate-limit) verified. The only real shortfall is test coverage of the update path — additive, non-blocking, accurately characterized. Verdict sound.

---

## Stage 20 — Tech portal mutations (photos / notes / timeline)  _(plan)_

**Verified status:** partially-done  **·  Verify verdict:** minor-corrections  **·  Effort:** M

**Evidence (re-confirmed against code):**
- src/app/api/tech/jobs/[id]/note/route.ts:43 — addFieldNote tech-scoped POST already ships (notes DONE)
- src/lib/tech/field-queries.ts:280 addFieldNote inserts into requestNotes with assignee+tenant guard via findOwnedJob (field-queries.ts:40 eq(serviceRequests.assignedTo, techUserId))
- src/app/api/tech/jobs/[id]/signature/route.ts:59-94 — R2 upload pattern (validateFile/generateStorageKey/uploadFile, ownership-then-record, orphan cleanup) is the reusable photo-upload template
- find src/app/api/tech: only note/materials/signature/status/timesheet — NO photo route
- grep photo/timeline/attachment in src/app/tech + src/components/tech: zero hits (no photo UI, no timeline UI)
- src/components/tech/tech-job-detail-client.tsx:222-243 handlePostNote is POST-only fire-and-forget; no notes/timeline render (grep for 'Timeline'/'Notes' headings = 0 matches)
- src/lib/db/schema.ts:1392 attachments.serviceRequestId already exists (nullable FK) + storageKey/filename/mimeType/size — photo storage needs NO migration
- src/lib/db/schema.ts:592 request_notes has authorId/content/createdAt — timeline source #1; serviceRequests has status history-adjacent fields

**Plan:**

Gap is PARTIALLY OPEN. The plan's claim 'no /api/tech/* mutation endpoints' is STALE — note POST (src/app/api/tech/jobs/[id]/note/route.ts) plus materials/signature/status/timesheet all ship. NOTES = DONE. Still genuinely missing: (1) tech photo upload, (2) a timeline/activity view. Both are M effort because the storage + ownership-guard patterns already exist to copy.\n\nPLAN:\n\n1) PHOTO UPLOAD — reuse attachments table (no migration; serviceRequestId/storageKey columns exist at schema.ts:1392).\n   - field-queries.ts: add `addJobPhoto(orgId, techUserId, serviceRequestId, {filename, mimeType, size, storageKey})` -> {ok:true,id}|{ok:false,reason:'not_owned'|'invalid_input'}. MUST call findOwnedJob(orgId, techUserId, id) FIRST (mirror addFieldNote:294), then `db.insert(attachments).values({organizationId, serviceRequestId, filename, mimeType, size, storageKey}).returning({id})`. Also `listJobPhotos(orgId, serviceRequestId)` -> select from attachments where serviceRequestId AND organizationId (withTenant scope), ordered by createdAt; owned-job pre-check.\n   - NEW route src/app/api/tech/jobs/[id]/photo/route.ts: copy signature/route.ts almost verbatim (runtime='nodejs', force-dynamic, getAdminSession, slidingWindow `tech:photo:${userId}` w/ RATE_LIMITS.adminMutation, formData file, storageClient.validateFile(file, MAX_FILE_SIZE), generateStorageKey(orgId, id, file.name), uploadFile). Verify ownership BEFORE upload concern: signature route uploads then deletes orphan on not_owned (signature/route.ts:84-94) — replicate that exact orphan-cleanup so a non-assignee can't leave files in R2. Add GET to list photos (mirror materials GET shape).\n   - Client tech-job-detail-client.tsx: add a Photos section + file input + POST(FormData) handler mirroring handleSign (no canvas), and render listed photos via the GET.\n\n2) TIMELINE — read-only aggregation, no new table.\n   - field-queries.ts: add `getJobTimeline(orgId, techUserId, serviceRequestId)` that owned-checks then merges: request_notes (createdAt, author, content), attachments-for-request (createdAt, filename as 'photo added'), and serviceRequests status/signedAt/completedAt timestamps into a typed `TimelineEntry[] = {kind:'note'|'photo'|'status', at:Date, label, detail?}` sorted desc. neon-http: these are independent SELECTs (no writes) so just await them (or db.batch for read parallelism) — NO transaction needed.\n   - NEW route src/app/api/tech/jobs/[id]/timeline/route.ts: GET only, mirror materials GET (session, owned-check inside query, successResponse(entries)).\n   - Client: render a Timeline section consuming the GET.\n\nTESTS (TDD, vitest, mirror field-queries.test.ts harness at field-queries.test.ts:1-40 — mock @/lib/db, mockSelectSeq):\n   - addJobPhoto: (a) not_owned -> {ok:false} and NO insert; (b) owned -> insert called with org+serviceRequestId, returns id.\n   - listJobPhotos / getJobTimeline: not_owned short-circuits; owned returns merged sorted entries.\n   - API integration (mirror tests/api/admin/technicians.test.ts + the signature/note route style): photo route 401 no session, 404 not_owned (assert storageClient.deleteFile called for orphan cleanup), 201 happy path; timeline route 404 not_owned, 200 entries.\n\nDEGRADE-SAFETY: photo upload must fail closed (404 not_owned) and clean up R2 orphan; timeline is read-only. TENANT SCOPE: every query carries organizationId + assignedTo guard (findOwnedJob). neon-http: no db.transaction(); the only multi-write (none here — single insert) so safe; reads use db.batch if parallelizing.\n\nVERIFY GATES: npx tsc --noEmit; npm run lint; npm run test:unit (new field-queries + route tests green); npm run build. No prompts touched -> no eval needed. No new migration (attachments.serviceRequestId pre-exists) -> skip db:migrate, but confirm attachments index attachments_service_request_idx (schema.ts:1409) covers the list query.

**Key risks:**
- Stage premise is stale: notes already ship (note/route.ts) — do NOT rebuild them; only photos + timeline remain
- Orphan-R2 risk: must replicate signature route's upload-then-delete-on-not_owned (signature/route.ts:84-94) or non-assignees leave files in storage
- attachments.serviceRequestId is nullable with no-op-on-delete FK (schema.ts:1392) — deleting a job will NOT cascade-delete photo rows; acceptable for read-only tech view but note for cleanup jobs
- validateFile allowlist is jpeg/png only (signature/route.ts:60) — field photos are fine, but if HEIC from iOS is desired the allowlist must be widened (currently NOT)
- Timeline must stay read-only aggregation; tempting to add a status-change write path — out of scope, status route already exists

**Independent verifier — minor-corrections:**
- keyRisk #4 (HEIC) is technically correct but understates the fix: r2-client's ALLOWED_TYPES DOES include image/heic (r2-client.ts:76-77, image/webp at 58) and there is a ready-made ADMIN_DOCUMENT_MIME_TYPES constant (r2-client.ts:117-123) containing jpeg/png/webp/heic/pdf. The signature route gets jpeg/png-only only because it calls validateFile(file, MAX_FILE_SIZE) with NO 3rd arg, falling back to the PUBLIC_UPLOAD_MIME_TYPES default (r2-client.ts:112-115, 300). So the fix for iOS HEIC is trivial: the new photo route should pass ADMIN_DOCUMENT_MIME_TYPES as validateFile's 3rd argument — no allowlist 'widening' of r2-client needed, contrary to the implication that HEIC support requires editing the allowlist.
- Timeline plan misses a better source: the assessment proposes deriving status entries from serviceRequests status/signedAt/completedAt timestamp columns, but a dedicated request_status_events table exists (schema.ts:1798-1820) with full transition history (fromStatus/toStatus/actorType/actorId/at + indexes request_status_events_request_idx). That is the authoritative status-timeline source and yields multiple ordered transitions instead of a single current-status snapshot. Plan is still feasible as written, but the chosen source is inferior.
- Minor mismatch between signature route's doc-comment and behavior (not the assessment's error, but relevant to the 'copy verbatim' instruction): the route comment at signature/route.ts:8-9/69-70 says it verifies ownership BEFORE uploading, but the code actually uploads first (line 72) THEN deletes the orphan if recordSignature returns not_owned (lines 84-94). The assessment's evidence/plan correctly describe the real upload-then-delete behavior, so copying it is fine — just don't trust the misleading comment.
- Plan's test-harness citation 'field-queries.test.ts:1-40' / 'mockSelectSeq' is real and accurate (mockSelectSeq defined ~line 37+), but note the existing test file imports addJobMaterial/removeJobMaterial/getActualMaterialsCostCents only — it does NOT currently exercise addFieldNote or recordSignature, so the new photo/timeline query tests will be net-new patterns in that file, not extensions of existing cases.

_Verifier notes:_ Core assessment is SOUND and independently confirmed. Status 'partially-done' is correct: NOTES ship end-to-end (src/app/api/tech/jobs/[id]/note/route.ts:43 POST -> addFieldNote field-queries.ts:280-310, assignee+tenant guarded via findOwnedJob at 28-48 with eq(serviceRequests.assignedTo, techUserId) at line 42). The plan's 'no /api/tech/* mutation endpoints' staleness call is accurate — note/materials/signature/status/timesheet all exist (verified via find). Photo upload and timeline are GENUINELY missing: zero photo route, zero photo/timeline UI in src/components/tech (grep confirmed; handlePostNote at tech-job-detail-client.tsx:222-243 is POST-only fire-and-forget, no notes/timeline render). All cited file:line refs verified accurate: signature orphan-cleanup at signature/route.ts:84-94; attachments.serviceRequestId nullable FK at schema.ts:1392-1394 (no-op-on-delete, confirming keyRisk #3); attachments_service_request_idx partial index at schema.ts:1409-1411 (covers the list query as claimed). r2-client exports (validateFile/generateStorageKey/uploadFile/deleteFile/MAX_FILE_SIZE/getStorageClient) all exist. Plan is feasible and would compile; tenant scoping (withTenant + assignee guard), neon-http no-transaction note, and read-only timeline constraint are all sound. Effort 'M' is reasonable. Two refinements (use ADMIN_DOCUMENT_MIME_TYPES for HEIC; use request_status_events for timeline) make the plan better but are not blockers — hence minor-corrections rather than sound.

---

