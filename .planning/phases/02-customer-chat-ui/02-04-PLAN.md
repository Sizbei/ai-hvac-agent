---
phase: 02-customer-chat-ui
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/app/error.tsx
  - src/app/chat/error.tsx
  - src/app/chat/loading.tsx
  - src/app/chat/success/error.tsx
  - src/components/chat/chat-error-fallback.tsx
  - src/components/ui/loading-skeleton.tsx
autonomous: true
requirements:
  - SC-UI-12

must_haves:
  truths:
    - "Root error boundary catches uncaught errors and shows retry button"
    - "Chat route has its own error boundary with chat-specific messaging"
    - "Chat route has loading skeleton shown during initial page load"
    - "Success page has error boundary"
    - "Loading skeleton visually resembles the chat layout (header + message area + input)"
  artifacts:
    - path: "src/app/error.tsx"
      provides: "Root-level error boundary"
      min_lines: 15
    - path: "src/app/chat/error.tsx"
      provides: "Chat-specific error boundary"
      min_lines: 15
    - path: "src/app/chat/loading.tsx"
      provides: "Chat loading skeleton"
      min_lines: 15
    - path: "src/app/chat/success/error.tsx"
      provides: "Success page error boundary"
      min_lines: 10
    - path: "src/components/chat/chat-error-fallback.tsx"
      provides: "Reusable error display component for chat context"
      min_lines: 15
    - path: "src/components/ui/loading-skeleton.tsx"
      provides: "Chat-shaped loading skeleton component"
      min_lines: 20
  key_links:
    - from: "src/app/chat/error.tsx"
      to: "src/components/chat/chat-error-fallback.tsx"
      via: "imports and renders"
      pattern: "ChatErrorFallback"
    - from: "src/app/chat/loading.tsx"
      to: "src/components/ui/loading-skeleton.tsx"
      via: "imports and renders"
      pattern: "ChatLoadingSkeleton"
---

<objective>
Add error boundaries and loading skeletons to all pages. Every route gets an error.tsx, the chat route gets a loading.tsx with a skeleton that matches the chat layout shape.

Purpose: Ensure no uncaught error shows a blank screen. Users see a friendly error message with retry option. Initial page loads show skeleton UI instead of blank content.
Output: Error boundaries for root, chat, and success routes. Loading skeleton for chat route. Reusable error fallback and skeleton components.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-customer-chat-ui/02-CONTEXT.md
@AGENTS.md

Read the Next.js 16 docs before writing any code:
- node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md (error boundary API -- uses unstable_retry NOT reset)
- node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/loading.md (loading convention)

<interfaces>
From src/components/ui/skeleton.tsx (installed by shadcn in Plan 01):
```typescript
// shadcn Skeleton component -- renders an animated placeholder block
// Usage: <Skeleton className="h-4 w-[250px]" />
```

From src/components/ui/button.tsx (installed by shadcn in Plan 01):
```typescript
// shadcn Button with variants: default, destructive, outline, secondary, ghost, link
```

IMPORTANT Next.js 16 change for error.tsx:
```typescript
// error.tsx props use unstable_retry (NOT reset):
export default function Error({
  error,
  unstable_retry,
}: {
  error: Error & { digest?: string };
  unstable_retry: () => void;
}) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create reusable error fallback and loading skeleton components</name>
  <files>src/components/chat/chat-error-fallback.tsx, src/components/ui/loading-skeleton.tsx</files>
  <read_first>
    - src/components/ui/skeleton.tsx (shadcn skeleton component)
    - src/components/ui/button.tsx (shadcn button component)
    - src/components/ui/card.tsx (shadcn card component)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md (error boundary API)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/loading.md (loading convention)
  </read_first>
  <action>
1. Create src/components/chat/chat-error-fallback.tsx:
   - "use client" directive
   - Props: `{ error: Error & { digest?: string }; onRetry: () => void; context?: string }`
   - Centered layout with flex flex-col items-center justify-center gap-4 p-8
   - Lucide AlertTriangle icon in orange/warning color, w-12 h-12
   - Heading: "Something went wrong" in text-xl font-semibold
   - Context-aware message: if context is "chat", show "We couldn't connect to the chat service. Please try again." If context is "success", show "We couldn't load the confirmation page." Default: "An unexpected error occurred."
   - Error digest display (if present): small text-muted-foreground showing `Error ID: {digest}` for support reference
   - "Try Again" button (shadcn Button, variant="default") calling onRetry
   - "Go Home" link (shadcn Button, variant="outline", as Link to "/")
   - Do NOT show error.message to users (could leak internals) -- only show the context-aware message

2. Create src/components/ui/loading-skeleton.tsx:
   - "use client" directive (optional -- skeleton can be server component, but keeping consistent)
   - Export ChatLoadingSkeleton component -- mimics the chat layout:
     ```
     +---------------------------+
     | [skeleton] HVAC Assistant | <- header skeleton
     |                           |
     | [skeleton bubble left]    | <- assistant message skeleton
     | [skeleton bubble left]    |
     |     [skeleton bubble right]| <- user message skeleton
     | [skeleton bubble left]    |
     |                           |
     | [input skeleton] [btn]    | <- input skeleton
     +---------------------------+
     ```
   - Use shadcn Skeleton component for each element:
     - Header: h-14 with Skeleton bars for title and button
     - Message area: 4-5 Skeleton bars of varying widths, alternating left/right alignment
     - Left-aligned bubbles: Skeleton className="h-12 w-3/4 rounded-2xl"
     - Right-aligned bubbles: Skeleton className="h-10 w-1/2 rounded-2xl ml-auto"
     - Input bar: Skeleton className="h-10 flex-1 rounded-md" + small Skeleton for button
   - Full height container: flex flex-col h-dvh max-w-lg mx-auto
   - Animate with subtle pulse (shadcn Skeleton has this built in)
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && ls src/components/chat/chat-error-fallback.tsx src/components/ui/loading-skeleton.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - chat-error-fallback.tsx does NOT display raw error.message to users
    - chat-error-fallback.tsx has "Try Again" button calling onRetry prop
    - loading-skeleton.tsx renders skeleton layout matching chat UI shape (header, messages, input)
    - Both files compile without TypeScript errors
  </acceptance_criteria>
  <done>Reusable error fallback component hides raw errors and shows user-friendly message with retry, chat loading skeleton mimics chat layout shape with shadcn Skeleton components</done>
</task>

<task type="auto">
  <name>Task 2: Create error boundaries and loading files for all routes</name>
  <files>src/app/error.tsx, src/app/chat/error.tsx, src/app/chat/loading.tsx, src/app/chat/success/error.tsx</files>
  <read_first>
    - src/components/chat/chat-error-fallback.tsx (just created)
    - src/components/ui/loading-skeleton.tsx (just created)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md (CRITICAL: verify unstable_retry prop name)
  </read_first>
  <action>
1. Create src/app/error.tsx (root error boundary):
   - "use client" directive (required by Next.js for error boundaries)
   - Receives props: `{ error: Error & { digest?: string }; unstable_retry: () => void }`
   - CRITICAL: Next.js 16 uses `unstable_retry` NOT `reset`. Verify in the docs file.
   - Renders ChatErrorFallback with context="root" and onRetry={unstable_retry}
   - useEffect to console.error the error for debugging (developers only)
   - Full page layout: min-h-dvh flex items-center justify-center

2. Create src/app/chat/error.tsx (chat-specific error boundary):
   - "use client" directive
   - Receives props: `{ error: Error & { digest?: string }; unstable_retry: () => void }`
   - Renders ChatErrorFallback with context="chat" and onRetry={unstable_retry}
   - useEffect to console.error the error
   - Chat-sized container: max-w-lg mx-auto h-dvh flex items-center justify-center

3. Create src/app/chat/loading.tsx (chat loading skeleton):
   - This can be a server component (no "use client" needed unless ChatLoadingSkeleton requires it)
   - Simply renders: `<ChatLoadingSkeleton />`
   - Import ChatLoadingSkeleton from "@/components/ui/loading-skeleton"

4. Create src/app/chat/success/error.tsx (success page error boundary):
   - "use client" directive
   - Receives props: `{ error: Error & { digest?: string }; unstable_retry: () => void }`
   - Renders ChatErrorFallback with context="success" and onRetry={unstable_retry}
   - Centered layout
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && ls src/app/error.tsx src/app/chat/error.tsx src/app/chat/loading.tsx src/app/chat/success/error.tsx && grep -q "unstable_retry" src/app/error.tsx && grep -q "unstable_retry" src/app/chat/error.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 error/loading files exist
    - All error files use "use client" directive
    - All error files receive unstable_retry prop (NOT reset -- this is Next.js 16)
    - Chat loading.tsx renders ChatLoadingSkeleton
    - All error boundaries use ChatErrorFallback component
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Root, chat, and success routes all have error boundaries using unstable_retry (Next.js 16 API), chat route has loading skeleton, all compile without errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Error object -> UI | Error details must not leak to user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | I (Information Disclosure) | error boundaries | mitigate | ChatErrorFallback shows generic message, never raw error.message; digest shown only as opaque ID |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. All error boundary files use `unstable_retry` (not `reset`)
3. All error boundaries are client components ("use client")
4. Chat loading.tsx renders skeleton UI
5. No raw error.message shown to users in any error boundary
</verification>

<success_criteria>
- Root error boundary (src/app/error.tsx) catches top-level errors
- Chat error boundary (src/app/chat/error.tsx) catches chat-specific errors
- Success error boundary (src/app/chat/success/error.tsx) catches success page errors
- Chat loading skeleton (src/app/chat/loading.tsx) shows during initial page load
- Error boundaries show user-friendly messages (never raw error details)
- All error boundaries have retry functionality via unstable_retry
- Loading skeleton visually matches chat layout (header, message bubbles, input)
</success_criteria>

<output>
After completion, create `.planning/phases/02-customer-chat-ui/02-04-SUMMARY.md`
</output>
