---
phase: 02-customer-chat-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/globals.css
  - components.json
  - src/lib/design-tokens.ts
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/input.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/scroll-area.tsx
autonomous: true
requirements:
  - SC-UI-01
  - SC-UI-13

must_haves:
  truths:
    - "shadcn/ui is initialized with New York style and slate base color"
    - "motion (framer-motion) is installed and importable from motion/react"
    - "Inter font is loaded via next/font/google and applied to root layout"
    - "Design tokens define blue primary (#2563EB), orange accent (#F97316), slate backgrounds"
    - "Core shadcn/ui components (button, card, dialog, input, badge, skeleton, scroll-area) are available"
  artifacts:
    - path: "components.json"
      provides: "shadcn/ui configuration"
      contains: "new-york"
    - path: "src/app/globals.css"
      provides: "CSS theme variables for the design system"
      contains: "--primary"
    - path: "src/app/layout.tsx"
      provides: "Root layout with Inter font"
      contains: "Inter"
    - path: "src/lib/design-tokens.ts"
      provides: "Shared design constants (colors, animation durations)"
      exports: ["COLORS", "ANIMATION"]
    - path: "src/components/ui/button.tsx"
      provides: "shadcn Button component"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import ./globals.css"
      pattern: "globals\\.css"
    - from: "src/app/globals.css"
      to: "CSS custom properties"
      via: "@theme inline directive"
      pattern: "--color-primary"
---

<objective>
Initialize the design system foundation: shadcn/ui with New York style + slate base, motion animation library, Inter font, and design tokens matching the HVAC brand colors (blue primary, orange accent, slate backgrounds).

Purpose: All subsequent UI plans depend on these components, tokens, and CSS variables being in place. This must be done first.
Output: components.json, updated globals.css with theme, updated layout.tsx with Inter font, design-tokens.ts, and 7 core shadcn/ui components installed.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-customer-chat-ui/02-CONTEXT.md
@AGENTS.md

Read the Next.js 16 docs at node_modules/next/dist/docs/ before writing any code per AGENTS.md instructions. Specifically check:
- node_modules/next/dist/docs/01-app/01-getting-started/13-fonts.md (font setup)
- node_modules/next/dist/docs/01-app/01-getting-started/11-css.md (CSS/Tailwind v4)

<interfaces>
<!-- Existing root layout to be modified -->
From src/app/layout.tsx:
```typescript
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
// Uses Geist font with variable CSS custom properties
// body has: className="min-h-full flex flex-col"
```

From src/app/globals.css:
```css
@import "tailwindcss";
/* Uses Tailwind v4 @theme inline directive */
/* Currently has Geist font references */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, initialize shadcn/ui, and add core components</name>
  <files>package.json, components.json, src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/dialog.tsx, src/components/ui/input.tsx, src/components/ui/badge.tsx, src/components/ui/skeleton.tsx, src/components/ui/scroll-area.tsx</files>
  <read_first>
    - package.json (current dependencies)
    - node_modules/next/dist/docs/01-app/01-getting-started/11-css.md (CSS/Tailwind v4 patterns)
  </read_first>
  <action>
1. Install motion (animation library -- NOT framer-motion, it has been renamed):
   ```bash
   npm install motion @ai-sdk/react
   ```
   Note: @ai-sdk/react is needed for the useChat hook in later plans. Install it now to avoid repeated installs.

2. Initialize shadcn/ui using the CLI. The project already has Tailwind v4 configured via @tailwindcss/postcss:
   ```bash
   npx shadcn@latest init -y --style new-york --base-color slate
   ```
   If the CLI asks questions interactively, answer:
   - Style: New York
   - Base color: Slate
   - CSS variables: Yes
   - Path alias: @/* (already configured in tsconfig)

3. After init, verify components.json exists and contains "new-york" style and "slate" base color. If the CLI created a different config, manually adjust components.json to use style "new-york" and baseColor "slate".

4. Add core UI components via CLI:
   ```bash
   npx shadcn@latest add button card dialog input badge skeleton scroll-area
   ```

5. Verify all 7 component files exist in src/components/ui/.
  </action>
  <verify>
    <automated>ls src/components/ui/button.tsx src/components/ui/card.tsx src/components/ui/dialog.tsx src/components/ui/input.tsx src/components/ui/badge.tsx src/components/ui/skeleton.tsx src/components/ui/scroll-area.tsx && grep -q "new-york\|new_york\|newYork" components.json && echo "PASS"</automated>
  </verify>
  <done>components.json exists with New York style, all 7 shadcn/ui component files present in src/components/ui/, motion and @ai-sdk/react in package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Configure design tokens, Inter font, and theme CSS</name>
  <files>src/lib/design-tokens.ts, src/app/globals.css, src/app/layout.tsx</files>
  <read_first>
    - src/app/globals.css (current state after shadcn init -- it may have been modified by shadcn)
    - src/app/layout.tsx (current state)
    - components.json (to understand what shadcn added)
    - node_modules/next/dist/docs/01-app/01-getting-started/13-fonts.md (font API for Next.js 16)
  </read_first>
  <action>
1. Create src/lib/design-tokens.ts with shared constants:
   ```typescript
   // Design tokens for the HVAC customer chat UI
   // Colors match the CSS variables in globals.css
   
   export const COLORS = {
     primary: '#2563EB',      // Blue -- trust, professionalism
     primaryLight: '#DBEAFE', // Blue-100 for subtle backgrounds
     accent: '#F97316',       // Orange -- warmth, action CTAs
     accentLight: '#FFF7ED',  // Orange-50 for subtle accent backgrounds
     slate50: '#F8FAFC',
     slate100: '#F1F5F9',
     slate200: '#E2E8F0',
     slate500: '#64748B',
     slate700: '#334155',
     slate900: '#0F172A',
     white: '#FFFFFF',
     success: '#16A34A',      // Green-600 for confirmed states
     error: '#DC2626',        // Red-600 for errors
   } as const;
   
   export const ANIMATION = {
     messageSlideUp: { duration: 0.2, ease: 'easeOut' as const },
     fadeIn: { duration: 0.2, ease: 'easeOut' as const },
     typingPulse: { duration: 0.6, repeat: Infinity, repeatType: 'reverse' as const },
     cardFadeIn: { duration: 0.3, ease: 'easeOut' as const },
   } as const;
   
   export const LAYOUT = {
     maxChatWidth: 'max-w-lg',    // Desktop max width
     mobileBreakpoint: 'md',       // Below this = full-screen chat
   } as const;
   ```

2. Update src/app/globals.css to include HVAC brand colors as CSS custom properties. IMPORTANT: The shadcn init may have already rewritten this file. Preserve whatever shadcn added (especially the @theme section and HSL variables), but ensure:
   - The primary color maps to blue (#2563EB / hsl(217.2 91.2% 59.8%))
   - An accent/orange color is available
   - Keep the slate base that shadcn configured
   - Add a reduced-motion media query that disables transitions:
     ```css
     @media (prefers-reduced-motion: reduce) {
       *, *::before, *::after {
         animation-duration: 0.01ms !important;
         animation-iteration-count: 1 !important;
         transition-duration: 0.01ms !important;
       }
     }
     ```

3. Update src/app/layout.tsx:
   - Replace Geist/Geist_Mono imports with Inter from next/font/google
   - Configure Inter: `const inter = Inter({ subsets: ['latin'] })`
   - Apply to html element: `className={inter.className}` (NOT variable -- Next.js 16 uses className directly per the docs)
   - Keep the existing body classes (min-h-full flex flex-col)
   - Keep the antialiased class on html
   - Update metadata title to "HVAC Customer Service" and description to "AI-powered HVAC service request assistant"
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | tail -5 && grep -q "Inter" src/app/layout.tsx && grep -q "COLORS" src/lib/design-tokens.ts && grep -q "prefers-reduced-motion" src/app/globals.css && echo "PASS"</automated>
  </verify>
  <done>Inter font replaces Geist in layout, design-tokens.ts exports COLORS/ANIMATION/LAYOUT constants, globals.css has blue primary color and prefers-reduced-motion rule, tsc --noEmit passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan is configuration-only with no user input processing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | I (Information Disclosure) | globals.css | accept | CSS variables are public by design; no secrets exposed |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `ls src/components/ui/` shows all 7 components
3. `grep -q "Inter" src/app/layout.tsx` confirms font change
4. `components.json` exists with "new-york" style
5. `npm ls motion @ai-sdk/react` shows both packages installed
</verification>

<success_criteria>
- shadcn/ui initialized with New York style and slate base
- 7 core UI components available (button, card, dialog, input, badge, skeleton, scroll-area)
- motion library installed and importable
- @ai-sdk/react installed (for useChat in Plan 03)
- Inter font applied globally via root layout
- Design tokens file exports COLORS, ANIMATION, LAYOUT constants
- globals.css has HVAC brand primary (blue) color and prefers-reduced-motion
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-customer-chat-ui/02-01-SUMMARY.md`
</output>
